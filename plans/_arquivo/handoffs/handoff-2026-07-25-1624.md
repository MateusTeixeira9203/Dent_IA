# Handoff — 2026-07-25 16:24

> **HANDOFF** · **R-20 → R-21** · sessão 25/07 · modos: execução → commit → discussão → planejamento
> (sessão longa e multi-modo, dirigida pelo Mateus)

## O que trabalhamos

- **R-02 Fase 3** (amarração de `grupo_id` com confirmação) — codado; reabriu a Decisão 2 da spec.
- **Fix do glifo do pino** no odontograma (dois bugs reais).
- **Commit de tudo que estava represado** — 7 commits organizados (R-02/R-04/R-16/R-17/R-18 + migration + plans + config).
- **R-20 (redesenho da ficha)** — debate → spec (planner) → aprovada → codado (3 fases) → validado ao vivo pelo Mateus → committado (2 commits).
- **R-21 (registros por dente)** — debate → spec (planner) → aprovada. Não começou execução.

## O que concluímos

**Pronto e committado (9 commits à frente do origin, SEM deploy):**
- R-04 (encaminhamento), R-16 (filtro), R-02 (ficha viva + amarração de grupo + fix do pino), R-17/R-18 (bugs da auditoria). Verificados ao vivo pelo Mateus 24-25/07 (menos R-02 Fase 3, ver abaixo).
- ✅ **R-20** — odontograma+painel lado-a-lado responsivo (`@container`), tabela de especialidade full-width abaixo, destaque do registro ao clicar o dente (Site A e B). **Validado ao vivo pelo Mateus** (screenshots). Committado (22db484, ab662bb). Componente novo: `OdontogramaComPainel`.

**Não verificado ao vivo (🟡):**
- 🟡 **R-02 Fase 3** — o modal "Continuar/Novo" de amarração de `grupo_id`. Codado, tsc+eslint+build+testes ok, mas o modal em si **nunca foi visto na tela** (o pane oculto não renderiza a ficha; ver Erros). Committado junto do R-02.

**Aprovado, não codado:**
- **R-21** — spec aprovada (`plans/specs/R-21-registros-por-dente.md`). Próximo: mockup dos 3 estados → aprovação visual → Fase 1.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| R-02 Fase 3: amarração de grupo COM confirmação | Silenciosa por estrutura | Fusão silenciosa de 2 procedimentos = "trabalho fantasma" que vaza pro orçamento/PDF; risco clínico inegociável. Reabriu a Decisão 2 da spec (que tinha adiado) |
| R-20: layout responsivo (um código, `@container`) | Dois layouts desktop/mobile | Uma manutenção; e a ficha vira base do modo consulta — container query encaixa na coluna estreita dele sem retrabalho |
| R-20: registros "destacar sem remover" | "Puxar pra fora" (o que o artefato R-01 mostrava) | Estabilidade > foco; lista que reorganiza sozinha assusta; menos código. (O Site A já fazia isso; Fase 3 deu pro Site B) |
| R-20: modo consulta FORA de escopo | Fazer junto | Fechar a ficha primeiro — ela é a base do modo consulta, que reusa o `OdontogramaComPainel` depois |
| R-21: registros agrupados por DENTE, ordem 11→48, dente solo direto/2+ colapsa | Por urgência (feito pra baixo), ou por procedimento | **Validado com um dentista de verdade** — ele pensa por dente. Urgência vira sinal (pill "N a fazer") no dente fechado |
| Commit pragmático (features puras separadas, 3 arquivos-núcleo entrelaçados num commit) | `git add -p` hunk a hunk | Features acumuladas sem commit ficaram entrelaçadas em FichasTab/actions/registro-card; separar por hunk = risco alto num diff de 625 linhas, e são interdependentes |

## Erros e como pensei em resolver

| Erro/obstáculo | Causa/hipótese | Como pensei em resolver | Resolvido? |
|---|---|---|---|
| Browser pane não renderiza a ficha | `visibilityState: hidden` — a aba é tratada como oculta → sem compositing, screenshot dá timeout, geometria zerada, e o fetch da ficha é gated por visibilidade e nem dispara (skeleton eterno) | Não é o código (servidor 200 real). Contorno: o Mateus valida ao vivo no localhost dele (onde o pane compõe), ou Playwright por script. Diagnóstico completo na memória `project-qa-playwright-harness` | Contornado (o Mateus validou tudo pelo lado dele) |
| Glifo do pino "sumia" / "pegava o dente inteiro" | (1) o glifo era desenhado ANTES do `crownPath`, que pintava por cima do núcleo (no colo). (2) `resumo.pino` entrava no `rootTint`, tingindo a raiz inteira | (1) mover o pino pra DEPOIS da coroa. (2) tirar pino do `rootTint` (artefato mostra raiz neutra). Proporções portadas do artefato R-02-simbolos por geometria, não no olho | ✅ (committado) |
| Mateus achou que o agrupamento "não funciona" | Ele olhou 4 canais em 4 dentes DIFERENTES (12/27/34/43) → 4 cards, que é o correto (agrupa por dente, não junta dentes distintos) | Rodei `agruparRegistros` nos dados reais do banco (2 endos no dente 11) e mostrei: 2 eventos → 1 card. E 4 dentes → 4 cards. Provado por output, não por afirmação | ✅ (esclarecido) |

## O que eu estava cogitando

- **Tensão urgência × dente** na lista de registros — não dá pra ter as duas cheias numa lista plana. O dentista resolveu (por dente); a urgência virou sinal no dente fechado. Mas se no dogfood a boca ficar "sem prioridade à vista", vale revisitar.
- **Duas colunas nos registros** (ideia inicial do Mateus) — ficou subsumida no R-21 (agrupar por dente resolve mais). Se voltar, é grid linha-a-linha, não CSS columns.
- **"Trabalho" de verdade** (endo+pino+coroa do mesmo dente como UMA unidade) — o mais clean e clínico, mas o mais caro (exige definir o que é um trabalho). É a visão do cockpit (R-15). O R-21 (agrupar por dente) é um passo nessa direção sem o custo.
- **R-21 mexe em cima do R-20** — a Fase 3 do R-20 (destacar card) vira "abrir o grupo do dente" (Fase 3 do R-21); a tabela sai do full-width-abaixo (R-20 Fase 2) pra dentro do dente (R-21 Fase 2). Evolução, não retrabalho — mas conferir na execução.
- **Contrato do `agruparPorDente`** assume `data.ancoras` no card — conferir na execução que o card expõe todas as âncoras (o dente), senão ajustar a fonte.

## Arquivos alterados (committados nesta sessão)

- **Código (committado):** `Odontograma.tsx` (símbolos+pino), `ToothDetailPanel.tsx` (Fase 3 + R-20 tabela), `FichasTab.tsx` (R-16/R-04/R-02/R-20), `OdontogramaComPainel.tsx` (novo), `consulta-client.tsx`, `actions.ts` (getGruposAbertos), `encaminhar-bar.tsx`, `lib/fichas/`, `lib/odontograma/` (agrupar-registros, grupos-abertos), `get-grupos-abertos.ts`, migration 109, notificações/dashboard (R-04), `tsconfig.json`.
- **Docs (não committado ainda):** `plans/ROADMAP.md` (R-21 ativo), `plans/specs/R-21-registros-por-dente.md` (novo, aprovado). Vão no próximo commit de plans.
