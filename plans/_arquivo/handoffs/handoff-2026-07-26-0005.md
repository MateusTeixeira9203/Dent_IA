> **HANDOFF** · **R-21 + lote não-voz + setup de auditoria** · sessão longa · modo execução → setup de audit

## 1. O que trabalhamos (mais importante primeiro)

- **R-21 (registros por dente) — executado inteiro e validado ao vivo.** Mockup aprovado → Fase 1
  (`agruparPorDente` + testes) → Fase 2 (tabela dentro do dente) → Fase 3 (clique abre+rola). Mateus: "tudo certo".
- **Lote não-voz** (Mateus adiou voz/complexo pra amanhã): **R-12** (contraste AA), **R-10 Parte 1** (jargão),
  **R-19** (barra × dock) — todos codados, gates verdes, **🟡 não vistos na tela**.
- **Dex da ficha rápida está fora do ar** — diagnosticado como ambiente (chave/quota), não código. Adiado.
- **Setup do audit visual do Fable** — brief escrito, harness de captura pronto, 20 screenshots públicas tiradas,
  **travado no login** da captura (ver §5).

## 2. O que concluímos

**Pronto (código) — mas 🟡 até o Mateus ver na tela / deploy:**
- R-21 — **✅ validado ao vivo** (única coisa não-🟡). `src/lib/odontograma/agrupar-por-dente.ts` (+teste 10/10),
  `dente-grupo-header.tsx`, render nos 2 sites do `FichasTab` (`renderSecoesPorDente`, `destacarDente`).
- R-12 🟡 — `--color-text-muted` dark #404040→#8d948d; 15 CTAs + 4 tabs teal → `bg-teal-ink text-surface hover:opacity-90`.
- R-10 P1 🟡 — `derivarV2DosEventos` sem `" - planejado"` (forward-only, caminho manual).
- R-19 🟡 — convenção `--dock-inset` (dock marca `body.has-dock`; barras ancoram na var). voice-ux sobe acima do dock.

**Pela metade / não feito:**
- R-02 símbolos (implante/coroa) — esperando eyeball do Mateus.
- Audit — brief + harness prontos, 20 públicas capturadas; falta a captura logada (login).
- R-10 Parte 2 (obs clínica no orçamento) + Dex "— planejado" — adiados (voz/eval).

## 3. Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| R-21: **manter ordem 11→48**, recency descartado | recency "último no topo" (só Site A) | scroll da Fase 3 já dá acesso rápido sem quebrar o mapa anatômico (validado c/ dentista) |
| R-12: todo CTA teal → `bg-teal-ink text-surface hover:opacity-90` | dock esconde / só produto | AA + 1 tratamento canônico; preview aprovado pelo Mateus |
| R-19: var `--dock-inset` (barras acima do dock) | dock some quando há barra | dock é nav principal, tem que ficar; var mata o número mágico do R-17 |
| R-10: só Parte 1 (status) agora | apagar obs clínica junto | `dentes_observacoes` alimenta orçamento **E** prontuário; strip da obs precisa de decisão |
| Audit: captura com **login manual** do Mateus | codegen storageState / conta de teste | codegen falhou; **eu não digito senha** (regra) — o login é dele |
| Captura via Playwright, não claude-in-chrome | claude-in-chrome (sessão real) | Playwright salva PNGs em arquivo (o Fable precisa ler arquivos); chrome-mcp não |

## 4. Erros e como pensei em resolver

| Erro | Causa/hipótese | Como eu ia resolver | Resolvido? |
|---|---|---|---|
| Pane não renderiza ficha/dashboard | `visibilityState: hidden` no pane embutido | Playwright + chromium empacotado (renderiza de verdade) | sim (pra captura) |
| `auth.json` do codegen "não funcionando" | save relativo/janela aberta/sessão + provavelmente já era o Google | pivô: script headed que espera o Mateus logar e captura sozinho (`capture-audit.cjs`) | superado pelo achado abaixo |
| Captura logada falhou 2x | **Google OAuth não roda em browser automatizado** (bloqueio anti-bot); conta do Mateus é só-Google; sem conta de teste ativa | logar com e-mail/senha (não Google) numa conta de teste com dados na clínica | **sim** — Mateus criou a conta de teste (26/07) |
| Porta 3000 em conflito (meu dev server vs preview_start) | app precisa da 3000 (OAuth/Supabase) | `launch.json` `autoPort:false` + matei meu server + preview_start | sim |
| `/consulta/demo` redireciona pro login | não é público (page faz auth própria) | capturar com sessão logada | pendente |
| login **escuro** sai em branco (form some) | telas de auth quebram no dark (ou são light-only) | achado do audit — o Fable/decisão define se é bug | registrado, não corrigido |
| R-12 furou: CTA sem `hover:bg-teal-*` escapou (ex. botão do /planos) | meu sweep só pegou linhas com esse hover | o audit varre todas sistematicamente; ou re-grep depois | registrado, não corrigido |
| Dex ficha rápida fora do ar | código intacto + model IDs atuais → ambiente (chave/quota) | pegar o erro real do terminal `npm run dev` | adiado (amanhã) |

## 5. O que eu estava cogitando

- **Fable via workflow** (um auditor por grupo de telas → síntese) vs. um agente só — decidi workflow pela escala,
  mas só disparo com ok e com as screenshots prontas.
- Usar **claude-in-chrome** (Chrome real logado do Mateus) pra pular o login — abandonei porque os screenshots
  não viram arquivo (o Fable-subagente precisa ler arquivo).
- 2 rótulos de texto teal **não-botão** (status "Em Atendimento", badge "MAIS POPULAR") ainda reprovam AA de texto —
  fora do escopo "botões"; candidatos a uma varredura de `#2f9c85` hardcoded (que vi espalhado).
- Se um dentista real reclamar do acesso à tabela, recency vira item ⏳ novo (Site A só) — não reabre R-21.

## 6. Estado do audit (pra retomar rápido)

- **Brief:** `plans/auditorias/2026-07-25-brief-audit-visual.md`.
- **Harness de captura:** `capture-audit.cjs` (raiz) — headed, espera login, captura tudo em `plans/auditorias/screens/`.
- **Já capturado:** 24 PNGs (só públicas — landing/login/cadastro/esqueci/planos × claro-escuro × desktop-mobile).
- **A parede da noite = o LOGIN.** Tentamos a captura logada 2x e falhou: o Mateus tem conta **só-Google**, e
  **Google OAuth não roda em browser automatizado** (Playwright — Google bloqueia bot; a janela morre no fluxo).
  Contas `test-diag-*` já foram limpas (handoff 23/07); banco é prod → não dá pra criar/limpar conta à toa.
  **Resolvido:** o Mateus **criou uma conta de teste** (26/07, e-mail/senha, na clínica dele → vê dados reais).
- **Próxima sessão (destravado):** server no ar → `node capture-audit.cjs plans/auditorias/screens` → na janela,
  logar **E-MAIL + SENHA da conta de teste** (NÃO o botão do Google) → captura roda sozinha → Claude confere →
  **dispara o workflow do Fable** sobre os PNGs → relatório priorizado em `plans/auditorias/`.

## 7. Arquivos alterados

- **R-21:** `src/lib/odontograma/agrupar-por-dente.ts` (+`.test.ts`), `src/components/fichas/dente-grupo-header.tsx`,
  `src/components/pacientes/FichasTab.tsx`, `plans/artefatos/R-21-registros-por-dente.html`.
- **R-12:** `src/app/globals.css` + 11 arquivos de botão (auth, dashboard, fichas, convite).
- **R-10 P1:** `src/components/pacientes/FichasTab.tsx` (`derivarV2DosEventos`).
- **R-19:** `src/components/layout/floating-dock.tsx`, `.../fichas/encaminhar-bar.tsx`, `.../fichas/voice-ux.tsx`, `globals.css`.
- **Audit:** `plans/auditorias/2026-07-25-brief-audit-visual.md`, `capture-audit.cjs`, `plans/auditorias/screens/*`.
- **Infra:** `.claude/launch.json` (`autoPort:false`). **Docs:** `plans/ESTADO.md`, `plans/ROADMAP.md`.
- **Nada commitado** — tudo working tree.
