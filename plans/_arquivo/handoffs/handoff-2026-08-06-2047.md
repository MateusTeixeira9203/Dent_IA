# Handoff — 2026-08-06 20:47

> **HANDOFF** · **R-64 → R-59** · sessão #23, contínua desde o handoff de 06/08 14:59 ·
> modo **execução** · **encerrada por ele** com R-64 fechado 🟡 e R-59 com Partes 1-4 codadas
> (1-2 verificadas ao vivo, 3-4 parciais), Parte 5 e o gate de 2 contas pendentes.

## Adendo — depois do fechamento (mesma sessão, 21h)

Ele pediu 2 coisas depois de eu já ter escrito o handoff e o ESTADO:

1. **Perguntou se os 3 problemas relatados no início da sessão foram sanados.** Respondi
   mapeando cada um: "ficha não salva" → 2 mecanismos corrigidos (Partes 3 e 4), nenhum
   confirmado como *a* causa exata da queixa dele, porque nunca reproduzi o gatilho — só
   fechei os 2 jeitos que produziriam esse sintoma. "Orçamento não puxa certo" → sim,
   confirmado ao vivo (Partes 1 e 2). "Ficha vira só quitado/pendente" → não é bug, nem
   comecei, segue como decisão de produto em aberto.
2. **Relatou um sintoma novo, de terceiro (não viu, não reproduziu):** dificuldade pra
   editar a ficha quando tem muitos procedimentos iguais — caso de 12 restaurações, um
   dentista não conseguiu editar um dente específico. Registrei em texto puro no
   `ESTADO.md` (## Esperando você), com uma hipótese carimbada como não-confirmada (pode
   ser a mesma área de `grupo_id` que a Parte 2 mexeu hoje — cards fundidos por engano
   virando difíceis de editar por dente) — mas sem `ficha_id`/print/passo a passo, não dá
   pra investigar de verdade ainda. Próxima sessão pergunta os detalhes antes de caçar.

Nenhum código mexido neste adendo — só documentação.

## O que trabalhamos

1. **Terminei o R-64** (herdado do handoff anterior): rodei `/auditar` — 4 reviewers em
   paralelo (`security-review`, `ux-reviewer`, `typescript-reviewer`, `ponytail-review`),
   2 deram veredito BLOCK. Corrigi os 6 achados seguros, ele decidiu os 2 que mexiam em
   decisão dele (D10), refiz o G9 com um caso que expõe o teto de 10 slots, e fechei o item
   como 🟡 — não ✅, porque G8 nunca foi testado e G2/G7 não deu pra reverificar sem sessão
   logada.
2. **Ele perguntou pelo Bloco 1 e pela auditoria do financeiro** — recapeei os dois e, no
   meio da resposta, achei uma spec (R-59) com código já no ar sem aprovação. Ele aprovou a
   spec ali mesmo.
3. **Executei o R-59, Partes 1-4**, uma sessão de "Dr. teste" já estava logada no browser
   (não fui eu quem autenticou). Achei e corrigi um achado que a spec descrevia errado
   (Parte 1's G1 não reproduzia o bug) e outro em que a premissa da spec era **falsa**
   (Parte 2 — bloqueei, levei a decisão pra ele, ele escolheu, corrigi).
4. **Testei G1 e G2 ao vivo** de verdade — clique real via JS (`computer` por coordenada
   continua não registrando, mesmo sintoma de 3 sessões atrás), banco conferido, orçamento
   gerado. A Parte 2 revelou um bug de dinheiro real (13 grupos antigos duplicando item de
   orçamento) — ele autorizou o backfill, rodei e verifiquei.
5. **A página do paciente parou de renderizar** no meio do teste da Parte 4 — sobreviveu a
   restart do servidor, `rm -rf .next` e aba nova. Recorri ao mesmo truque do G9 do R-64
   (rota debug temporária chamando o server action direto) pra provar a lógica sem depender
   do React.

## O que concluímos

**✅ R-64 — fechado 🟡** (não ✅). `/auditar` completo, spec e artefato arquivados
(`_arquivo/specs/R-64-marcar-retorno.md` §10 tem o veredito inteiro). G9 verificado 2×
(caso simples + caso de intervalo não-divisor, achado pelo `typescript-reviewer`). 6 achados
corrigidos: 3 falhas WCAG AA, `clinica_id` faltando numa query, `try/finally` faltando em 2
handlers, prop `clinicaId` morta em 5 arquivos. **Ficou 🟡**: G8 (conflito) nunca testado,
G2/G7 sem sessão logada nesta rodada — risco residual aceito por ele, registrado.
**Virou fila:** R-68 (grade sem sinal de expediente) e R-69 ("marcar mesmo assim" ausente).

**🟡 R-59 — Partes 1-4 codadas, item ativo.**
- **Parte 1** ✅ codada + **G1 confirmado ao vivo** com dado de teste (`marcos`) — o G1
  original da spec citava um paciente que não reproduzia o bug.
- **Parte 2** ✅ codada (opção B, escolha dele) + **G2 confirmado ponta a ponta** — UI, banco
  (`grupo_id` compartilhado) e orçamento (quantidade 1). **Achado ao vivo, não só calculado:**
  o mesmo orçamento mostrou 3× o mesmo item de um grupo antigo — R$1.050 fantasma.
  **Backfill rodado e verificado** (27 linhas, 13 grupos, dry-run antes/depois).
- **Parte 3** ✅ codada + **G3 🟡 parcial** — ficha própria confirmada ao vivo; a metade de
  2 contas continua devendo (só havia 1 sessão logada).
- **Parte 4** ✅ codada + **G4 🟡 parcial** — provei a lógica do servidor forçando a RPC a
  falhar de verdade (rota debug, sem depender do React); o banner em tela em si eu não vi,
  porque a página travou renderizando.
- **Parte 5** — zero código, só falta o clique (verificar o fix do R-47 ao vivo).

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| R-64 fica 🟡, não ✅ | Marcar ✅ dado o volume de verificação feita | G8 nunca teve teste nenhum, ao vivo ou por rastreamento — "código escrito ≠ verificado" vale por gate, não por "quanto trabalho rendeu" |
| `sendHoraList` mantém o comportamento novo (duração precisa caber inteira) | Replicar o overhang do código antigo pra bater 100% com I2 ao pé da letra | Ele confirmou: I2 é "não perder horário válido", não "preservar bug" — o antigo deixava marcar consulta que estourava o fechamento |
| H2/H3 da auditoria do R-64 viram fila (R-68/R-69), não fix imediato | Corrigir na hora, já que achei | H2 mexe direto na liberdade que ele pediu no D10; H3 nunca apareceu em nenhuma decisão D1-D10 — risco de reverter algo sem ele saber |
| R-59 aprovada com a Parte 1 já codada regularizada, não revertida | Desfazer o código e recomeçar pela spec | Furo de processo real, mas o código em si estava certo (rastreei contra dado real antes de aprovar) — desfazer seria puro teatro |
| Parte 2 (`faces` na chave) vira opção B (`grupo_id` compartilhado), não A (consolidar na gravação) | Opção A, mais alinhada ao que o prompt do Dex promete | Ele escolheu B — não mexe em dado antigo, usa um campo (`grupo_id`) que já existe pro propósito exato |
| Backfill dos 13 grupos rodado nesta sessão | Deixar como risco aceito até a próxima edição de cada ficha | Ele autorizou direto depois de ver o R$1.050 fantasma ao vivo — achado concreto, não hipotético |
| G4 verificado só no servidor (rota debug + RPC forçada a falhar), não na tela | Insistir em fazer o React renderizar | Já eram restart + cache limpo + aba nova sem sucesso — mais uma tentativa não tinha razão pra dar resultado diferente |

## Erros e como pensei em resolver

| Erro | Causa/hipótese | Como resolvi (ou ia resolver) | Resolvido? |
|---|---|---|---|
| Testei G9 (R-64) só com intervalo que divide exato — não pegava o bug real | Desenho de teste incompleto, só percebi quando o `typescript-reviewer` achou a divergência | Redesenhei o teste com intervalo não-divisor; primeira tentativa ainda bateu no teto de 10 slots do próprio código, encolhi a janela até o teto não interferir | Sim |
| `Edit` recusou um bloco de `FichasTab.tsx` (`closePanel`) que era idêntico por `Read` | Suspeita: diferença invisível de encoding/whitespace não visível no texto renderizado | Reduzi o `old_string` pras 2 últimas linhas do bloco — casou. Causa raiz não investigada | Contornado, não resolvido |
| Grep mostrou `\ ` no lugar de `//` no início de linhas de comentário em 2 arquivos diferentes | Parecia corrupção real no código-fonte | `Read` no mesmo trecho mostrou `//` normal — artefato de exibição do Grep, não do arquivo. `tsc` limpo confirmou (um `\` cru quebraria o parse) | Sim — era falso alarme |
| Página `pacientes/[id]` travou no skeleton (`animate-pulse`) o resto da sessão | Servidor sempre respondeu 200 completo; suspeita é o mesmo "pane não composita" de sessões anteriores, causa raiz nunca achada | Restart do servidor, depois `rm -rf .next`, depois aba nova — nenhum resolveu. Recorri ao mesmo truque do G9 (rota debug chamando o server action direto, sem depender de React) pra confirmar a lógica sem a tela | **Não** — 4ª sessão com sintoma da mesma família (pane/click), sem causa raiz |
| `computer` (clique por coordenada) não registrou o clique em "Gerar orçamento" | Mesmo sintoma documentado 3× antes | `.click()`/`dispatchEvent` via JS direto no elemento, mesmo workaround de sempre | Sim (contornado) |

## O que eu estava cogitando

- **Se valia insistir mais na página travada antes de desistir e ir pro servidor** — decidi
  que não: já eram 3 estratégias de recovery sem sucesso (restart, cache limpo, aba nova), e
  nenhuma delas tinha razão nova pra funcionar numa 4ª tentativa. Registrei como suspeita de
  investigação futura em vez de insistir mais.
- **4 achados menores da auditoria do R-64 (typescript-reviewer) nunca viraram item de
  roadmap** — a query de `agendamentos` sem `.eq('clinica_id')` em `disponibilidade.ts` eu já
  corrigi na hora (não é isso), mas os outros 4 (mistura client/server no mesmo módulo,
  grade da secretária mostrando disponibilidade enganosa, `Map` engolindo duplicata
  silenciosa de `horarios_disponiveis`, `semanaInicioISO` sem validar que é domingo) ficaram
  só documentados no relatório do reviewer, sem linha no ROADMAP nem no ESTADO. Não é perda
  de informação (a análise sobrevive na resposta do agente dentro da sessão), mas não tem
  como ele achar isso de novo sem reler esta conversa. **Deveria ter criado pelo menos 1
  linha agrupada no ROADMAP antes de fechar — não criei, sinalizando aqui em vez de inventar
  prioridade sozinho.**
- **Se a Parte 2 devia ter ficado bloqueada esperando decisão, ou se eu podia ter implementado
  a opção B direto** (já que B claramente não mexe em dado antigo, o argumento mais forte
  contra qualquer risco) — decidi parar e perguntar mesmo achando B óbvia, porque a spec
  aprovada dizia explicitamente "sem trade-off" com uma premissa que eu tinha acabado de
  provar falsa — mudar o contrato da spec sem ele saber é o tipo de coisa que a regra 6 existe
  pra evitar, mesmo quando a escolha parece óbvia.
- **Se `marcos` (paciente de teste sem sufixo "(apagar)") devia ganhar esse sufixo agora,
  já que virou fixture recorrente de 2 sessões (R-63 e esta)** — não mexi, porque não é meu
  dado pra renomear sem perguntar, mas registro que ele já parece ser tratado como fixture
  informal e talvez merecesse entrar no padrão.

## Arquivos alterados

**R-64 (fechamento da auditoria):** `src/components/pacientes/retorno-semana-grid.tsx` (3
CRITICAL WCAG + H1 da janela dinâmica) · `src/components/pacientes/marcar-retorno-modal.tsx`
(H4, dica no botão) · `src/lib/agenda/disponibilidade.ts` (`clinica_id` na query, comentário
órfão) · `src/services/whatsapp.service.ts` (try/catch no `sendHoraList`) ·
`src/app/dashboard/meu-dia/_components/registrar-painel.tsx` (try/finally, prop `clinicaId`
cortada) · `src/app/dashboard/pacientes/[id]/_components/paciente-detail-client.tsx`
(try/finally, prop `clinicaId` cortada) · `src/app/dashboard/meu-dia/_components/meu-dia-client.tsx`
(prop `clinicaId` cortada) · `src/app/dashboard/meu-dia/page.tsx` (prop `clinicaId` cortada) ·
`plans/specs/R-64-marcar-retorno.md` → **movida pra `plans/_arquivo/specs/`** (§10 novo, o
veredito inteiro) · `plans/artefatos/R-64-marcar-retorno.html` → **movido pra
`plans/_arquivo/artefatos/`**.

**R-59 (Partes 1-4):** `src/server/patients/salvar-ficha.ts` (Parte 3 — `.select('id')` +
guard; RPC renomeada e revertida durante o teste do G4) · `src/components/pacientes/FichasTab.tsx`
(Parte 4 — `eventosFalharamAoSalvar`, banner, painel não fecha mais em falha) ·
`src/components/odontograma/ToothDetailPanel.tsx` (Parte 2, opção B — `grupo_id`
compartilhado ao criar 2ª face) · `plans/specs/R-59-ficha-orcamento-integridade.md`
(aprovada, G1-G4 documentados com o que foi verificado e como).

**Banco (produção, cada write autorizado na hora):** `horarios_disponiveis` — 2 linhas de
teste inseridas e apagadas (G9 do R-64, 2 rodadas). `odontograma_eventos` — 2 eventos de
teste no dente 24 do `marcos` inseridos (clique real) e apagados depois de verificar o
G2. **Backfill real, permanente:** `UPDATE` em 27 linhas de `odontograma_eventos` (13 grupos
de faces antigas) atribuindo `grupo_id` compartilhado — autorizado por ele, dry-run
conferido antes e depois.

**Doc:** `plans/ROADMAP.md` (R-64 fechado, R-59 status atualizado 3×, R-46 rebaixado de 🔵
pra 🟡 — só pode 1 ativo, R-68/R-69 novos) · `plans/ESTADO.md` (reescrito várias vezes ao
longo da sessão, trimado de volta pro teto no fim).

**Herdado de sessões anteriores, não tocado nesta:** `evals/extracao-clinica/*` ·
`src/app/api/dex/formatar-evolucao/route.ts` (R-46d) · `a-fazer-bloco.tsx`/
`anexar-documentos-bloco.tsx`/`campo-magico-meu-dia.tsx`/`cockpit-grid.tsx`/
`historico-bloco.tsx`/`nesta-sessao-bloco.tsx` (R-63) · `bloco-moldavel.tsx` (deletado,
R-63) · `captura-livre-card.tsx` · `Odontograma.tsx`/`types/odontograma.ts` ·
`combobox.tsx` (deletado) · `casar-procedimento-local.*`/`ditado-devolve-mapa.*` (R-62) ·
specs R-46-C7/R-50/R-61/R-62/R-63 · artefatos R-46-C7/R-61/R-63 · auditoria do financeiro
(2026-08-06) — tudo já coberto pelo handoff de 06/08 14:59.

**Nenhum commit nesta sessão** — represa junto com o lote anterior (C7, R-50, R-61, R-62,
R-63 inteiro, agora R-64 e o começo do R-59). ~30 commits acumulados.
