> **HANDOFF** · **R-88 / R-67 / R-92** · sessão #43 · modo execução (com uma parada de
> planejamento no meio, pro R-92)
> ⚠️ Sessão rodou **em paralelo** com outro chat no mesmo repositório — vários commits no log
> não são desta sessão (R-105/R-105a/R-105b). Distingui abaixo.

# Landing no ar, R-67 corrigido, R-92 vira Stripe

## O que trabalhamos

- **Codei o R-88 inteiro contra o artefato aprovado v7** e subi pra produção. Eixo Continuidade
  (escolhido por pergunta direta), preço 299/259 (você corrigiu o número que eu tinha proposto),
  Google OAuth testado clicando de verdade, geometria do odontograma importada da produção (não
  redesenhada), Dex trocado pelos componentes canônicos depois que você notou que eu tinha
  desenhado uma carinha própria em vez de usar `DexAvatar`/`DexMark`.
- **Achei e corrigi o R-67** (embed ambíguo derrubando consulta do prontuário e da timeline) —
  provado com SQL direto no banco, antes/depois, não só no olho.
- **R-92 mudou de rumo duas vezes na mesma sessão**: você pediu pra descartar, eu levantei que
  isso deixaria a promessa "cobra no 15º dia" no ar sem nunca ter sido testada, você decidiu
  executar de novo. No meio disso, mudou de provedor — AbacatePay sai, **Stripe entra**, chave
  chega segunda-feira. Escrevi a emenda técnica (§8 da spec) com o mecanismo certo verificado na
  doc oficial.
- **A cobrança avulsa de paciente (2ª integração AbacatePay, Pix pra orçamento) foi cortada do
  sistema** — você decidiu que isso passa a ser o dentista cobrando por fora. Virou tarefa de
  decommission separada, que você já iniciou numa sessão à parte (`task_471aea18`).

## O que concluímos

**Pronto e no ar:**
- ✅ (tecnicamente, verificado por mim — **não por você ainda**) R-88: landing nova em
  `dentia.app.br`, HTML servido confere `R$299`/`R$259`, zero menção a "Modo Consulta"/"Poder da
  IA", 5 CTAs `/cadastro` + 1 `/login`. Botão do Google clicado e confirmado indo pro OAuth real
  do Supabase.
- ✅ R-67 corrigido nos 4 pontos (`prontuario/route.ts`, `get-patient-workspace-data.ts`,
  `get-visible-timeline-events.ts` ×2), provado por SQL antes/depois contra o banco real. **Não
  pushei eu — a outra sessão pushou junto com o trabalho dela** (`cbf6627`, confirmado em
  `origin/main`).
- ✅ Preço novo (299/259) aplicado em `lib/planos.ts`, fonte única — a landing e o e-mail de fim
  de trial leem de lá agora, nada mais hardcoded.
- ✅ Rota nova `POST /api/landing/ficha-exemplo` — a "ponte" da landing (quem não quer pôr cartão
  ainda) agora manda um aviso de verdade pro e-mail da equipe, com rate limit de 3/h por IP.
  Testei validação (400), rate limit (429) e mandei 1 e-mail real de teste (autorizado por você).

**Pela metade / não feito:**
- 🟡 **Nada disso foi visto por você pessoalmente ainda.** Landing, botões, R-67 — tudo testado
  por mim (browser, SQL, curl em produção), zero verificação sua. Pelo `CLAUDE.md`, isso é 🟡,
  não ✅, até você olhar.
- 🟡 R-92 Dia 1 (recebimento em 2 gestos, Dex no `voice-ux`) segue **codado desde 09/08 e nunca
  testado ao vivo** — não toquei nele nesta sessão, só constatei que continua parado.
- ❌ `activateTrial` ainda não cobra cartão nenhum — a landing promete "cobrança no 15º dia" e
  isso só vira verdade quando o R-92 Dia 3 (Stripe) estiver no ar. **Publicamos a promessa antes
  do mecanismo existir**, de novo — igual o alerta que a própria spec do R-92 dava sobre a
  landing. Registrei isso no ROADMAP pra não se perder.
- ❌ Envio da ficha da "ponte" é manual — o e-mail avisa a equipe, ninguém automatizou o PDF
  ainda (falta encenar na Teste01, exportar, anonimizar).

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Eixo **Continuidade** | Conversão · Gestão | Único que os mockups dos 3 passos do artefato já ilustravam sem redesenhar telas. Gestão prometia importação de pacientes que não existe |
| Preço **R$299 / R$259** | R$300 / R$250 (o que eu tinha aplicado primeiro) | Você corrigiu direto, sem explicar — apliquei sem questionar |
| Preço lido de `lib/planos.ts` (não hardcoded na landing) | Hardcode 300/250 na landing | Você escolheu explicitamente por pergunta — zero risco de a landing divergir do checkout |
| `.bloco > * { min-width: 0 }` — diverge do artefato | Manter overflow horizontal de 237px abaixo de 610px | Mesma correção que o próprio artefato já usa em `.passo > *`; não muda nada em ≥880px |
| Dex = componentes canônicos (`DexAvatar`/`DexMark`) | Ícone próprio desenhado por mim | Você apontou o erro; é o mesmo padrão do "Modo Consulta" — inventar UI que já existe em outro lugar |
| R-92: **retomado**, não descartado | Descartar de vez, como você pediu primeiro | Levantei que "cobrança no 15º dia" já estava publicada e sem teste — você mudou de ideia depois de ver o argumento |
| Checkout do plano: **Stripe** | Continuar na AbacatePay | Decisão sua, sem justificativa pedida ("considero ela melhor"). Chave chega segunda |
| Cobrança de paciente (Pix avulso): **sai do sistema** | Migrar pra Stripe também | Sua decisão — Stripe exige 60 dias de conta processando antes de liberar Pix, e a migração não tinha motivo claro |
| Mecanismo do trial: `trial_period_days` legado, não a `Trial Offer` (preview) | API preview `product_catalog/trial_offers` | A preview não funciona com Checkout hospedado, só com a API de Subscriptions direto, e exige header de versão preview — errado pro tamanho do projeto |

## Erros e como pensei em resolver

| Erro | Causa / hipótese | Como eu ia resolver | Resolvido? |
|---|---|---|---|
| **Desenhei uma carinha própria pro Dex na landing** | Copiei a improvisação do próprio artefato (que também tinha inventado) em vez de checar os componentes reais do produto primeiro | Troquei por `DexAvatar` (campo mágico) e `DexMark` (fecho) — os mesmos que `captura-livre-card.tsx` e `voice-ux.tsx` usam | ✅ Você pegou antes de eu perceber sozinho |
| **`git commit --amend` puxou 2 renames de handoff da outra sessão** pro meu commit de preço | Rodei `--amend` sem `--` de paths depois de descobrir um erro de mensagem, e o index tinha arquivos staged pela outra sessão (renames de handoff) que eu não tinha colocado lá | `git reset --soft` até antes dos 3 commits, recommitei cada um com paths explícitos (`git commit -- <arquivos>`), devolvi o index da outra sessão intacto com `git add -A` nos mesmos caminhos | ✅ Aconteceu **duas vezes** (mesmo erro, mesma correção) — primeira vez no commit do R-88/preço, segunda no commit do R-67. Nas duas vezes percebi antes de pushar |
| **Mandei 2 e-mails de teste da ponte, não 1** | Autorizado 1; ao tentar confirmar o rate limit logo depois, o contador em memória estava associado ao processo do dev server antigo (que tinha caído e voltado), então a segunda tentativa não bateu no mesmo limiter e passou | Terminei de provar o rate limit só com requisições de e-mail inválido (consomem cota sem enviar) | ✅ Sem dado perdido, mas contei o erro a você na hora, não escondi |
| **Fetchei a API preview do Stripe (`Trial Offer`) primeiro**, achando que era o caminho certo pro trial | A busca trouxe o artigo mais novo/vistoso primeiro; não percebi de cara que era uma API 2026-03-25.preview até ler as limitações | Fetchei a doc do fluxo legado (`payments/checkout/free-trials`) separadamente e confirmei que é o caminho estável e o único que funciona com Checkout hospedado | ✅ Corrigido antes de escrever a spec — a emenda cita o caminho certo, com a ressalva do que quase entrou errado |
| **`dpl_...BUILDING` na Vercel** — cheguei a monitorar um deploy que na verdade não era o que eu queria acompanhar (a landing já tinha ido ao ar por outro deploy antes) | Havia múltiplos deploys disparados quase juntos (o meu push + o da outra sessão) | Não cheguei a usar o Monitor de fato — troquei pra `curl` direto em `dentia.app.br` e conferi o HTML servido, que é a prova que importa | ✅ |

## O que eu estava cogitando

- **Testar o Dia 1 do R-92 ao vivo agora** (registrar um recebimento real, ver o Dex reagindo à
  gravação) — não fiz porque não tenho sessão autenticada nesta janela, e criar uma sozinho seria
  eu decidindo por você. Fica pro início da próxima sessão, se quiser.
- **Rodar o `auditoria-completa`** pra promover o que está 🟡 pra ✅ — cogitei sugerir, mas a
  skill é clara que ela roda "antes de release, ou quando os 🟡 acumulam", e neste momento o mais
  valioso é você mesmo abrir a landing e o export de prontuário, não uma varredura automatizada.
- **Recontar a fila inteira do ROADMAP** (os números `32 ⏳ · 39 🟡...`) — ajustei só pelos 2 itens
  que mexi (R-67, R-88), não fiz recontagem completa. Se a outra sessão também moveu itens, esse
  número pode estar levemente errado agora.
- **Perguntar se a chave da Stripe já existe em algum lugar (Vercel, outro cofre)** antes de
  assumir que só chega segunda — não perguntei porque você já tinha dado a data explicitamente.

## Arquivos alterados

| Arquivo | O quê |
|---|---|
| `src/app/page.tsx` | reescrito — landing R-88, server component |
| `src/components/landing/*.tsx`, `landing.css` | **novo** — 8 componentes + CSS da landing |
| `src/hooks/use-em-cena.ts` | **novo** — hook de revelação por scroll (substitui o IntersectionObserver do artefato) |
| `src/app/api/landing/ficha-exemplo/route.ts` | **novo** — rota da "ponte", rate-limited |
| `src/lib/planos.ts` | preço 249/179 → 299/259 |
| `src/lib/email/templates/onboarding.ts` | lê preço de `planos.ts` em vez de hardcode |
| `src/app/api/pacientes/[id]/prontuario/route.ts`, `src/server/patients/get-patient-workspace-data.ts`, `src/server/patients/get-visible-timeline-events.ts` | fix R-67 — FK nomeada nos embeds de `dentistas` |
| `plans/artefatos/R-88-landing-conversao.html` | cabeçalho corrigido pra "aprovado, implementado" |
| `plans/specs/R-92-fechar-para-cobrar.md` | +§8, emenda Stripe (não commitado ainda) |
| `plans/ROADMAP.md` | linhas R-67/R-88/R-92 atualizadas, contexto do topo corrigido |

**Commits desta sessão:** `343e8a4` (preço) · `66be0ee` (landing) · `cfa18f7` (artefato aprovado)
· `cbf6627` (R-67) — todos pushados. `plans/ROADMAP.md`, `plans/ESTADO.md` e
`plans/specs/R-92-fechar-para-cobrar.md` seguem **não commitados**, fechando agora.
