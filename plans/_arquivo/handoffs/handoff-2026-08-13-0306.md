# Handoff — 2026-08-13 03:06

> **HANDOFF** · **R-107a-d** (+ R-103b/c commitados) · sessão #39 · modo discussão →
> planejamento → execução (×4) → discussão de design → commit

<!-- Este arquivo é imutável: é o log do raciocínio da sessão.
     A situação ATUAL (o que falta, o que trava) mora no plans/ESTADO.md, não aqui. -->

## O que trabalhamos

1. **Debate longo sobre a spec "Painel de Procedimentos Rápidos"** que ele colou no início —
   confrontei com o que já existe (campo mágico, matcher local, catálogo de 2 camadas) antes de
   aceitar qualquer premissa. Boa parte da spec original acabou não virando código — o que
   sobreviveu veio de atrito real que ele descreveu ao vivo, não da spec colada.
2. **R-107a codado e commitado** — barra do campo mágico sem Status/Observação globais, chips
   de Profilaxia/Clareamento portados de `FichasTab.tsx`.
3. **R-107b codado e commitado**, com migration 139 aplicada em produção — busca livre no
   painel do dente, tipo genérico `outro`, "Dente ausente" (reaproveita `exodontia`+
   `origem:'preexistente'`), chip Incluso removido.
4. **R-107c codado e commitado** — `min-h-[308px]` no card da direita, resolve o colapso visual
   entre dente vazio (139px) e mapa (286px). Medido antes/depois, não aproximado no olho.
5. **R-107d codado e commitado** — faixa de lote (2+ dentes → 1 procedimento pra todos),
   Restauração pede face antes de aplicar. Decisão baseada em consulta real ao banco (SQL via
   Supabase MCP): quase metade das fichas com 4+ dentes tem face diferente por dente — "aplica
   direto" estaria errado na metade dos casos.
6. **Adendo ao vivo: "Modo multidente"** — achado testando o R-107d (clicar 2º dente sempre
   trocava o espelho pelo histórico, exigindo "voltar à boca" entre cada seleção). Toggle novo
   que pula esse comportamento enquanto ligado; desliga sozinho em toda ação de lote. Codado e
   testado — **posição na tela ainda em aberto**, ver "O que eu estava cogitando".
7. **R-103b/c commitados** — código de sessão anterior (#38), sem trabalho novo meu hoje além
   de revisar e incluir no lote de commits a pedido dele.
8. **5 commits organizados por camada** (migration → refactor → código R-107 → código R-103b/c
   → docs), depois de eu apontar que a regra dele de "1 commit por mudança revertível sozinha"
   colidiria com arquivos que carregam 2 itens entrelaçados (`registrar-painel.tsx`,
   `FichasTab.tsx`) — expliquei o trade-off (granular por hunk vs. por camada) e ele escolheu
   por camada.

## O que concluímos

- ✅ **R-107a** — codado, testado ao vivo (8/8 gates), **commitado** (`c32ee24`). Zero erro de
  console em toda a verificação.
- ✅ **R-107b** — codado, testado ao vivo (8/8 gates, incluindo G6 com escrita real no banco —
  item de teste apagado depois de conferido), **commitado**. Migration 139 **aplicada em
  produção**, conferida no schema via `pg_get_constraintdef`, não só na tabela de migrations.
- ✅ **R-107c** — codado, medido antes/depois (não aproximado), **commitado**.
- ✅ **R-107d** — codado, testado ao vivo (8/8 gates), **commitado**. Inclui o adendo do "Modo
  multidente", também testado e commitado, mas com decisão de posição pendente.
- ✅ **R-103b/c** — commitados nesta sessão (código já existia de #38). Gates G3/G4/G5/G6/G8
  continuam pendentes (mesma trava de #38: exige 2 contas reais).
- 📌 **5 commits locais, zero push.** Ele não pediu push — fica esperando sinal, como sempre.
- 🟡 **"Modo multidente" — posição não fechada.** Código está funcional (toggle acima do
  odontograma), mas ele quer opinião de dentistas reais amanhã antes de mover.

**Tudo commitado nesta sessão foi testado ao vivo por mim antes do commit** — não é "código
escrito", é "código verificado" (typecheck+lint+build limpos em cada etapa, interações reais no
browser, zero erro de console). A única verificação que falta é a dele mesmo — nenhum gate
pediu login que só ele tem, diferente do R-103b/c.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Sai Status (a fazer/feito) e Observação globais da barra | Manter, só reposicionar | Redundantes com o pill de status e o textarea por-evento que já existem em `ToothDetailPanel`/`NestaSessaoBloco` — ele mesmo achou "inútil hoje" |
| Incluso sai do chip, tipo continua no sistema | Remover o tipo do TS/CHECK também | Uso real na base: 1 evento, 1 paciente. Peso morto na UI, mas apagar o tipo migraria/perderia esse 1 registro à toa |
| Faceta vira tipo genérico `outro`, sem símbolo próprio | Símbolo novo no odontograma | Ele mesmo: "não é muito diferente de identificar, como a restauração" — só precisa pintar, não precisa desenho especial |
| Salvar procedimento avulso no catálogo é ação SECUNDÁRIA, nunca bloqueia o registro | Pedir preço antes de registrar | O registro já aconteceu no clique de "Lançar"; "salvar no catálogo" é oferta opcional depois, pulável |
| Dente ausente reaproveita `exodontia`+`origem:'preexistente'` | Tipo novo `ausente` | Mecanismo de render (contorno tracejado, badge) já existe pra esse `origem` — zero mudança em `Odontograma.tsx`. Custo: `cycleDenteTipo` precisou aprender a filtrar por origem pra não colidir com "Extração" normal |
| `min-h-[308px]`, não `height` fixo | `height` fixo no card da direita | Tabela de especialidade (endo/implante) precisa continuar livre pra crescer além do piso — `height` fixo cortaria dado clínico, pior que o soluço visual que motivou o item |
| Restauração em lote pede 1 face antes de aplicar | Aplicar direto, ou não oferecer lote pra Restauração | Consultei o banco: quase metade das fichas com 4+ dentes tem face diferente por dente — aplicar sem perguntar estaria errado na metade dos casos. Faces diferentes por dente fica fora do lote (dente a dente) |
| "Modo multidente" pula `setDenteAberto`, não muda o clique padrão | Mudar o comportamento padrão do clique (nunca mais abrir histórico) | Ele foi explícito: mudar o clique padrão é decisão separada que afeta o fluxo de 1-dente-só já aprovado — não misturar |
| Commits organizados "por camada" (migration → refactor → R-107 → R-103b/c → docs) | Granular por hunk (`git add -p`), 1 commit por sub-item (a/b/c/d) | `registrar-painel.tsx` e `FichasTab.tsx` carregam 2 itens entrelaçados cada — hunk-splitting arriscava commits intermediários que não compilam, o oposto do motivo da regra (reversibilidade). Ele escolheu depois de eu explicar o trade-off |
| R-98b (`ApresentarPaciente`/`ApresentarPanel`/`usePlanejamentoPaciente`, migration 136) e o fix de plural do `casaLabel` (sessão #38) ficaram FORA dos commits | Incluir tudo que estava no working tree | Não são meus de hoje, não foram pedidos, e o R-98b tem motivo de pausa "não explicado" registrado no ESTADO — não decido por ele |

## Erros e como pensei em resolver

| Erro | Causa (ou hipótese) | Como pensei em resolver | Resolvido? |
|---|---|---|---|
| Usei classes Tailwind dinâmicas (`` `border-${cor}` ``) nos chips de rotina do R-107a | Copiei o padrão errado — Tailwind precisa de string literal pra gerar a classe no build, interpolação não funciona | Troquei pro padrão que `FichasTab.tsx` já usa (style inline com `color-mix` sobre custom properties) antes mesmo de rodar o build | Sim, achado e corrigido antes do build |
| Múltiplos `.click()` disparados numa mesma chamada JS de teste não acumulavam a seleção de dentes | React 18 batching — closures de `onToothToggle` capturadas antes do primeiro clique reprocessar o estado; sucessivos `.click()` síncronos liam `onde` desatualizado | Passei a clicar 1 dente por chamada JS separada, deixando o React re-renderizar entre cada uma | Sim — confirmei que era artefato do teste, não bug do app |
| "Modo multidente" pareceu não funcionar em vários testes (`aria-pressed` sempre `false`) | Nós DOM órfãos de testes anteriores na mesma aba (rect zerado, elemento fantasma) ficaram sendo pegos por `querySelectorAll` | Testei em aba nova + `dispatchEvent(new MouseEvent(...))` num elemento recém-consultado — funcionou de primeira | Sim — era artefato de automação, não bug real |
| `get_page_text` voltava conteúdo vazio/truncado várias vezes no meio dos testes | Dev server sob carga (3-5s por render, sessão longa acumulando HMR) — a ferramenta capturava estado intermediário de hidratação | Troquei pra `read_page`/`javascript_tool` direto no DOM em vez de confiar no `get_page_text` | Sim |
| Rename do handoff `2026-08-11` foi parar no commit da migration em vez do commit de docs | Estava pré-staged (`git add`) de uma sessão anterior, antes de eu começar a trabalhar hoje — qualquer `git add` parcial que eu fizesse não desfazia esse staging | Não desfiz nem re-commitei — é um `git mv` sem efeito prático, avisei ele explicitamente em vez de deixar passar em silêncio | Aceito como está, avisado |

## O que eu estava cogitando

- **Onde o "Modo multidente" deveria morar** — passei por 3 posições ao vivo com ele: (1) colado
  no odontograma, em linha própria (onde ficou codado); (2) dentro da barra de Legenda do
  `Odontograma.tsx` (rejeitei sugerir com convicção — mexe em componente compartilhado com a
  ficha do paciente); (3) dentro da faixa do campo mágico, ao lado de Profilaxia/Clareamento/
  Manutenção (minha recomendação final, depois de reconhecer que meu argumento original de
  "tem que ficar perto do clique" era mais fraco que "é da mesma família de controles"). Ele
  decidiu **não aplicar nada agora** — quer opinião de dentistas reais amanhã. Documentei as 3
  opções e a recomendação no adendo §9 do R-107d, pra não perder o raciocínio até ele decidir.
- **Se a faixa de lote inteira deveria descer pra "Nesta ficha"** — ele levantou isso 2 vezes,
  de formas ligeiramente diferentes (1ª: só o "Modo multidente"; 2ª: a faixa inteira). Argumentei
  contra as duas vezes pelo mesmo motivo — separaria de novo o que a gente tinha acabado de
  juntar (toggle + contador + chips no mesmo bloco visual). Ele aceitou o argumento nas duas
  vezes sem insistir, mas como ele voltou a cogitar a mesma ideia 2x em formas diferentes, não
  descarto que ele peça de novo numa sessão futura — vale reler o §9 antes de re-litigar do zero.
- **Se valia aumentar o zoom do odontograma** (pedido dele, discussão separada) — mostrei que o
  teto sem mexer no grid é ~3% (invisível: 0.68→0.70), e que ganho de verdade (+20%, até 0.82)
  exige tirar largura da coluna "Nesta ficha" — revisando a proporção 1,50:1 que é decisão
  explícita do R-78 §4.2. Ele decidiu **deixar como está** por ora, sem aplicar nada. Fica
  registrado como possibilidade pra quando ele quiser revisitar aquela proporção.
- **Se o "Modo multidente" deveria também impedir a extração de faces em detalhe** — não chegou
  a virar pergunta explícita, mas ao desenhar o toggle percebi que ele só afeta
  `onToothToggle` (o clique no espelho) — nunca toquei em `ToothDetailPanel`/`cycleFace`. Não é
  gap, é escopo: o modo é especificamente sobre o clique no espelho, que é onde o atrito foi
  sentido.

## Arquivos alterados

| Arquivo / grupo | Mudança | Commit |
|---|---|---|
| `supabase/migrations/20260813000000_139_odontograma_tipo_outro.sql` | **Novo** — CHECK ganha `'outro'`, aditiva | `7b09f88` |
| `src/lib/odontograma/rotina-boca.ts` | **Novo** — `eventoRotina`/`cycleRotina` extraídos de `FichasTab.tsx` | `68b8cf9` |
| `src/app/dashboard/meu-dia/_components/registrar-painel.tsx` | R-107a (tira status/observação, chips de rotina) + R-107d (lote, modo multidente) — 508 linhas de diff | `c32ee24` |
| `src/app/dashboard/meu-dia/_components/meu-dia-client.tsx` | `min-h-[308px]` (R-107c) | `c32ee24` |
| `src/components/odontograma/ToothDetailPanel.tsx` | R-107b — busca livre, tipo `outro`, Dente ausente, tira Incluso | `c32ee24` |
| `src/components/pacientes/FichasTab.tsx` | R-107a (importa `rotina-boca.ts`) + R-107b (prop `catalogoProcedimentos`) | `c32ee24` |
| `src/app/dashboard/pacientes/[id]/_components/paciente-detail-client.tsx` | Passa `catalogoProcedimentos` pro `FichasTab` (R-107b) | `c32ee24` |
| `src/types/odontograma.ts` | Tipo `outro` + `TIPO_LABEL.outro` | `c32ee24` |
| `src/app/api/dex/mes/route.ts`, `src/app/api/dex/retencao/route.ts`, `src/lib/dex/numeros-mes.ts`+`.test.ts`, `src/lib/dex/retencao.ts`+`.test.ts` | R-103b/c — código de sessão #38, sem mudança minha hoje | `88d966f` |
| `src/app/api/dex/context/route.ts`, `src/components/layout/dex-hub/coluna-numeros.tsx`, `src/hooks/useDexHub.ts`, `src/lib/dex/pendencias.ts`, `src/lib/dex/tipos.ts` | R-103b/c — integração no hub | `88d966f` |
| `plans/specs/R-107a-barra-meu-dia.md`, `R-107b-perfil-do-dente.md`, `R-107c-altura-estavel-perfil-dente.md`, `R-107d-lote-multidente.md` | **Novas** — as 4 specs, com gates preenchidos após verificação ao vivo. R-107d ganhou §9 (adendo do modo multidente) | `a64b912` |
| `plans/ROADMAP.md`, `plans/ESTADO.md` | Atualizados — status de commit, 🔵 movido de R-103 pra R-107 | `a64b912` (+ esta sessão de fechamento) |

**Fora dos commits, de propósito (não são meus de hoje):** `src/components/pacientes/
ApresentarPaciente.tsx`, `ApresentarPanel.tsx`, `src/hooks/usePlanejamentoPaciente.ts`,
`supabase/migrations/..._136_apresentacao_modelos.sql` (R-98b, motivo do botão desativado
"não explicado" — ESTADO.md já registrava isso antes de hoje) e `src/lib/odontograma/
casar-procedimento-local.ts`+`.test.ts` (fix de plural, sessão #38).
