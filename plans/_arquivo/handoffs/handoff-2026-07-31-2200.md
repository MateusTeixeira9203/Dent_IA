# Handoff — 2026-07-31 22:00

> **HANDOFF** · **R-46 · R-47** · sessão #9, seguindo do handoff de 31/07 11:10 · modo
> **discussão → planejamento → execução**
> Sessão muito longa: push do que ficou pendente da #8 no início (9 commits), depois
> discussão sobre atrito da ficha virou spec do R-46 (Meu dia), Fase 0 achou os bugs que
> viraram R-47, R-47 corrigido, R-46a codado, e uma sequência de fixes pontuais no PDF de
> orçamento a partir de bugs reais que ele achou testando ao vivo.
> **9 commits, todos pushados** — `main` em `4331e61`.

## O que trabalhamos

1. Push dos 9 commits pendentes da #8 + correção de ROADMAP/ESTADO.
2. Discussão de produto: atrito da ficha ("orto campo por campo" → ficha inteira → premissa
   de "8 pacientes por dia") virou a spec do R-46 (Meu dia), com pesquisa real de mercado
   (Open Dental, Dolphin, Ortho2 etc.) via `thinking-partner`.
3. R-46 aprovado; R-15 (modo consulta) absorvido e arquivado. Fase 0 (diagnóstico do
   pipeline Dex da ficha rápida) achou 6 bugs, 3 de perda silenciosa de dado — virou R-47,
   corrigido em 2 rodadas (a 1ª correção tinha um gap real que a verificação adversarial achou).
4. R-46a codado: rota `/dashboard/meu-dia`, rail do dia + coluna de contexto, zero escrita
   (2 rodadas de verificação adversarial, achou e corrigiu 2 bugs reais no reduce de pendências).
5. Sequência de fixes pontuais a partir do que ele achou testando ao vivo o PDF de
   orçamento: download liberado pra secretária, import quebrado da lib `pdf-parse`, cor de
   fundo e quebra de página no PDF, orçamento novo nasce sem valor por item.

## O que concluímos

**Pronto e verificado ao vivo:**
- Push inicial (9 commits da #8) — confirmado via deploy no Vercel (READY).
- `mostrar_valor_por_item` default `false` — testei ao vivo (criei orçamento novo, PDF
  confirmou só total).
- `print-color-adjust` (gradiente no PDF) — testei ao vivo (JS na página real) e depois
  **ele confirmou** com PDF baixado de verdade.
- `page-break-inside:avoid` — **ele confirmou** com PDF real: quebra caiu entre dois cards,
  nenhum cortado ao meio.
- Download de PDF liberado pra secretária, import do pdf-parse — confirmados por leitura de
  código (causa raiz clara), não por clique ao vivo.

**🟡 sem verificação ao vivo real:**
- **R-47** (perda de dado no Organizar com Dex) — typecheck/lint/build limpos + 2 workflows
  de verificação adversarial contra o código, nunca clicado. O pane do browser não
  compositou a sessão inteira até bem tarde (só voltou a funcionar durante os testes de PDF
  de orçamento) — não voltei pra retestar isso especificamente depois.
- **R-46a** (Meu dia) — mesma situação: 2 rodadas de verificação adversarial, zero clique real.

**Pela metade / decisão pendente:**
- **R-28 Parte 3** (orçamentos dessincronizados — pagamento duplicado, "saldo pendente
  fantasma") — achei 2 ocorrências reais ao vivo (orçamento da Gessica R$250, orçamento do
  "marcos" R$5.360 fantasma) enquanto testava outra coisa. Diagnostiquei a causa provável
  (parcelamento cria parcelas como "Outro" em vez de herdar a forma de pagamento, e não
  fecha a parcela quando um pagamento real chega) mas **não toquei em nada** — dado
  financeiro de cliente real + decisão já registrada como "em aberto" desde o R-28.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Meu dia (R-46) absorve e substitui o modo consulta, em fases | Manter os dois lado a lado indefinidamente | Ele decidiu na discussão: "isso mata o modo consulta" — fases só pra não trocar 26% de uso real por promessa antes do substituto provar que funciona |
| Fase 0 antes de qualquer código do R-46 | Ir direto pro R-46a | O Organizar com Dex já era suspeito ("não está funcionando direito"); construir R-46d em cima de pipeline com bug seria herdar o defeito |
| R-47 conserta antes de qualquer coisa do R-46 | Deixar pra dentro do R-46d | Pedido explícito dele: perda de dado clínico não espera |
| Merge de `eventosDraft`: o que já existe nunca perde pra reextração | Manter o dedup por "menor id" (1ª correção do R-47) | Verificação adversarial achou ~50% de chance de trocar evento real por um mais pobre; trade-off aceito: duplicata visível é melhor que perda silenciosa |
| Orçamento novo nasce com `mostrar_valor_por_item=false` | Só desligar o toggle de um orçamento específico | Ele escolheu explicitamente via pergunta direta: "todo orçamento novo, daqui pra frente" |
| PDF: só o fix de CSS nesta sessão, não mexer no pagamento duplicado | Resolver os dois juntos (apareceram no mesmo PDF) | CSS é pontual; pagamento duplicado é dado financeiro real + decisão de produto já "em aberto" — categorias diferentes |
| "Meu dia": porta no hero com peso de botão + item na sidebar | Só link de texto pequeno | Pedido explícito: "algo ativo, fácil de ver" |

## Erros e como pensei em resolver

| Erro | Causa (ou hipótese) | Como pensei em resolver | Resolvido? |
|---|---|---|---|
| 1ª correção do R-47 (achado 1) tinha gap real | `dedupEventosDraft` desempata colisão por "menor id" — arbitrário entre id real do banco e id novo aleatório, ~50% de apagar o evento certo | Verificação adversarial (2ª rodada) achou; troquei pra "o que já existe nunca perde pra reextração" | Sim — verificado por leitura própria + typecheck/lint/build |
| G2 do R-46a (reduce de pendências) tinha 2 bugs | `.is('assinatura_id', null)` excluía o `realizado` assinado do reduce, deixando o `indicado` irmão vencer por omissão; `registrado_em` é DATE sem hora, empate no mesmo dia é comum | Removi o filtro (`status !== 'indicado'` final já resolve sozinho); `created_at` como 2ª chave de ordenação | Sim — verificado por 2ª rodada de leitura + typecheck/lint/build |
| `pdf-parse` quebrava upload de PDF no campo mágico | Bug conhecido da lib: `index.js` roda `isDebugMode = !module.parent` no top-level, avalia `true` sob bundling do Next.js, tenta ler arquivo de teste inexistente, derruba o import inteiro | Import direto de `pdf-parse/lib/pdf-parse.js` (parser real, sem o wrapper) + shim de tipo | Sim — confirmei o import resolve e exporta função; não reproduzi o bug original isolado (só se manifesta sob bundling real), mas a causa é documentada |
| Browser pane não compositava (screenshot/clique falhando) a sessão inteira | Desconhecida, do lado do usuário | Segui com verificação estática rigorosa em vez de fingir teste ao vivo; fui explícito toda vez que isso limitou a confiança | Parcialmente — o pane **voltou a funcionar** bem mais tarde (durante os testes de PDF), mas não voltei pra retestar R-47/R-46a com ele funcionando |
| PDF baixado divergia da tela (sem gradiente, sem barra) | Navegador não imprime fundo de tela sem `print-color-adjust:exact` | Adicionei a propriedade no `@media print` do CSS do orçamento | Sim — confirmado por ele com PDF real |

## O que eu estava cogitando

- **R-28 Parte 3 é o maior risco solto da sessão** — 2 ocorrências reais com dinheiro errado
  na tela (R$250 e R$5.360 "fantasma"). Diagnostiquei a causa provável só pelos sintomas —
  não li o código do fluxo de parcelamento em si. Se ele decidir atacar, começar por
  `registrarPagamentoRapido` / `marcarPagamentoPago` / a criação do plano (R-34) antes de
  mexer em qualquer dado.
- **Retestar R-47 e R-46a agora que o browser funciona** — o pane voltou a funcionar bem
  depois (durante os testes de PDF), então esses dois ficaram 🟡 mesmo tendo, na prática,
  uma chance boa de eu confirmar ao vivo se tivesse tentado de novo. Não tentei porque o
  foco já tinha virado pros bugs do PDF que ele reportava em tempo real. Primeira coisa a
  fazer numa próxima sessão com o browser funcionando.
- **A lista interna de orçamentos não respeita `mostrar_valor_por_item`** — confirmei que é
  intencional (R-38 sempre foi sobre o documento pro paciente, não a gestão interna), mas
  não perguntei se ele quer esconder ali também agora que mudei o default. Deixei como
  observação, não decidi sozinho.
- **Abertas da spec do R-46** — A1 (Dex em lista escala pra cirurgia longa? decide a fase 3,
  aposentar `/consulta`), pendência de colega executa direto ou pede confirmação, ordem de
  construção das próximas fatias. Nenhuma bloqueia o que já foi codado.

## Arquivos alterados (por commit, mais recente primeiro)

| Commit | Arquivos principais |
|---|---|
| `fix(orcamentos)`: valor por item | `orcamentos/actions.ts`, `pacientes/[id]/_components/paciente-detail-client.tsx` |
| `fix(orcamentos)`: PDF cor/quebra | `lib/prontuario-html.ts` |
| `docs(plans)`: roadmap/estado | `ROADMAP.md`, `ESTADO.md` |
| `fix(orcamentos)`: secretária | `orcamentos-client.tsx` |
| `feat(dashboard)`: Meu dia — R-46a | `server/dashboard/get-meu-dia.ts` (novo), `app/dashboard/meu-dia/` inteiro (novo — `page.tsx` + 3 componentes), `next-appointment-hero.tsx`, `sidebar-content.tsx` |
| `fix(anexos)`: pdf-parse | `lib/extract-text.ts`, `api/importar-procedimentos/route.ts`, `types/pdf-parse-lib.d.ts` (novo) |
| `fix(fichas)`: R-47 | `components/pacientes/FichasTab.tsx`, `server/patients/salvar-ficha.ts`, `components/fichas/captura-livre-card.tsx` |
| `docs(plans)`: Fase 0 | `auditorias/2026-07-31-fase0-dex-ficha-rapida.md` (novo) |
| `docs(plans)`: spec R-46 | `specs/R-46-meu-dia.md` (novo), 3 artefatos HTML (novos), `_arquivo/specs/R-15-...` (rename) |

**Tudo pushado.** `main` remoto em `4331e61`, 9 commits acima do `4931c4c` que fechava a #8.
