# Handoff — 2026-08-05 00:56

> **HANDOFF** · **R-46/C7 → R-50** (Meu dia) · sessão #19, contínua desde o handoff de
> 04/08 13:47 · modo **execução** o tempo todo, com uma parada de **discussão** no meio
> (a ficha rápida como diferencial) que virou spec (R-50) e um item congelado (R-60,
> aberto na sessão anterior).

## O que trabalhamos

1. **Fechei o C7** (spec já aprovada no fim da sessão #18): painel do dente saiu de
   "flutuando ao lado do odontograma" e virou 3º bloco de acordeão independente na coluna
   direita — matei `colapsarDireita` de novo. Testado ao vivo com sessão real do browser
   (persistiu entre navegações, não precisei logar de novo): WCAG idêntico (40,8×72,2px),
   sem card duplo, tabela de especialidade continuando no centro.
2. **Adendo ao vivo no C7, pedido dele depois da aprovação**: a seção inteira de "eventos
   do dente" (não só a tabela de endo/implante) desceu pro centro via o mesmo portal do
   R-20. Motion em `BlocoMoldavel` — abre/fecha com altura animada, e os blocos vizinhos
   deslizam quando um irmão monta/desmonta acima deles (`layout="position"` + `AnimatePresence`).
3. **Ele trouxe um print reclamando de 2 coisas** — tabela de endo cortada no histórico, e
   o incômodo de "toda vez cria ficha nova". Rodei um workflow de investigação (12 agentes:
   5 de levantamento, 4 de desenho por lente, 3 críticos adversariais) antes de sugerir
   qualquer coisa. Achou 2 defeitos mais graves que o que ele reportou: odontometria
   apagada ao concluir tratamento multi-sessão (`pendenciaParaDraft` não carrega `detalhe`)
   e ficha irmã sem assinatura (`buscarFichaParaAssinar` pega só 1 ficha por atendimento).
   **Confirmei no banco**: o bug da odontometria é real no código mas **0 casos hoje** — o
   fluxo multi-sessão nunca foi exercitado de verdade ainda. Fica registrado, não consertado.
4. **A pergunta que fiz pra ele destravou a decisão dele**: perguntei o que "Salvar" pela
   2ª vez no mesmo atendimento significa — "guarda o que tenho" ou "terminei". Ele não
   respondeu direto (a pergunta ficou em aberto — próxima sessão pode retomar), mas a
   conversa migrou pra uma direção mais concreta: ele quer a ficha rápida como o grande
   diferencial do produto, e apontou 2 pontos práticos (tirar o fallback é prematuro; orto
   precisa entrar pelo Dex).
5. **Corrigi minha própria leitura do R-50(a)** quando ele apontou: arcada continua
   obrigatória (é dado clínico essencial), o conserto é a IA **recusar** em vez de
   inventar — não tornar o campo opcional. Escrevi a spec R-50 com os 3 furos (schema sem
   `_inferior`, IA sem regra de recusa, Meu dia descartando o orto extraído em texto).
6. **Codei o R-50 e achei um bug real no meio do trabalho**: a 1ª versão da regra de
   recusa não segurava — a IA escolhia "ambas" como padrão em 2 de 3 tentativas manuais
   quando nada foi dito (não era ruído de temperatura, era viés sistemático). Reforcei o
   prompt com um par errado/certo explícito, reconfirmei 3/3.
7. **Testei o R-50 ponta a ponta com prova real**: criei um agendamento de teste no banco
   (autorização explícita dele) porque a data virou e o rail só mostra "hoje". Ditei a
   manutenção nas duas arcadas no campo mágico ao vivo, cliquei Salvar de verdade, e
   confirmei os 8 campos gravados em `fichas.orto_manutencao` direto no banco.
8. **Ele apontou contraste ruim no formulário de orto em light mode.** Medi ao vivo antes
   de mexer: o wrapper do chip e os inputs do `OrtoForm` usavam o MESMO token
   (`bg-surface-alt` duplicado) — confirmado por computed style, as duas cores eram
   `rgb(218,218,222)` idênticas. Troquei o wrapper pra `bg-surface`. Verificado nos dois
   temas depois.

## O que concluímos

**🟡 Codado e testado ao vivo — nada em produção, nada commitado:**
- **C7** — painel do dente como 3º bloco independente, `colapsarDireita` morto de vez.
  G3-G8, G12 confirmados ao vivo. **G9 (troca de paciente) e G10 (seleção múltipla)
  seguem sem teste** — não cheguei a voltar neles.
- **Adendo do C7** — seção de eventos no centro + motion no `BlocoMoldavel`. Testado
  funcionalmente (sem erro, DOM correto); **suavidade da animação não é verificável
  pelas minhas ferramentas** — ele confirma no olho.
- **R-50** — orto pelo Dex ponta a ponta. Eval 16/16 (subiu de 15/16 baseline, 0
  inventados). G1-G9 todos com prova ao vivo ou no banco, nenhum só por typecheck.
- **Contraste do `OrtoForm` em light mode** — corrigido e verificado nos 2 temas.

**Registrado, não consertado (de propósito, fora de escopo das specs fechadas):**
- Odontometria apagada em fluxo multi-sessão (`pendenciaParaDraft` sem `detalhe`) — 0
  casos reais ainda, mas o caminho de código está lá. Sem item de roadmap formal ainda.
- Ficha irmã sem assinatura quando há 2 fichas no mesmo atendimento — mesma raiz do
  ponto acima (múltiplas fichas por atendimento), acoplado à decisão pendente do R-54.
- `multi-dente` deu 500 na rota no baseline do eval; sumiu no eval seguinte (não
  investigado a fundo — pode ter sido transitório ou pode voltar).
- Notificação da secretária dispara a cada Salvar (não só na 1ª ficha do atendimento) —
  achado do workflow de investigação, não verificado no banco ainda.

**Pergunta em aberto, registrada, não decidida:** "Salvar" pela 2ª vez no mesmo
atendimento — guarda ou encerra? Depende disso: manter o modelo (conserta o botão/rótulo)
ou reabrir o R-54 (schema + `.select()` no update + trava de autor). **Não decidir isso
sozinho na próxima sessão** — é dele.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Arcada do orto continua obrigatória; IA recusa em vez de inventar | Tornar `arcada` nullable (minha leitura original do R-50a) | Ele corrigiu ao vivo: arcada é dado clínico essencial, campo opcional só empurra o problema |
| Reforçar o prompt com par errado/certo explícito | Confiar numa 2ª frase de instrução genérica | A 1ª tentativa de regra de recusa não segurou na prática (2/3 falhas) — testei antes de considerar fechado, achei o furo, só então reforcei |
| Portar a seção INTEIRA de eventos pro centro (não só a tabela) | Deixar só a tabela de especialidade no portal, como o contrato original do C7 previa | Pedido dele ao vivo, depois da aprovação — registrado como adendo (§9) na spec em vez de reescrever o contrato original |
| `layout="position"` no `BlocoMoldavel` + animação de altura separada no corpo | Um único `layout` cobrindo tamanho e posição | Composição de `layout` (tamanho) com `AnimatePresence`/altura explícita é o padrão com risco de conflito de transform documentado da Motion — separei por segurança |
| Criar agendamento de teste direto no banco (SQL, autorização explícita dele) | Pedir pra ele testar manualmente, ou simular sem dado real | G4-G7 do R-50 exigem UI real (rail só mostra "hoje"); ele autorizou explicitamente antes de eu escrever qualquer INSERT |
| Trocar `bg-surface-alt` do wrapper pro `bg-surface` | Mudar o `inputCls` do `OrtoForm` (componente compartilhado) | `OrtoForm` já funciona bem no outro lugar que o usa (`FichasTab.tsx`, sem wrapper -alt); o bug era específico do wrapper do Meu dia, não do componente |

## Erros e como pensei em resolver

| Erro | Causa/hipótese | Como resolvi (ou ia resolver) | Resolvido? |
|---|---|---|---|
| IA devolvia `arcada:"ambas"` como default quando nada foi dito, mesmo com a regra de recusa escrita no prompt | Hipótese: o modelo trata "manutenção genérica" como padrão-seguro-pra-ambas em vez de padrão-nulo; regra sem contraste concreto não pegou | Reforcei com par errado/certo explícito ("troquei as ligaduras" → null; "dos dois lados, de cima e de baixo" → ambas) e testei 3x seguidas | Sim, 3/3 depois |
| Minha própria verificação de contraste (`closest('.rounded-lg.border')`) comparava o input com ele mesmo, não com o wrapper — me fez achar por um momento que o fix não tinha pegado | O input do `OrtoForm` também tem as classes `rounded-lg` e `border` na própria className — `.closest()` inclui o próprio elemento antes de subir | Refiz a checagem subindo a cadeia de `parentElement` manualmente até achar o wrapper de verdade | Sim |
| Erro de sintaxe transitório no `ToothDetailPanel.tsx` durante a restruturação da seção de eventos, aparecendo no console/logs do servidor mesmo depois de eu já ter corrigido | Duas edições em sequência deixaram o arquivo com chaves desencontradas por um instante entre elas — o dev server compilou esse estado intermediário e logou o erro | Confirmei via `Read` do arquivo atual (sintaxe correta) + `tsc` limpo + os ÚLTIMOS logs do servidor (200 OK) que era histórico, não estado atual | Sim |
| `confirm()` nativo bloqueando um 2º "Organizar com Dex" na mesma sessão de teste — parecia que o clique não fazia nada | O ambiente de automação recusa dialogs nativos automaticamente (`confirm()` retorna `false`); a re-análise de texto já processado dispara esse dialog de propósito, pra não sobrescrever sem avisar | Recarreguei a página (estado limpo) e rodei o fluxo uma única vez por sessão de teste, sem re-analisar | Sim, contornado (não é bug do R-50) |
| `AUTH`/`OUT` do eval apontavam pra diretórios de scratchpad de sessões antigas que não existem mais | Os caminhos eram hardcoded de uma sessão anterior; scratchpad é por-sessão e não sobrevive | Escrevi um script mínimo e descartável de login headed (mesmo padrão do `capture-audit-3.cjs`, ele loga, eu nunca digito senha), salvei a sessão no scratchpad atual, atualizei os 2 caminhos no `run.cjs` | Sim |

## O que eu estava cogitando

- **Se valia testar G9/G10 do C7 antes de seguir pro R-50** — decidi que não, porque o
  print dele sobre a tabela cortada chegou no meio e pareceu mais urgente (ele estava
  olhando aquilo na hora). Ficou pendente, registrado, não esquecido.
- **Se o achado da odontometria apagada merecia virar item de roadmap agora** — decidi
  não abrir essa frente: são 0 casos reais hoje (fluxo multi-sessão nunca rodou), e a
  sessão já estava em cima do R-50. Registrei no handoff e no `ESTADO.md` em vez de
  inventar um R-NN sem conversar com ele primeiro.
- **Se a IA reforçada (3/3) era prova suficiente ou se eu deveria rodar mais rounds** —
  decidi que 3/3 em 2 rounds diferentes (isolado + dentro do eval completo) era evidência
  razoável pra um item que ainda vai passar por uso real; não fiz um stress test de 10x.
- **Se eu deveria ter perguntado antes de portar a seção de eventos inteira pro centro**
  (não só a tabela) — decidi que não, porque foi um pedido direto dele ("joga isso pra
  baixo do odontograma"), só documentei como adendo depois em vez de reabrir a spec
  inteira pra reaprovação formal.
- **Se apago o agendamento de teste que criei** — decidi que não, é decisão dele (mexer
  em dado de agenda, mesmo de teste, não é limpeza óbvia). Registrado em "Esperando você".

## Arquivos alterados

**Nenhum commit nesta sessão** — tudo acumulado em cima dos commits represados de sessões
anteriores (decisão dele: só sobe quando fechar a fatia do Meu dia inteira).

**Novos:** `plans/specs/R-46-C7-painel-dente-direita.md` ·
`plans/artefatos/R-46-C7-painel-dente-direita.html` · `plans/specs/R-50-orto-pelo-dex.md`

**Não é meu — pré-existente, não investigado:** `plans/specs/R-59-ficha-orcamento-integridade.md`
e `src/app/dashboard/pacientes/[id]/_components/paciente-detail-client.tsx` aparecem
modificados no `git status`, mas eu não toquei neles nesta sessão nem na anterior — é
trabalho dele ou de sessão que não está neste log. Não mexi, só registrando pra não
confundir a próxima sessão.

**Modificados (C7 + adendo):** `cockpit-grid.tsx` · `meu-dia-client.tsx` ·
`registrar-painel.tsx` · `ToothDetailPanel.tsx` · `bloco-moldavel.tsx`

**Modificados (R-50):** `api/dex/formatar-evolucao/route.ts` · `campo-magico-meu-dia.tsx` ·
`registrar-painel.tsx` (mesmo arquivo do C7, mudanças diferentes) · `types/odontograma.ts`
(comentário) · `evals/extracao-clinica/golden.json` (+2 casos) ·
`evals/extracao-clinica/run.cjs` (`ORTO_CAMPOS` + `camposDistintos` + caminhos de sessão)

**Modificado (fix de contraste, pontual, sem spec):** `registrar-painel.tsx` (wrapper do
chip de orto, `bg-surface-alt` → `bg-surface`)

**`plans/`:** `ROADMAP.md` · `ESTADO.md` (reescrito por este handoff)

**Banco (com autorização explícita dele, nada oculto):**
- 1 `INSERT` em `agendamentos` — agendamento de teste hoje, paciente "Teste R-31a",
  observação `"R-50 teste ao vivo (apagar) — gate G4-G7"`. **Não apagado.**
- 1 `Salvar` real via UI — criou 1 ficha nova com `orto_manutencao` estruturado (prova
  do G6). Dado de teste, paciente já marcado "(apagar)".
- Zero migration, zero mudança de RLS.

**Nenhuma escrita fora do paciente de teste. Nenhum dado de paciente real tocado.**
