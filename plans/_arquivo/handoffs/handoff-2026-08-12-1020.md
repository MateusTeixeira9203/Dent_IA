# Handoff — 2026-08-12 10:20

> **HANDOFF** · **R-101 → R-102 → R-103 (planejamento)** · sessão #38 · modo execução → planejamento

<!-- Este arquivo é imutável: é o log do raciocínio da sessão.
     A situação ATUAL (o que falta, o que trava) mora no plans/ESTADO.md, não aqui. -->

## O que trabalhamos

1. **R-101 fechado e R-102 codado do zero, os dois no ar** (`7f9f512`..`9a8ac3a`, migrations 137
   e 138). O R-102 saiu inteiro nesta sessão: schema, RLS, 3 actions novas, conflito nos 2
   sentidos, extensão de `getDisponibilidadeSemana`, render nas 3 grades da agenda.
2. **Um erro visual meu, apontado por ele ao vivo:** o dialog do compromisso pessoal copiou o
   padrão **errado**. Reescrito e aprovado — e isso virou regra permanente no `CLAUDE.md`.
3. **Planejamento do R-103 (Painel do Dex)** com artefato aprovado em 2 rodadas + `planner`
   acionado com autorização dele. Diagnóstico achou **4 causas** de o Dex estar "travado", todas
   confirmadas no código.
4. **Medição em produção que mudou o desenho da feature:** a métrica de paciente inativo está
   morta (devolve 0 em todas as clínicas) e a definição ingênua de "não retorna há 60 dias"
   produziria 230 nomes, 226 de gente que nunca teve ficha.

## O que concluímos

- ✅ **R-101** — no ar, **testado e aprovado por ele**. Spec arquivada, ROADMAP fechado.
- 🟡 **R-102** — no ar (`cbd39ee`), layout revisado e aprovado por ele, 1 bug de dado corrigido.
  **G1-G6 nunca tiveram teste formal.** Ele avaliou o risco do G6 (2 contas) como baixo — a RLS é
  cópia 1:1 de `agendamentos_access`, já comprovada — e decidiu subir assim. Trata-se como 🟡.
- 📄 **R-103** — 2 specs escritas em rascunho, aguardando aprovação:
  [master](../specs/R-103-painel-do-dex.md) (126 linhas) +
  [R-103a](../specs/R-103a-destravar-o-dex.md) (294 linhas). Artefato aprovado. **Zero código.**
  R-26 marcado como absorvido no ROADMAP.
- 📌 **Regra nova no `CLAUDE.md`** (§Artefatos, "Artefato aprovado é a base — inegociável"), a
  pedido explícito dele pra valer em todo desenvolvimento futuro.
- 🟡 **R-98b** — segue codado, migration no ar, botão desativado, **não commitado**. Não tocado
  nesta sessão; o motivo da desativação continua sem resposta.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| R-102: dialog no padrão **Dialog centralizado** do "Novo Agendamento", com tokens semânticos | Sheet lateral com gradiente do "Encaixe" (o que eu fiz na 1ª versão) | Ele apontou ao vivo: "está com o layout antigo". O Encaixe é o padrão velho; o Novo Agendamento é o atual. Meu erro também violou "proibido cor hardcoded" (`amber-500`/`red-500`) |
| R-102: atalhos de período (Manhã 08h/Tarde 13h/Dia inteiro), preenchendo hora+duração | Trocar hora fixa por seletor de período | Pedido dele, e ele mesmo se corrigiu no meio ("ah não, pode deixar a hora, na real") — então período é **atalho**, não substituto |
| R-102: subir sem G3/G4/G6 testados | Represar até o teste de 2 contas | Decisão dele com o risco na mesa: a RLS é cópia literal de uma policy já comprovada em produção |
| R-103: modal **central e largo, 3 colunas** | Slide-over de 420px (minha 1ª versão) | Pedido dele. Achado depois: o widget atual **já é** modal central 75vw×90vh — o formato nunca foi o problema, o conteúdo era |
| R-103: painel vira **dono único** das notificações e **o sino sai** | Sino conviver com o hub | Decisão dele: "meio redundante, já não está funcionando muito legal" |
| R-103: badge = pendências **+** notificações não lidas | Só pendências | Decorre de matar o sino: sem contar as não lidas, um check-in chegaria sem aviso visual nenhum |
| R-103: **limpar o mock ANTES de destravar** o Dex | Destravar primeiro (ordem original da spec) | Ele revelou que o mock nunca chegou a produção porque o Dex estava fechado — logo destravar primeiro **publicaria** a ficção |
| R-103: "nunca veio" fica **fora** do card de "parou de vir" | Implementar "não retorna há 60 dias" ao pé da letra | Medição: 230 nomes na Clindent, **226 nunca tiveram ficha**. Ao pé da letra o card nasce ruído |
| R-103: quebrar em R-103a/b/c + R-104 | Uma spec só | Deu 363 linhas num arquivo — teto de 300 estourado é erro de recorte. E o `a` não depende de resposta nenhuma dele |
| R-103: `planner` proibido de escrever arquivo | Deixar ele salvar a spec | Precedente da sessão #37: um `planner` escreveu num spec uma verificação que nunca aconteceu |

## Erros e como pensei em resolver

| Erro | Causa (ou hipótese) | Como pensei em resolver | Resolvido? |
|---|---|---|---|
| Dialog do compromisso pessoal com o layout antigo | Copiei o Sheet do "Encaixe" (vizinho no mesmo arquivo) em vez do Dialog do "Novo Agendamento". Peguei o padrão mais **próximo no código**, não o mais **recente no produto** | Ele apontou; fui ler o Dialog do Novo Agendamento e reescrevi do zero no padrão certo, trocando os hex por tokens (`warning-*`/`coral-*`) | Sim — typecheck/lint/build limpos, e ele aprovou ("agora sim, layout novo") |
| Compromisso pessoal da secretária nascia com o `dentista_id` **dela** | Passei `dentistaAtualId` cru como default do Select, em vez do helper `dentistaPadraoForm()` que Encaixe e Novo Agendamento já usam | Achei investigando o adendo dele ("a secretária também tem que conseguir marcar pro dentista"); troquei pelo helper | Sim (`cbd39ee`) — **não testado ao vivo** |
| Errei ao dizer que eu tinha inflado o ROADMAP em ~35 linhas | Comparei 240 linhas com o "205" do hook de abertura, que conta diferente | Medi `git show HEAD:` vs. arquivo atual: HEAD já tinha 239, eu somei 1 | Sim — e me corrigi no chat |
| As specs do R-103 atribuíam a **ele** uma posição sobre o badge que era **minha** | O `planner` herdou de mim ("minha posição: só pendências") e escreveu "Ele disse"; eu não peguei na 1ª revisão | Grep por "Ele disse" nos dois arquivos, corrigi a atribuição e registrei a correção no próprio texto | Sim — grep confirma zero atribuição falsa |
| Digitei "не" (cirílico) em vez de "não" no artefato | Provável troca de layout durante escrita longa | Peguei relendo antes de servir o arquivo; corrigi | Sim |
| Migration 138 bloqueada 2× pelo classificador de permissão | Mudança de schema em produção via MCP — trava do harness, não erro meu | Parei e pedi pra ele aplicar pelo SQL Editor, em vez de procurar caminho alternativo | Sim — ele aplicou; confirmei coluna, índice, trigger e policy por query |
| Usei `ScheduleWakeup` sem estar em `/loop` | Confundi a ferramenta com um "aguardar" genérico | Cancelei na mesma hora e avisei | Sim |
| `python -m http.server` pra servir o artefato | Python não existe nesse Windows | Escrevi um servidor estático de 20 linhas em Node no scratchpad | Sim — roda na 8099 |

## O que eu estava cogitando

- **Se o `DexPresencePanel` órfão foi abandonado pelo conflito de `dex-toggle`.** É a hipótese
  mais econômica (dois listeners no mesmo evento), mas **não confirmei** — o handoff anterior
  registra "sem motivo dado". A spec decide apagá-lo por outro motivo (geometria errada), então a
  decisão não depende disso.
- **Se `sidebar.tsx`/`sidebar-content.tsx` estão mortos de verdade.** `grep` não achou importador,
  mas a regra do projeto é verificar com **compilador**, não grep — e eu não verifiquei. Por isso
  a spec não toca nesses arquivos. Vale um `tsc` depois de removê-los, quando for a hora.
- **Se o realtime de `notificacoes` funciona hoje.** Li a subscrição, nunca vi um INSERT acender a
  tela. Se estiver quebrado, matar o sino leva junto uma função que já não existia — e ninguém
  perceberia. É o G9.
- **Volume da zona "Aconteceu".** Não medi quantas notificações não lidas cada clínica tem. Se
  forem centenas, a coluna 1 precisa de limite/agrupamento e o badge fica absurdo.
- **A causa de `pacientesInativos60d` devolver 0** — a hipótese da migration 125 (`UPDATE
  public.pacientes` carimbando `updated_at` da tabela inteira em 31/07) é plausível e **não
  provada**. Não muda a decisão: a métrica é errada por construção.
- **Os 226 "cadastro que nunca virou atendimento" da Clindent.** Deixei fora do R-103b, mas é
  informação de produto que ninguém tinha olhado: **319 cadastros e só 93 com ficha**. Se algum
  dia virar item, é campanha de reativação (parente do R-45), não painel.
- **Se o mock enganou alguém.** Perguntei porque muda a urgência da limpeza; ele respondeu que
  não, o Dex estava desativado. Foi essa resposta que revelou a inversão de ordem das fases —
  a pergunta valeu mais pelo efeito colateral do que pela resposta.

## Arquivos alterados

| Arquivo / grupo | Mudança |
|---|---|
| `supabase/migrations/..._138_agenda_bloqueios.sql` | R-102 — tabela, índice, trigger, RLS. **Aplicada em produção por ele** (SQL Editor) |
| `src/app/dashboard/agendamentos/actions.ts` | R-102 — 3 actions novas + checagem de bloqueio em `criarAgendamento`/`atualizarAgendamento`/`criarEncaixe` |
| `src/app/dashboard/agendamentos/_components/compromisso-pessoal-dialog.tsx` | R-102 — **novo**, reescrito 1× no padrão visual certo |
| `src/app/dashboard/agendamentos/_components/{day,week,month}-view.tsx` | R-102 — bloqueio mesclado nas caixas antes de `calcularFaixas` |
| `src/app/dashboard/agendamentos/_components/agendamentos-client.tsx` | R-102 — botão, estado, fio do dialog, fix do dentista padrão da secretária |
| `src/app/dashboard/agendamentos/page.tsx` | R-102 — `BloqueioRow` + busca da janela visível |
| `src/lib/agenda/disponibilidade.ts` | R-102 — bloqueio entra em `ocupados`; bot e retorno respeitam de graça |
| `plans/artefatos/R-103-painel-do-dex.html` | **Novo** — 2 versões (slide-over → modal central 3 colunas). Aprovado por ele |
| `plans/specs/R-103-painel-do-dex.md` | **Novo** — master: 4 causas, medição, recorte, 6 abertas |
| `plans/specs/R-103a-destravar-o-dex.md` | **Novo** — contrato da 1ª fatia, 7 fases, 12 gates |
| `CLAUDE.md` | **Regra nova** §Artefatos: artefato aprovado é contrato, implementação copia |
| `plans/ROADMAP.md` · `plans/ESTADO.md` | R-101 fechado, R-102 no ar, R-103 registrado, R-26 absorvido |
| `plans/_arquivo/specs/R-101-odontograma-proxima-secao.md` | Arquivado no fechamento do R-101 |
