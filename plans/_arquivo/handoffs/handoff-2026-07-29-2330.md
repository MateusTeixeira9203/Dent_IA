> **HANDOFF** · **auditoria técnica → R-30..R-35** · sessão #4 de 29/07 · modo **auditoria + planejamento**
> Zero linha de código de produção alterada. 6 specs escritas. Nenhum commit.

## O que trabalhamos

- **Auditoria técnica completa** sobre a lista de 11 problemas que ele trouxe (P1–P11), lendo
  código **e** o banco de produção em modo somente-leitura (~30 queries). Ele autorizou
  explicitamente só `SELECT`, depois de barrar a primeira chamada MCP.
- **Diagnóstico do bug da ficha** — o que ele mais queria. Reproduzido no dado real: ficha
  `45ecdebe` (Ildaumi Oliveira da Silva, Jenaina, 23/07, editada 29/07 18:18:54).
- **Seis specs escritas** — R-30 a R-35, mais duas rodadas de subagentes (4 mapas de terreno +
  crítico de completude; 4 entregáveis + verificador adversarial).
- **Varredura de código morto provada pelo compilador** — 31 arquivos, 2.801 linhas, com
  typecheck e build exit 0 antes e depois. Branch desfeita; a lista ficou na R-35 §13.
- **Artefato de planejamento** publicado pra ele ler:
  `https://claude.ai/code/artifact/19dc0f82-79e1-4d81-8f37-458f7122b7d2`

## O que concluímos

**Pronto:**
- As 11 queixas dele colapsam em **4 causas** + um eixo que ninguém tinha olhado (credencial e
  arquivo: storage, OAuth, metadata). Documentado.
- **P4 provado numericamente:** na Clindent existem 50 orçamentos. Paula vê **0**, Armando 8,
  **Gabriel, que é admin, vê 9**. Só a secretária vê os 50. Nada foi apagado — é RLS.
- **P1 provado:** a RLS de `pacientes` **já** libera a clínica inteira; é o app que estreita
  (`pacientes-list.tsx:48`). Paula veria 0 de 226.
- **P9 causa raiz + escopo:** `salvar-ficha.ts:56` valida `min(11).max(85)`; "boca toda" é o
  sentinela **99**. **25 fichas em produção já têm sentinela gravado e não abrem para editar**
  (23 não assinadas). Correção de 1 linha.
- **P7 recontado:** 14 grupos por nome exato, **17 ignorando acento**, 35 linhas, 18 cópias.
  **Zero conflito** de telefone/nascimento/CPF. **Zero linha assinada** — o único bloqueio
  técnico real de um merge não existe.
- **P5 resolvido** (ver Erros — mudei o diagnóstico 3 vezes até fechar).
- **Dente azul resolvido:** não é padrão do sistema, é defeito. `handleEdit` enche
  `selectedTeeth` de `dentes_afetados` → teal sólido `#2f9c85`, a mesma família de cor de
  "realizado". Virou a Parte 7 da R-30.
- **Achado fora da lista dele, P0:** bucket `avatars` é `public: true` com **foto de paciente
  real** dentro (nome dela no arquivo), legível sem login. E as policies `avatars_own_*` só
  checam `auth.uid() IS NOT NULL` — **qualquer usuário logado de qualquer clínica apaga ou
  troca qualquer foto**.

**Pela metade / não feito:**
- 🟡 **Nada subiu.** Não há código para marcar 🟡 — a sessão foi diagnóstico e planejamento.
- **R-31 estourou o teto** (323 linhas). Precisa quebrar em prevenção + merge.
- **Dois dos 11 seguem sem spec:** P6 (uma interface de ficha) e P10 (faceta nos chips).
- **Numeração de migration sem dono único** — R-29→114, R-32→115, R-34→116; R-31 e R-35 têm DDL
  **sem número**. Risco de colisão. E o repo **já** tem dois arquivos `_112_`.
- **ROADMAP.md** ainda não tinha as linhas de R-30..R-35 (corrigido no fechamento).

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Orçamento visível para **autor + admin + secretária** | clínica inteira · manter só autor | dele, 29/07. Admin ver 9 de 50 está errado até pelo critério dele |
| **Helper novo** `can_see_orcamento`, não mexer em `is_own_clinical_record` | adicionar `admin` na função existente | a função é usada por agenda, pagamento, horário, catálogo e planejamento — mexer vazaria **agenda e dinheiro** como efeito colateral silencioso |
| `pacientes.dentista_id` perde função de escopo | manter filtro por dentista | dele: paciente é da clínica, todo dentista vê |
| **Admin só vê** orçamento de outro, não edita | admin edita | dele. Combina com a 3.1: "trabalho é do autor" |
| **Secretária edita, exclui e muda status de orçamento**; de ficha e procedimento **não** | gate uniforme | dele, explícito |
| **Cascade ficha→orçamento fica como está** | trocar para `SET NULL` | dele: "todo orçamento é baseado numa ficha". Levantei o risco (29 de 50 afetados), ele reafirmou. Trabalho virou *tornar a consequência visível* na confirmação |
| Merge por **nome completo**, sem casamento parcial | similaridade/nome parcial | dele. E o dado defendeu a regra: o único par com telefone igual e nome diferente são **dois irmãos** (`Luiz Miguel` e `Lucas Gabriel Delgado Monteiro da Silva`) — afrouxar teria fundido pessoas distintas |
| **Mateus** = paciente de teste | merge | dele |
| **Manter tudo de WhatsApp** na limpeza | apagar `send-pdf.ts` e as tabelas do bot | dele |
| Colunas em `orcamentos` + índice único, **não** tabela nova de plano de pagamento | `orcamento_planos_pagamento` 1:N | 64 orçamentos, 0 usos de `condicoes_pagamento`; renegociação é feature a especificar, não presumir. Migração pra tabela depois é aditiva |
| **`valor_acordado`** como coluna única de "quanto se deve" | comparar sempre contra `total` | desconto à vista faz `soma(pagos)` nunca alcançar `total` → orçamento nunca auto-aprova |
| Atalho de 1 clique **fecha a próxima parcela**, nunca quita tudo | quitar tudo · desabilitar com plano | quitar tudo erra invisível; desabilitar empurra a secretária pro caminho pior (linha nova). Dele: "prefiro 2 cliques e número confiável" |
| Duas specs em vez de uma | um documento único | teto de ~300 linhas e mistura de assuntos (modelo de dado × migração de dado) |

## Erros e como pensei em resolver

| Erro | Causa / hipótese | Como eu ia resolver | Resolvido? |
|---|---|---|---|
| Afirmei que "22 de 26 fichas com procedimento vazio foram editadas depois" como evidência de P5 | `updated_at` contaminado por UPDATE em massa: **32 fichas, 3 clínicas, 6 dentistas, 14 datas de criação** no mesmo dia 18/07 = migration `100_ficha_data_atendimento` | agrupar por dia de edição e olhar dispersão de clínica/dentista antes de usar `updated_at` como sinal | **Sim** — retratei e parei de usar `updated_at` |
| Disse que P5 e P9 eram provavelmente o mesmo bug | pressa em unificar causas; o sintoma "não salva" parecia um só | separar por mecanismo, não por sintoma | **Sim** — são dois independentes |
| Disse que a causa do P5 era a precedência do texto do formulário sobre o derivado | é **uma** das três caras, não a raiz | procurar a fonte de verdade em vez do sintoma | **Sim** — a raiz é a dupla representação (texto por dente × eventos) |
| Apontei `eventoViewParaDraft` como suspeito nº1 do dente azul | parecia o ponto de conversão salvo→rascunho | ler a função campo por campo | **Sim, exonerada** — repassa `status`/`origem`/`ancora` sem transformação. O verificador confirmou |
| Marquei "Preencher restante" como **JÁ TEM** no inventário do orçamento | o tile equivalente existe, mas **dentro do `else` de `pagamentos.length === 0`** | conferir o gate, não só a existência | **Sim** — virou porte 12. Perda de função marcada como "já tem" é o erro exato que o inventário existia pra evitar |
| Escrevi que o atalho de 1 clique fica "bloqueado pela R-34" | não olhei quantos chamadores a action tem | contar chamadores antes de sequenciar | **Sim** — tem **um** chamador (a tela que morre). Ou porta antes, ou o corte espera |
| Contrato do atalho dizia "fecha a próxima parcela aberta" **sem definir "aberta"** | `atualizarStatusOrcamento` insere pendente do total cheio com `parcela_numero` nulo — cai no meio da ambiguidade | definir normativamente | **Sim** — "aberta" = `status='pendente'`, `parcela_numero` fora da definição |
| Marquei `lib/export/csv.ts` como morto **contra o que meu próprio script dizia** | fui "confirmar" com `grep -v "export/"` e filtrei justamente `from '@/lib/export/csv'` | rodar typecheck em vez de grep | **Sim** — typecheck acusou em segundos. **Eu buscava confirmação, não refutação** |
| Recomendei tratar o bucket público "fora da fila" | subestimei que a correção toca caminho + policy + bucket | aplicar o critério dele: se toca exposição de dado, entra com plano | **Sim** — retirei o pedido |
| Chamei `tw-animate-css` de dependência morta | varredura só olhava `.ts`/`.tsx`; ele é `@import` no `globals.css:2` | incluir CSS e config no haystack | **Sim** |
| Primeira varredura perdeu 3 arquivos mortos | morte **transitiva** — arquivo só referenciado por arquivo morto | iterar até estabilizar | **Sim** — achou `ganhos-7dias-chart`, `export/columns`, camada de erro |

## O que eu estava cogitando

- **Consolidar as 6 representações de procedimento** (`procedimentos`,
  `procedimentos_concluidos`, `procedimentos_status`, `dentes_afetados`, `dentes_observacoes`,
  `odontograma_eventos`). É a dívida de fundo da ficha, mas é migrar dado clínico de 82 fichas
  reais. Deixei fora de escopo com o lançamento próximo — mas o item 10 da R-33 e a Parte 4 da
  R-30 já encostam nisso, e em algum momento vai cobrar.
- **A infraestrutura especulativa é sintoma, não acidente.** 17 dos 31 arquivos mortos são
  andaime escrito antes da necessidade — e os comentários confessam (`jobs/index.ts`: *"Ready
  to migrate to Inngest"*; `webhook-payload.ts`: *"for future integrations"*). O `mutation-guard`
  é o caso mais irônico: 93 linhas ensinando a proteger update de orçamento, com exemplo pronto,
  e o padrão foi reimplementado à mão em dois lugares. Vale uma conversa sobre o que dispara
  isso, não só apagar.
- **A camada de erro tipada morreu em bloco** (`app-error` + `error-codes` + `classify`) enquanto
  o `CLAUDE.md` pede "erros tratados explicitamente". E `orcamentos/actions.ts` devolve
  `error.message` cru em **9 pontos**, sanitiza em 3. Os dois fatos juntos contam a mesma
  história.
- **A secretária é hoje um ponto único de falha operacional:** é a única que vê os 50 orçamentos
  e a única para quem "Registrar recebimento" funciona (a função nunca gravou uma linha — 0 em
  `receitas_manuais`, 0 `pagamentos` com `dentista_id` nulo). Perguntei se é intencional; não
  voltou resposta.
- **Remover a fotinha em vez de proteger.** Ele disse que é opcional pra clínica, e o dado
  ajuda: 1 de 238 pacientes, 0 de 13 dentistas, e o `uploadPatientPhoto.ts` **já está morto**.
  A funcionalidade se removeu pela metade sozinha. Esperando o ok — apaga foto de paciente real.
- **Modo de pagamento por procedimento** foi pedido no fim da sessão e não entrou na R-34 ainda.
  Precisa de vínculo novo entre `pagamentos` e `orcamento_itens` — hoje pagamento se liga ao
  orçamento inteiro, então o sistema sabe "entraram R$150" e não sabe "foram da extração do 38".

## Arquivos alterados

**Criados** (`plans/specs/`):
`R-30-ficha-fonte-unica-procedimento.md` · `R-31-paciente-unico-prevencao-e-merge.md` ·
`R-32-orcamento-visivel-autor-admin-secretaria.md` · `R-33-orcamento-tela-unica.md` ·
`R-34-plano-de-pagamento.md` · `R-35-riscos-nao-reportados.md`

**Reescritos no fechamento:** `plans/ESTADO.md` · `plans/ROADMAP.md` (linhas de R-30..R-35)

**Código de produção: nenhum.** A branch `limpeza/codigo-morto` (31 remoções, typecheck e build
verdes) foi **desfeita de propósito** — a lista verificada é o entregável e está na R-35 §13,
reproduzível num `git rm`.
