# Handoff — 2026-08-01 00:07

> **HANDOFF** · **R-46** (R-46g, R-46b, R-46b2) · sessão #10, seguindo do handoff de 31/07
> 22:00 · modo **planejamento → execução**, um bloco de discussão pontual no meio (R-28,
> atrito da ficha)
> Sessão longa: fechou o R-46g (porta) — spec, código, verificação parcial ao vivo. Achou
> que o R-46b original ("registrar") tinha muito mais construção nova que reuso — quebrou em
> R-46b + R-46b2 depois de pesquisa de código que corrigiu 3 alegações do artefato. Codou o
> R-46b inteiro. Também: plano do R-28 Parte 3 (pagamento duplicado), e um documento novo de
> discussão sobre atrito de formulário (orto/endo).
> **Nada commitado.** Trabalho local desde `02dd01a` (fechamento da sessão passada).

## O que trabalhamos

1. **R-28 Parte 3** — ele pediu plano pro "saldo pendente fantasma" (pagamento duplicado
   achado na sessão passada). Rodei o agente `planner`; plano ficou dentro da spec do R-28
   existente, como Parte 3. Não implementado — 3 decisões esperando ele (D7/D8/D9).
2. **R-46 A6/A7** — feedback ao vivo dele sobre o hero do dashboard (botão "Entrar no Modo
   Consulta" mais forte que "Ver meu dia") e a coluna de pendências (queria formato de
   ficha, não lista crua). A7 virou ajuste pontual em `contexto-coluna.tsx` (eventos reais
   da última visita em vez da frase "Evolução"). A6 virou a spec do R-46g.
3. **R-46g — "a porta"** — spec completa (inventário de 15 capacidades do `/consulta` e 7
   portas vivas, por varredura de 13 agentes com verificação adversarial), aprovada, codada
   (8 itens: renomeia e reaponta o CTA do hero, saída pro atendimento no rail, alergia no
   Meu dia, robustez de erro/loading, navegação mobile). Verificação ao vivo parcial —
   6 de ~15 gates confirmados clicando de verdade antes do pane do browser voltar a travar.
4. **R-46b + R-46b2** — ele pediu a spec de "registrar no Meu dia". Pesquisa de código (3
   agentes lendo `Odontograma`/`ToothDetailPanel`/`salvarFicha`/RPC 107/catálogo de
   procedimentos por completo, não em excertos) achou que o artefato superestimava o reuso
   em 3 pontos e that "assina com CRO+data" não existe como mecanismo simples — corrigi o
   D10 do R-46 principal. Quebrei em duas specs, ele aprovou as duas, e codei o R-46b
   inteiro nesta sessão.
5. **Discussão "como diminuir o atrito"** — ele trouxe um ponto novo (chips de orto e
   tabela de endo têm atrito real, quer soluções gerais, não só nesses dois casos). Virou
   documento novo em `plans/discussoes/`, com uma regra (estado × evento) e 3 casos
   medidos no código real.

## O que concluímos

**Pronto e verificado ao vivo (clicando de verdade):**
- R-46a — retestado no início da sessão, funcionando.
- A7 (última visita com eventos reais) — visto renderizando certo depois.
- R-46g: G2 (`?ag=` pré-seleciona o slot certo), G4 (`?ag` inválido cai no default), G6
  (375px sem overflow), G8 (clicar no card não navega), G14/G15 (chip de alergia aparece e
  some certo). Bônus: vi "Continuar atendimento" aparecer sozinho quando um agendamento de
  teste virou `in_progress` de verdade — confirma o rótulo condicional que eu não tinha
  testado.

**🟡 codado, verificado só por typecheck/lint + estrutura (árvore de acessibilidade), NUNCA clicado:**
- **R-46g** — G1/G3/G5, G7 parcial (só vi o rótulo `checked_in`, não `in_progress`/
  `completed`), G9–G13 (efeito colateral do status + não-regressão das outras portas).
  Evitei clicar em "Iniciar consulta" de propósito pra não sujar dado de teste sem
  necessidade real de prova.
- **R-46b** — typecheck e lint limpos. A página carregou sem erro nenhum (nem de servidor,
  nem de console) e a árvore de acessibilidade bateu certo: 16 botões "fazer hoje" pros 16
  eventos pendentes reais, odontograma completo com os 32 dentes, typeahead/chips/status
  presentes. **Zero clique, zero tecla digitada** — o pane parou de compositar (mesmo
  sintoma intermitente do resto da sessão) bem na hora de testar interação.

**Pela metade / não iniciado:**
- **R-46b2** ("Salvar e chamar próximo") — spec aprovada, nada codado. Sem ele o rascunho
  do R-46b não vira ficha em lugar nenhum — é o próximo passo óbvio.
- **R-28 Parte 3** — plano escrito, zero linha de código.
- **Discussão do atrito (orto/endo)** — só o documento. O achado do endo (`detalhe` não
  carrega ao continuar tratamento, `ToothDetailPanel.tsx:294`) ainda não é item de roadmap.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Botão "Entrar no Modo Consulta" vira o trigger pro Meu dia | Manter os dois botões (hero + "Ver meu dia") separados | Pedido direto dele, 31/07 |
| R-46g: a troca do CTA só entra **junto** com o rail ganhar saída pro atendimento | Trocar o destino do botão sozinho | Sem a saída, a ação primária do dashboard vira beco sem saída |
| Alergia entra no R-46g (D9), não espera o modelo novo do R-46f | Esperar o R-46f | "É muito bom ter esse dado" — e é reaproveitar o chip que o hero já tem, não construir |
| "Salvar e chamar próximo" fica **sem** assinatura dentro do gesto | Incluir assinatura (CRO+data) no botão, como o D10 original dizia | O único mecanismo de assinatura que existe hoje exige o paciente desenhando num canvas — ele confirmou que quer algo mais simples, que **não existe ainda** (é achado, não just decisão de design) |
| Catálogo de procedimentos do R-46b sem frequência de uso — usa o que já está no sistema | Calcular frequência real (união de `fichas.procedimentos` + `orcamento_itens`) | "Muito relativo", decisão dele — não vale o investimento agora |
| R-46 quebrado em specs próprias por fatia (R-46g, R-46b, R-46b2), não uma spec-mãe só | Manter tudo dentro de `R-46-meu-dia.md` | Cada fatia sozinha já passava o teto de ~300 linhas quando pesquisada a fundo |
| R-46b dividido de R-46b2 | Uma spec só pro registrar+salvar | Isola a decisão de assinatura (que quase travou tudo) do resto, que não dependia dela |
| Ordem do R-46 muda: g entra antes de b/b2, que entram antes do colar do Word (c) | Manter a ordem original (a→c→b→d→e→f) | Sem registrar, a hipótese de atrito da spec nunca é testável de verdade — "vamos seguir", dele |

## Erros e como pensei em resolver

| Erro | Causa (ou hipótese) | Como pensei em resolver | Resolvido? |
|---|---|---|---|
| `TypeError: Cannot read properties of undefined (reading 'data')` em `getMeuDiaData`, no 4º item do `Promise.all` (a query nova do catálogo) | Hipótese: artefato de HMR — muitas edições seguidas no mesmo arquivo confundiram o fast refresh do webpack (o código, relido com calma, estava sintaticamente correto: array de 4 promises, 4 tuplas destruturadas) | Reiniciei o dev server do zero em vez de caçar um bug de código que eu não achava relendo | **Sim** — logs do servidor limpos depois do restart, página renderizou certo |
| Browser pane parou de compositar bem na hora de testar clique/digitação no R-46b | Mesmo sintoma intermitente do resto da sessão (documentado nos handoffs anteriores) — do lado do ambiente, não do código; `getBoundingClientRect()` zerado mesmo com o DOM correto | Segui com verificação estrutural (árvore de acessibilidade — confirma o quê está lá, não confirma que clicar funciona) em vez de fingir teste de interação | **Parcial** — renderização confirmada, interação não |
| Achei (mid-implementação) que o typeahead do R-46b não tinha como buscar a tabela `procedimentos` como a spec original pedia — não existe de-para entre nome comercial livre e o tipo fechado que o evento clínico exige | Gap real de dado, achado só ao tentar codar — a spec D2 assumia que dava pra usar o catálogo direto | Troquei o typeahead pra buscar `TIPO_LABEL` (os 16 tipos que o resto do sistema já usa) em vez de inventar um mapeamento nome→tipo, que seria adivinhar em cima de prontuário | **Decisão registrada, não resolvida** — `catalogoProcedimentos` continua buscado e pronto, só não fiado; ele precisa decidir a fonte de verdade |

## O que eu estava cogitando

- **Parar o R-46b na primeira aberta (typeahead) em vez de codar e avisar depois?** Decidi
  seguir porque as duas decisões que tomei sozinho (fonte do typeahead, "fazer hoje" direto)
  eram baratas de reverter — nenhuma delas grava no banco (R-46b inteiro é rascunho local) —
  e parar ali teria cortado a sessão ao meio sem entregar nada testável. Registrei os dois
  pontos bem claros na spec (`R-46b §7.1`, `§9 A3/A4`) pra ele revisar sem precisar
  reconstruir meu raciocínio.
- **O achado do endo (carry-forward do `detalhe`) ainda não é item de roadmap** — só vive
  no documento de discussão. Cogitei abrir como R-48 já nesta sessão, mas ele não confirmou
  a ordem (antes ou depois do R-46b) — fica esperando.
- **Não toquei no R-28 Parte 3 nem no R-46g A1** (gate de assinatura/plano pago) — os dois
  são decisões de negócio (dinheiro real, monetização) que não são minhas pra tomar. Ficam
  registradas como perguntas, não como trabalho pela metade.
- **Base UI já ter `popover`/`combobox` prontos foi sorte, não pesquisa prévia** — só
  descobri quando fui checar `dialog.tsx` pra copiar o estilo. Vale lembrar disso da próxima
  vez que precisar de um componente "novo": checar o que `@base-ui/react` já resolve antes
  de cogitar instalar algo.

## Arquivos alterados

**Novos:**
| Arquivo | O quê |
|---|---|
| `plans/specs/R-46g-porta-modo-consulta.md` | Spec da porta (hero → Meu dia) |
| `plans/specs/R-46b-registrar-meu-dia.md` | Spec de registrar no Meu dia |
| `plans/specs/R-46b2-salvar-chamar-proximo.md` | Spec do "Salvar e chamar próximo" |
| `plans/auditorias/2026-07-31-inventario-consulta-meu-dia.md` | Inventário completo `/consulta` vs. Meu dia (base do R-46g) |
| `plans/discussoes/como-diminuir-o-atrito.md` | Discussão nova — regra estado×evento, casos orto/endo/ficha |
| `src/components/dashboard/abrir-meu-dia-button.tsx` | Substitui `consulta-cta-button.tsx` (removido) |
| `src/app/dashboard/meu-dia/loading.tsx`, `error.tsx` | Robustez da rota (R-46g D6) |
| `src/app/dashboard/meu-dia/_components/registrar-painel.tsx` | Painel de registrar (R-46b) |
| `src/app/dashboard/meu-dia/_components/onde-seletor.tsx` | Chips região + trigger da grid FDI |
| `src/app/dashboard/meu-dia/_components/fdi-popover.tsx` | Grid FDI leve, multi-toque |
| `src/components/ui/combobox.tsx`, `popover.tsx` | Wrappers novos sobre `@base-ui/react` (zero pacote novo) |

**Modificados:** `get-meu-dia.ts` (searchParams `?ag=`, alertas D9, catálogo+pendência
completa D2/D4), `meu-dia-client.tsx` (seleção por `agendamentoId`, monta `RegistrarPainel`),
`rail.tsx` (ação de atender por slot), `contexto-coluna.tsx` (A7 + chip de alergia),
`next-appointment-hero.tsx` (CTA reapontado), `floating-dock.tsx`/`mobile-drawer.tsx` (item
Meu dia), `Odontograma.tsx` (exporta `TEETH_*_DEC`), `page.tsx` (fia `catalogoProcedimentos`).

**Docs:** `R-46-meu-dia.md` (D10 corrigido, ordem das fatias, links pras specs novas),
`R-28-pagamento-fecha-sem-duplicar.md` (Parte 3), `ROADMAP.md`, `ESTADO.md`.

**Nada commitado.** `main` local segue em `02dd01a` — tudo acima é working tree.
