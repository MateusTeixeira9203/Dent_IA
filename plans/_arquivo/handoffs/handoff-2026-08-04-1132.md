# Handoff — 2026-08-04 11:32

> **HANDOFF** · **R-46** (Meu dia) — R-58, R-53, investigação C6/R-46d · sessão #17, seguindo
> do handoff de 04/08 01:31 · modo **execução → planejamento/auditoria** — os dois itens da
> fila (R-58, R-53) fecharam em execução pura; "terminar o bloco 1" virou auditoria de specs +
> medição no fim, sem código novo.

## O que trabalhamos

1. **Executei R-58 (histórico detalhado) e R-53 (orçamento agregado) até o fim** — as duas
   specs já estavam prontas pra codar (aprovadas/contrato) da sessão anterior. Escopo
   confirmado com ele antes de cada uma (R-58 explicitamente; R-53 veio do "segue pro R-53").
2. **Achei e corrigi 1 bug real em cada item, ao vivo, não em leitura de código** — R-58: fichas
   do mesmo dia trocavam evento uma da outra em `feitosAqui`. R-53: nada quebrado, mas o G1
   revelou que a spec citava o paciente errado (rastreei o caso real por varredura SQL).
3. **Pedi aprovação antes de escrever dado de teste 2× nesta sessão** — fixture temporária no
   paciente "Teste R-31a (apagar)" pro G1 do R-58, e um orçamento real (criado e apagado pelo
   fluxo normal do produto) pro G5 do R-53. As duas vezes ele aprovou antes.
4. **Organizei e commitei em 4 commits separados** (refactor · feat R-58 · feat R-53 · docs) —
   `FichasTab.tsx` tinha mudança das duas features entrelaçada (mesmo padrão do R-51+R-52 do
   handoff anterior: "cirurgia além do que `git add -p` resolve"), então virou o commit de
   refactor que os outros dois dependem.
5. **"Vamos continuar pra terminar o bloco 1"** — antes de codar C6/R-46d D1 (que o ROADMAP já
   apontava como próximo), segui o próprio `MAPA-MEU-DIA.md`: achei que a etapa "4.5 — D5 + medir
   o G1 de verdade" nunca tinha sido feita, e que a tabela de contradições (§5) tinha 6 itens
   "Eu"-owned nunca fechados. Investiguei os dois antes de tocar em código novo.

## O que concluímos

**Codado, verificado ao vivo, commitado (4 commits, `git log` no fim):**
- **R-58** — 🟡 hierarquia invertida no histórico do Meu dia (texto em evidência, indicados
  aparecem, badge "N em aberto", evento fechado depois nas 2 entradas). G1/G2/G3/G4/G5/G8/G9
  provados ao vivo. **G6/G7 parciais** — sem dado real de endodontia nem texto >4 linhas pro
  dentista logado hoje.
- **R-53** — 🟡 orçamento nasce do agregado de todos os indicados abertos do paciente, não só
  1 ficha. G1 provado 2× (14 itens exato, depois 11/3 pelo filtro de responsável — aritmética
  bate). G4/G5/G6/G7/G8 provados (G5 com orçamento real criado, PDF 200 OK, apagado depois).
  **G2/G3 parciais** — zero indicado assinado existe hoje (garantia só por código); o único
  evento encaminhado elegível do banco é o único indicado do paciente dele, então o chip de
  responsável nem renderiza (regra "solo não mostra chip") — não vi o nome do destino no chip,
  só confirmei que o evento entra no orçamento.
- **G9 dos dois** (2 contas logadas) — **não verificado.** Só a conta "teste" disponível nesta
  sessão; login é dele, não meu.

**Refactor que os dois dependem:**
- `eventosParaCards` (FichasTab) → `src/lib/odontograma/eventos-para-cards.ts`, genérica sobre
  T, com 5 testes. `corpoEspecialidade` → `src/components/fichas/corpo-especialidade.tsx`
  (fica em components/, não lib/, por retornar JSX — os dois SAÍAM juntos do D0 do R-46d, mas
  não tinham por que morar no mesmo arquivo). Chips de responsável (R-16) →
  `src/components/fichas/chips-responsavel.tsx`, reusado agora no modal de orçamento.
- `tsconfig.json` ganhou `allowImportingTsExtensions` — sem isso `node --test` não resolvia o
  import relativo de um lib file pra outro (cenário que o D0 nunca tinha exercitado, zero
  import relativo).

**Investigação (sem código, só `plans/`):**
- `MAPA-MEU-DIA.md` §5 — as 6 contradições "Eu"-owned (C2, C3, C4, C5, C9, C10) **já estavam
  resolvidas** nos specs reais (`R-51-53`, `R-46-C6`, `R-46d`, `R-55`) — conferido texto por
  texto. O mapa é que nunca foi atualizado depois das emendas de 04/08. Corrigido.
- **Medição ao vivo do gate de entrada do C6** (1440×900, pós-R-58): a coluna **centro** do
  cockpit (odontograma + `RegistrarPainel`) mede 635px e termina em y=937 — **37px além do
  viewport, antes do dock** (mais ~112px de overlay por cima). Não é mais hipótese — **C6 está
  confirmado urgente**, e quem ataca esse número é o R-46d D1 (D12 tira ONDE/STATUS da faixa
  fixa), não o C6 (que mexe só nas laterais).
- **D5 (piso de 36px)** remedido: ainda falha nos mesmos controles de 03/08 — chips ONDE/STATUS
  (28px), tabs do odontograma/Legenda (~28px), "ver mais" do histórico (18px). Achado novo: os
  chips ONDE/STATUS e o "+ texto da visita" **morrem sozinhos com o R-46d D12/D7** — não
  precisam de fix próprio.

**🟡 Nada subiu — 4 commits locais (mais os 20 de antes), working tree só com `plans/`.**

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| `eventosParaCards` (dado puro) → `lib/`; `corpoEspecialidade` (JSX) → `components/` | 1 arquivo só pros dois, como o D0 fez | D0 nunca teve esse problema (funções puras só). Aqui uma retorna JSX — respeita a fronteira lib/components que o `CLAUDE.md` define |
| `feitosAqui` do R-58 filtra por **data indicada ≠ data desta ficha**, não só `ficha_id != f.id` | manter o filtro original do contrato | Achado ao vivo: 2 fichas do MESMO dia (C0.1 — "caso normal") trocavam evento uma da outra. `ficha_id != f.id` sozinho não distingue "indicado noutro dia" de "indicado na ficha irmã de hoje" |
| G1 do R-58: usar fixture de teste (criar, provar, apagar) em vez de aceitar sem prova completa | aceitar `4df91e93` como estava documentado, sem notar que o evento não existe nesse paciente | Ele aprovou explicitamente depois que expliquei o achado (paciente errado na spec + só metade do mecanismo provável sem escrita) |
| `abrirOrcamentoParaFicha` (R-53) converge pro MESMO agregado do botão geral — `fichaId` clicado vira só contexto, nunca fonte dos itens | manter mostrando só a ficha clicada | Já era a decisão escrita na spec (§2) — confirmei que funciona ao vivo (14 itens do paciente inteiro, não os 3 da ficha clicada) |
| G5 do R-53: criar um orçamento real, verificar lista/detalhe/PDF, apagar pelo fluxo normal | aceitar sem prova ao vivo (como o G2/G3 ficaram) | Ele aprovou explicitamente — pediu esse nível de prova pra essa gate específica |
| Contradições C2/C3/C4/C5/C9/C10 do mapa marcadas ✅ resolvidas, sem reabrir como decisão dele | tratar cada uma como pendência esperando resposta dele | Verificação direta contra o texto ATUAL dos specs mostrou que já estavam corrigidas (algumas com nota própria "corrigido em 03/08 noite") — não eram decisões em aberto, era o mapa desatualizado |
| Commit em 4 blocos (refactor · R-58 · R-53 · docs), refactor primeiro | 1 commit por feature, aceitando `FichasTab.tsx` misturado | Mesmo padrão do R-51+R-52 no handoff anterior — as mudanças de R-58 e R-53 em `FichasTab.tsx` estão em regiões diferentes mas o arquivo é físico só 1; separar por feature exigiria cirurgia de hunk que não compensa. Isolei o que É puramente refactor (comportamento idêntico) num commit à parte, que é mais limpo que misturar refactor com feature |

## Erros e como pensei em resolver

| Erro | Causa/hipótese | Como resolvi (ou ia resolver) | Resolvido? |
|---|---|---|---|
| Spec do R-58 citava paciente errado pro caso do G1 (`4df91e93` em vez do real) | Provável erro de transcrição ao escrever a spec, sessão anterior — não confirmei a causa, só o efeito | Rastreei o caso real por varredura SQL completa na tabela inteira (não confiei no ID citado), achei `abf0fd7f`, expliquei o achado antes de seguir | Sim |
| `feitosAqui` cruzava eventos entre fichas do MESMO dia — bug não previsto em nenhum invariante do contrato | `ficha_id !== f.id` sozinho não distingue "ficha de outro dia" de "ficha irmã de hoje"; não pensei nesse caso ao escrever o filtro original, só testando ao vivo apareceu | Achei clicando no modal real (não relendo código) — 2 fichas de 03/08 mostravam o card uma da outra. Corrigi comparando a data indicada (`fichaDataPorId.get(e.ficha_id)`) contra a data da ficha atual, reverifiquei ao vivo com reload | Sim |
| `node --test` não resolvia import relativo entre 2 libs (`ERR_MODULE_NOT_FOUND`) | Node ESM exige extensão explícita em specifier relativo; D0 nunca tinha import relativo nenhum, então o projeto nunca bateu nesse caso | `allowImportingTsExtensions` no `tsconfig.json` + `.ts` explícito no import — confirmei com `tsc --noEmit` e `next build` completos que não quebra nada além do pretendido | Sim |
| Cabeçalho do ROADMAP dizia "13 commits acumulados"; contei 20 de verdade via `git log` | O número nunca foi recontado entre sessões, só carregado adiante | Contei, corrigi, registrei a causa provável (subcontagem de 2 sessões inteiras) — sem isso a decisão de push ficaria baseada em número errado | Sim |
| G3 do R-53 (responsável exibido = destino do encaminhamento) só meio provado | O único evento encaminhado elegível do banco inteiro é o único indicado do paciente dele — sem 2º responsável, o chip nem renderiza (regra "solo não mostra chip") | Documentei como parcial, expliquei por que o mecanismo é confiável mesmo assim (código idêntico ao dos chips da ficha, R-16, em produção desde 26/07) — não criei um cenário sintético só pra forçar a prova visual | Não — fica 🟡, precisa de caso real com 2+ responsáveis incluindo 1 encaminhado |
| G9 (2 contas) dos dois itens não verificado | Só a conta "teste" disponível; login com senha é dele, "Login com senha é você — eu não faço" (regra já registrada de auditoria anterior) | Documentei como não verificado em vez de tentar contornar | Não — fica pro gate de 2 contas do projeto |

## O que eu estava cogitando

- **Se valia perseguir a diferença exata entre a aritmética do MAPA (441px) e minha medição ao
  vivo (598px brutos no topo do grid, ~486px descontando o dock)** — os dois caminhos concordam
  na conclusão que importa (o cockpit estoura), então não investiguei a causa exata da
  diferença de ~45px. Pode ser deriva de código entre 03/08 e agora, ou diferença de método
  (aritmética de CSS × medição de `getBoundingClientRect`) — registrado como não perseguido,
  não como resolvido.
- **Se valia criar um 3º cenário de teste (escrita) só pra fechar o G3 do R-53 de vez** — decidi
  que não: já tinha usado 2 rodadas de aprovação de escrita de teste na mesma sessão (G1 do
  R-58, G5 do R-53), e pedir uma terceira sem necessidade de produto clara pareceu abuso da
  abertura que ele deu, não uso dela.
- **A pergunta que fiz e não foi respondida:** C6 primeiro e sozinho, ou C6+R-46d D1 juntos numa
  tacada (como o ROADMAP já sugeria)? Ele encerrou a sessão antes de escolher — é a primeira
  coisa a perguntar na próxima.

## Arquivos alterados

**4 commits nesta sessão** (mais os 20 acumulados de sessões anteriores, nada pushado):
```
a86afe0 docs(plans): specs R-58/R-53 verificadas ao vivo + roadmap atualizado
71a4f4e feat(orcamentos): nasce dos indicados em aberto do paciente — R-53
fd78c4b feat(meu-dia): histórico detalhado com texto em evidência — R-58
77b4980 refactor(fichas): extrai cards de registro e chips de responsável pra libs compartilhadas
```

**Novos:** `src/lib/odontograma/eventos-para-cards.ts` + `.test.ts` ·
`src/components/fichas/corpo-especialidade.tsx` · `src/components/fichas/chips-responsavel.tsx`

**Modificados (código):** `src/server/dashboard/get-meu-dia.ts` ·
`src/app/dashboard/meu-dia/_components/historico-bloco.tsx` ·
`src/app/dashboard/pacientes/[id]/_components/types.ts` ·
`src/app/dashboard/pacientes/[id]/_components/paciente-detail-client.tsx` ·
`src/app/dashboard/pacientes/[id]/_components/modals/novo-orcamento-modal.tsx` ·
`src/components/pacientes/FichasTab.tsx` · `tsconfig.json`

**`plans/` (não commitado ainda, fica pra esta escrita de fechamento):**
`plans/specs/R-58-historico-detalhado.md` · `plans/specs/R-53-orcamento-indicados-abertos.md` ·
`plans/MAPA-MEU-DIA.md` · `plans/ROADMAP.md`

**Nenhuma migration. Nenhuma mudança de RLS.** 2 escritas de teste em dado real (fixture do
R-58 + orçamento do R-53), as duas aprovadas antes e revertidas depois — banco de produção
está no mesmo estado clínico de antes da sessão.
