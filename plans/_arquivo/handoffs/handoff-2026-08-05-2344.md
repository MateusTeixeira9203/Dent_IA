# Handoff — 2026-08-05 23:44

> **HANDOFF** · **R-63** (Meu dia → cockpit, sub-item do R-46) · sessão #20, contínua desde o
> handoff de 05/08 13:11 · modo **planejamento → execução**, sem pausa entre os dois (ele
> aprovou a spec na hora e eu segui pra F1 sem trocar de sessão) · **1 parada de discussão**
> no meio (o estouro vertical, que virou decisão codada, testada e revertida na mesma
> conversa) · **encerrada por ele com F2 investigada mas não iniciada** (contexto do chat cheio)

## O que trabalhamos

1. **Fechei a discussão de layout que ficou pendente do handoff anterior** ("melhorar o
   layout pensado como dentista durante a consulta", explicitamente adiada pra esta sessão).
   Medi o cockpit real contra o `MAPA-MEU-DIA.md`, trouxe contrapontos (a régua de "gestos
   por registro" contra qualquer solução que sacrificasse alvo de toque), e usei
   `AskUserQuestion` com preview ASCII pra ele escolher entre 4 opções de layout — escolheu
   o híbrido (perfil do dente fixo + abas). Ele respondeu com 6 decisões numa mensagem só
   (D1-D7 na spec), incluindo "chairside = nada" e "procedimento precisa expandir com
   tabela", que abriram uma pergunta minha ("e se for adicionar em outro dente?") que ele
   respondeu ao vivo e virou a troca condicional (D5).
2. **Construí o artefato `R-63-layout-meu-dia.html`** com geometria do dente PORTADA de
   `tooth-geometry.ts` (não aproximada — aprendizado de 22/07 aplicado), tabela de
   `endo-form.tsx`, orto de `orto-form.tsx`. Iterei 3 vezes ao vivo, cada uma respondendo a
   um teste dele: a pergunta "e se for adicionar" (nasceu a troca condicional), a decisão
   "fecha o perfil, a tabela some, o odontograma só some se precisar" (nasceu o gatilho
   condicional + motion), e "o ditado devolve o mapa" (nasceram os 3 casos do
   `ditadoDevolveMapa`, com o caso do mesmo-dente e o de âncora de boca).
3. **Escrevi a spec `R-63-layout-cockpit-slot-central.md`** sozinho no thread principal (não
   acionei o agente `planner` — as decisões já tinham saído da conversa, um subagente teria
   menos contexto que eu). Ele aprovou na hora. Executei a **F1** (slot central):
   lifting do `detalheAbertoIdx` em `ToothDetailPanel.tsx`, `ditadoDevolveMapa` extraído com
   9 testes unitários (`node --test`), CTA `bg-teal`→`bg-teal-dark`, motion com
   `useReducedMotion()`.
4. **Verifiquei a F1 ao vivo** — o Browser pane travou no meio (mesmo sintoma sem causa raiz
   do handoff de hoje cedo: `document.hidden=true`, cliques não registravam) e voltou
   sozinho quando ele reabriu o pane. Rodei G1-G8/G11 clicando de verdade, achei e corrigi
   **1 bug real** (`onDetalheAbertoChange` chamado durante o render — aviso do React), e com
   autorização explícita dele cliquei **Salvar de verdade** pra provar G9 (Histórico 9→10).
5. **Discussão + ciclo completo do estouro vertical, tudo na mesma sessão.** Remedi ao vivo
   com a F1 no ar — os números do `MAPA-MEU-DIA.md` (703×441) estavam desatualizados; real
   era 598×474 (mapa), 567px (endo, já cabia), 659px (orto). Ele decidiu "scroll só na
   coluna central" — codei (`overflow-y-auto` + teto medido via `getBoundingClientRect` +
   `--dock-inset`, recalculado por `ResizeObserver`), testei os 3 gates ao vivo, todos
   passaram. **Ele testou na tela de verdade e mandou reverter** — scroll aninhado incomodou
   na prática, mesmo com os números fechando no papel. Revertido por completo, zero resíduo.
6. **Comecei a investigar a F2** (colunas viram abas) quando ele encerrou por contexto
   cheio — só leitura (`nesta-sessao-bloco.tsx`, `anexar-documentos-bloco.tsx`, releitura
   completa de `meu-dia-client.tsx`) e 1 grep confirmando que os 4 blocos consumidores do
   `BlocoMoldavel` não têm uso fora de `meu-dia/_components`. **Nenhuma linha de código
   escrita pra F2.**

## O que concluímos

**🟡 F1 do R-63 — codada e verificada ao vivo, nada commitado.** G1-G9 e G11 confirmados
clicando de verdade (a lista completa dos 9 gates está no ESTADO.md, não repito aqui). Só
**G10 não verificado** — o SO desta máquina não tem `prefers-reduced-motion` ligado, e a
ferramenta de browser não emula a media query.

**✅ Decisão do estouro vertical — fechada, sem código pendente.** Deixou de ser "fora de
escopo" (como estava no fim da sessão anterior) pra virar decisão explícita registrada na
spec §4.8: nenhum mecanismo novo, a página rola inteira quando o conteúdo excede o viewport
(como sempre rolou). O G1 do contrato do cockpit ("cabe sem scroll") fica formalmente
relaxado. O código do scroll interno foi implementado, testado, E revertido — o resultado
líquido é "nada mudou no comportamento", mas o raciocínio completo (por que se tentou, por
que se abandonou) está registrado na spec, não perdido.

**⏳ F2 (colunas em abas) — não iniciada.** Só investigação de leitura.

**⏳ F3 (varredura de AA restante) — não iniciada, nem investigada nesta sessão.**

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Troca condicional: só endo/implante e orto tomam o lugar do mapa; os outros 15 de 17 tipos abrem o perfil na direita e o mapa fica | Trocar sempre que houvesse qualquer tabela/perfil aberto | Ele perguntou "e se for adicionar em outro dente?" ao ver a 1ª versão (troca incondicional) — isso reprovava a régua de "3 gestos por registro" do próprio projeto |
| Ditado devolve o mapa só com confirmação real a dar (dente diferente do que a tabela aberta mostra) | Devolver sempre que houvesse qualquer ditado | Mesmo dente (arrancaria a tabela por nada) e âncora de boca (nível boca nunca pinta dente, D5 do R-06-07) devolveriam a tela sem motivo |
| CTA muda pra `bg-teal-dark` | Manter `bg-teal` | Fix do 3.38:1 (reprova AA) que o `MAPA-MEU-DIA.md` já tinha apontado por escrito em 04/08 e nunca foi feito |
| Estouro vertical: nenhum mecanismo novo — página rola inteira | Scroll interno só na coluna central (chegou a ser codado e testado) | Ele testou ao vivo e voltou atrás: *"exclua o scroll dentro do card, o dentista rola a página inteira do jeito que tava, tava sensacional"* — scroll aninhado lê pior na prática do que a medição sugeria |
| Eu escrevi a spec R-63 sozinho, sem acionar o agente `planner` | Acionar o `planner` pra formalizar | Todas as decisões (D1-D7) já tinham saído da conversa nas palavras dele; um subagente sem esse histórico produziria uma spec pior, não melhor |
| `detalheAbertoIdx` (ToothDetailPanel) notifica o pai via `useEffect`, não durante o render | Chamar `onDetalheAbertoChange` direto no corpo do componente (1ª versão) | Causava o aviso do React "Cannot update a component while rendering a different component" — só é seguro ajustar ESTADO PRÓPRIO durante o render (padrão `idAoResetar`), não uma callback que escreve no pai |

## Erros e como pensei em resolver

| Erro | Causa/hipótese | Como resolvi (ou ia resolver) | Resolvido? |
|---|---|---|---|
| Browser pane parou de compositar frames e de aceitar clique no meio da verificação da F1 — `document.hidden=true`, `getBoundingClientRect()` zerado em qualquer elemento, cliques (via `.click()`, via `PointerEvent`+`MouseEvent`, via invocar o `onClick` direto no fiber do React) sem efeito nenhum, nem no gesto mais simples possível (toggle Permanentes/Decíduos) | O próprio erro da ferramenta de screenshot disse a causa: "the Browser pane is not displayed, so the page is not compositing frames" — é estado do lado do cliente dele, fora do meu controle | Reportei o bloqueio com honestidade em vez de forçar ou fingir que tinha verificado; pedi pra ele mostrar o pane. Voltou sozinho quando ele reabriu | Sim, mas sem entender a causa raiz — 2ª ocorrência do mesmo sintoma no mesmo dia (a 1ª está no handoff das 13:11) |
| `onDetalheAbertoChange` disparava "Cannot update a component while rendering a different component" toda vez que o ditado trocava de dente com a tabela aberta (achado testando G6 pela 1ª vez) | Notificação pro pai chamada durante o corpo do componente filho — só é seguro ajustar estado PRÓPRIO durante o render (o padrão `idAoResetar` já usado no projeto), nunca uma callback que escreve estado alheio | Virei a notificação pra dentro de um `useEffect` (roda depois do commit); o reset do índice local continua durante o render, sem efeito, porque é estado do PRÓPRIO componente | Sim — repeti o mesmo gatilho 2× com dentes diferentes depois do fix, console limpo (só os 2 erros antigos, resquício de antes do fix, sem nenhum novo) |
| Minha 1ª medição da altura da coluna central devolveu `getBoundingClientRect` zerado em tudo (`maxHeight: none`, `overflowY: visible`, `styleAttr: null`) mesmo depois do código aplicar o style corretamente | Confundi `grids[0].children[1]` (o wrapper `min-w-0` que o `CockpitGrid` já cria) com o MEU wrapper — o meu ficava um nível mais fundo, como filho daquele | Corrigi a query pra `children[1].firstElementChild` | Sim |
| Scroll interno no centro: implementado, os 3 gates (G18-G20) passaram testando eu mesmo — mas ele rejeitou ao usar de verdade, no mesmo dia | Scroll aninhado (um scroll dentro de outro scroll potencial da página) é o tipo de coisa que só se sente usando, não lendo métrica — a medição (124px de estouro, mecanismo tecnicamente correto) não captura a sensação de uso | Revertido por completo a pedido dele — não tentei "salvar" a ideia com ajuste fino, ele foi direto e claro | Sim (resolvido = revertido; registrei o ciclo inteiro na spec §4.8 em vez de apagar o rastro) |

## O que eu estava cogitando

- **Se implementava o scroll interno (F1.5) direto ou parava pra confirmar mais antes** —
  decidi implementar direto, porque "vamos com o scroll só na coluna central" já era uma
  decisão explícita dele, não uma ideia solta. O fato de ele ter revertido depois de VER
  funcionando não significa que eu deveria ter perguntado mais antes de codar — algumas
  decisões de sensação de uso só se provam certas ou erradas testando de verdade, e o custo
  de testar e reverter foi baixo (zero resíduo, spec documentou o porquê).
- **Se cortava mais a spec R-63 pra caber no teto de ~300 linhas ou aceitava passar um
  pouco** — depois de 3 rodadas reais de corte (299→334, cortei pra 327→318→317), decidi
  parar em 317 (17 acima) porque mais corte começaria a tirar invariantes e os snippets de
  código que sustentam a implementação, não gordura de verdade. (A reversão do scroll
  interno depois trouxe a spec de volta pra **299 linhas** por conta própria, sem eu ter
  que voltar a mexer nisso.)
- **Se confirmava a autorização do G9 (Salvar real) antes de clicar** — ele já tinha
  autorizado escritas reais de teste em sessões anteriores, mas confirmei que ESTA sessão
  tinha autorização explícita e específica ("autoriza, pode clicar em Salvar") antes de
  gravar qualquer coisa no paciente "marcos".
- **Comecei a desenhar mentalmente a F2** antes de escrever qualquer coisa: um componente
  genérico de abas (tipo `AbasBloco`) que os dois lados reusam, com `AFazerBloco` /
  `NestaSessaoBloco` / `HistoricoBloco` / `AnexarDocumentosBloco` perdendo o `BlocoMoldavel`
  próprio (cabeçalho vira a aba, eles exporiam só o corpo). Não cheguei a validar essa forma
  com ele nem a escrever nada — é só a direção que eu tinha em mente quando a sessão
  encerrou. **Não é decisão, é ponto de partida pra próxima sessão.**
- **Não cheguei a pensar** em como tratar `painelDenteAberto` (que a spec diz que "morre" na
  F2) na prática — se é uma remoção limpa de estado ou se sobra algum uso residual. Fica
  como primeira coisa a checar ao retomar.

## Arquivos alterados

**Nesta sessão (novo):**
`plans/specs/R-63-layout-cockpit-slot-central.md` ·
`plans/artefatos/R-63-layout-meu-dia.html` ·
`src/lib/odontograma/ditado-devolve-mapa.ts` · `…/ditado-devolve-mapa.test.ts`

**Nesta sessão (modificado):**
`src/components/odontograma/ToothDetailPanel.tsx` (prop `onDetalheAbertoChange`, reset
durante o render por comparação de `dente`, notificação via `useEffect`, botão "‹ voltar ao
mapa" na seção portada) ·
`src/app/dashboard/meu-dia/_components/meu-dia-client.tsx` (`detalheEspecialidadeAberto`,
resets nos 3 pontos de troca de dente/paciente/Salvar — o scroll interno da F1.5 foi
adicionado e revertido, **zero resíduo**) ·
`src/app/dashboard/meu-dia/_components/registrar-painel.tsx` (`SlotCentral` derivado,
`OrtoForm` saiu da faixa fixa e virou ocupante do slot, `ditadoDevolveMapa` dentro de
`registrar()`, CTA `bg-teal-dark`, motion com `AnimatePresence`+`useReducedMotion`) ·
`plans/ROADMAP.md` (linha do R-63 atualizada 3×, refletindo aprovação → F1 verificada →
estouro vertical fechado) · `plans/ESTADO.md` (reescrito integralmente, ver abaixo)

**Lido nesta sessão, intocado (contexto pra F2, decisão, ou o próprio contrato manda ficar
puro):** `cockpit-grid.tsx` (contrato C1 — shell puro, não muda) ·
`nesta-sessao-bloco.tsx` · `anexar-documentos-bloco.tsx` · `a-fazer-bloco.tsx` ·
`historico-bloco.tsx` · `bloco-moldavel.tsx` · `endo-form.tsx` · `orto-form.tsx` ·
`Odontograma.tsx` · `tooth-geometry.ts` · `globals.css` · `get-meu-dia.ts`

**Herdado de sessões anteriores, não tocado nesta:** `evals/extracao-clinica/golden.json` ·
`evals/extracao-clinica/run.cjs` · `src/app/api/dex/formatar-evolucao/route.ts` ·
`src/app/dashboard/meu-dia/_components/campo-magico-meu-dia.tsx` ·
`src/app/dashboard/pacientes/[id]/_components/paciente-detail-client.tsx` ·
`src/components/fichas/captura-livre-card.tsx` · `src/components/ui/combobox.tsx` (deletado)
· `src/types/odontograma.ts` · specs R-46-C7/R-50/R-59/R-61/R-62 · artefatos R-46-C7/R-61 ·
`casar-procedimento-local.ts`/`.test.ts`

**Nenhum commit nesta sessão** — represado junto com C7/R-50/R-61/R-62 da sessão anterior
(decisão dele: só sobe quando a fatia do Meu dia fechar de vez). R-63 F1 entra na mesma pilha.

**Banco:** só a escrita real já registrada no ESTADO.md (Canal · dente 17, autorizada, gate
G9). Zero migration, zero mudança de RLS.
