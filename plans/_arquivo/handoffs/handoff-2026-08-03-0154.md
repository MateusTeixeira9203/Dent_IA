# Handoff — 2026-08-03 01:54

> **HANDOFF** · **R-46** (fatia cockpit) · sessão #13, seguindo do handoff de 02/08 20:30
> · modo **execução** (C0→C1→C2→C3 codados) intercalado com **discussão** (2 workflows de
> investigação sobre propostas dele que mexiam em decisão já aprovada)
> Sessão longa: o cockpit saiu de "contrato pronto, zero código" pra **C0, C1, C2 e C3
> codados, typecheck/lint/build limpos**. No meio, duas propostas dele (campo mágico como
> entrada primária; 3º status pra procedimento multi-sessão) foram investigadas a fundo
> antes de codar — as duas tinham motivo real por trás, mas a forma proposta quebrava coisa
> que ele não tinha visto. Nada commitado.

## O que trabalhamos

1. **C0** — `get-meu-dia.ts` passou a devolver `visitas[]` (histórico inteiro) e `jaFeito[]`
   (acumulado clínico), mais `pacienteIdade`. Corrigido no meio da sessão: o join evento↔visita
   era por **data**, decisão dele (03/08) de que "visita rápida" também gera ficha própria
   quebrava isso (2 fichas no mesmo dia) — trocado pra `ficha_id`, o vínculo real.
2. **C1** — grid de 3 colunas, `BlocoMoldavel` (acordeão), e os blocos Histórico/A fazer/Já
   feito/Nesta sessão. Depois de feedback dele ao vivo (screenshot), viraram: histórico só a
   última visita (nota sem corte de 160 chars) · "A fazer" as 5 pendências mais antigas ·
   "Nesta sessão" dividido em "Concluídos hoje"/"Novos procedimentos" pelo `status` do
   rascunho · ordem livre no registro (procedimento antes do "onde" não descarta mais) ·
   número do dente aceito direto no typeahead ("restauração 35").
3. **C2** — rail arrasta (limiar de 5px, clique suprimido em fase de captura no arraste) ·
   CTA virou "Salvar" (auto-avanço saiu, decisão dele) · as 2 travas do §5.6 contra ficha
   duplicada colapsaram numa condição só (`semRascunho`), local e síncrona.
4. **C3** — `ToothDetailPanel` foi pro lado do odontograma (290px, ele já tinha `className`
   pronto pra isso); coluna direita **esconde inteira** quando o dente abre, devolvendo
   312px pro centro (é isso que tira o dente de 22,8px — reprova WCAG — pra ~34px).
5. **2 workflows de investigação** sobre propostas dele que reabriam decisão aprovada:
   (a) campo mágico virar entrada primária de "procedimento + dente" — achado: as specs
   desenharam ele pra texto LONGO (upload/ditado), não pra isso, e a R-46 §5 F1 já decidiu
   campo mágico **com IA** — a proposta era reversão, não complemento; (b) 3º status
   "em andamento" pra procedimento multi-sessão — achado: quebra 23 arquivos em silêncio
   (zero exhaustive check no TS), e `grupo_id` já é o modelo, sem migration.

## O que concluímos

**Codado, typecheck/lint/build limpos, zero teste ao vivo por mim (ele testou C0/C1 e achou
3 bugs reais; C2/C3 ele ainda não testou):**
- C0, C1, C2, C3 do cockpit (`get-meu-dia.ts` + 7 componentes novos em `_components/`).
- `contexto-coluna.tsx` apagado (virou os 4 blocos da coluna esquerda/direita).

**🟡 Nada disso está verificado em produção** — só localhost, e só parte foi testada por ele
ao vivo (C0/C1). C2/C3 saíram nesta sessão sem ele ver ainda.

**Registrado, não codado:**
- **R-51 a R-54** no `ROADMAP.md` — modelo multi-sessão (`grupo_id`), encaminhar dentro do
  "A fazer", orçamento a partir dos indicados em aberto, e o bug da ficha duplicada no
  mesmo dia. Precisam de **uma spec só** (são interdependentes — R-53 depende de como R-51
  e R-52 definem "o que conta como pendente").

**Pela metade:**
- **C4** (campo mágico, espera R-46c) e **C5** (múltipla seleção) do contrato não entraram.
- A spec do bloco R-51-54 não foi escrita — só a investigação que a alimenta.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| `PageContainer variant="wide"` no lugar do wrapper que eu tinha inventado | `mx-auto max-w-[1440px]` sob medida | Achado ao vivo por ele: sem padding nenhum no topo, mais estreito que o resto do dashboard. `PageContainer` já existe e é o padrão das outras telas densas |
| Histórico mostra só a **última** visita, nota sem corte de 160 chars | Últimas 2 (minha proposta anterior) | Ele: "a última evolução é a mais importante" — corte de 160 saía sem função com só 1 visita na tela |
| "A fazer" mostra as **5 mais antigas** por padrão | 5 mais recentes | São as que estão apodrecendo na fila — ele foi explícito nisso |
| **Sem painel** pro "A fazer" — só scroll com respiro (`pr-2`) | Painel lateral (minha proposta) | Mostrei mockup dos 2 estados; ele julgou que o volume real (5-6, não 14 do teste) não justifica |
| Ordem livre no registro (~10 linhas) em vez de mexer no campo mágico | Campo mágico como entrada primária de procedimento | Workflow: campo mágico é pra texto longo (specs), não pra isso; o bug real era a ordem obrigatória, achado em `registrar-painel.tsx:132-137` |
| Número do dente aceito no typeahead | Gramática de região também (arcada/quadrante) | Metade barata do parser (validador FDI já existe); a metade cara é o de-para procedimento↔tipo, 55 de 161 chaves sem tipo — ficou de fora de propósito |
| Chips de "Onde" (+dente/Arc/Q1-Q4) **continuam** | Matar os chips, só texto livre | Workflow: sem eles não há rede de segurança pro erro de digitação em prontuário; `+dente` estava funcionando (testei nos 2 temas), o alvo de 26px é que é pequeno |
| Multi-sessão vira **derivado de `grupo_id`** (soma indicado+realizado no mesmo grupo) | 3º status `em_andamento` | Workflow: 3º status quebra 23 arquivos em silêncio (zero exhaustive check); `grupo_id` já é o modelo, zero migration; nenhum sistema de mercado persiste "em andamento" |
| Ficha = **atendimento** (toda visita, inclusive "rápida"); tratamento = **grupo_id** | "Toda visita é ficha nova" confundido com "todo tratamento é ficha nova" | Ele esclareceu: "não é que toda visita será uma ficha [nova], é que se ele iniciar novo tratamento aí sim" — os 2 conceitos são eixos diferentes |
| Encaminhar continua no **"A fazer"** | Mover pro "Nesta sessão" (proposta dele) | Mecanicamente incompatível: rascunho não tem id real no banco, e nasce com `status:'realizado'` fixo — a action só aceita `indicado` |
| "Nesta sessão" virou **"Concluídos hoje"** + **"Novos procedimentos"**, por `status` | Manter 1 bloco só | Dá lugar visível pro chip Status que já existia mas decidia algo invisível; "Novos procedimentos" é a base do orçamento depois de salvar |
| CTA "Salvar" (sem "e gerar orçamento") | Rótulo do artefato | Contrato §0: rótulo só vale quando o R-46h (modal de orçamento) existir |
| Direita **esconde inteira** quando o dente abre | "Faixa fina" com conteúdo (texto do contrato) | O artefato não tem essa tela — `.splitdente` é seção desconectada, 4000px mais abaixo na mesma página, sem ligação com o clique do cockpit. Sem desenho aprovado, escondi em vez de inventar |

## Erros e como pensei em resolver

| Erro | Causa | Como resolvi | Resolvido? |
|---|---|---|---|
| **C0 juntava evento à visita errada** | Join por `data_atendimento`; a decisão de "visita rápida gera ficha" (03/08) tornou 2 fichas no mesmo dia comum, não exceção — a 1ª levava todos os eventos, a 2ª ficava vazia | Troquei pra `ficha_id` (já existe em `odontograma_eventos`, zero query nova) | **Sim** |
| **Apaguei alertas de alergia + "Ver perfil" ao deletar `contexto-coluna.tsx`** | Migrei `ondeLabel`/`fmtData` mas esqueci esses 2 elementos, que o contrato dizia "MIGRAM, não somem" | Peguei antes de reportar pronto — botei um cabeçalho mínimo em `meu-dia-client.tsx` só com o que já existia (não o phead completo) | **Sim** |
| **Scroll só no Histórico, esqueci A fazer/Já feito/Nesta sessão** | Copiei o padrão `max-h-[420px] overflow-y-auto` só no primeiro bloco que escrevi | Ele testou com 14 pendências reais e a página esticou — apliquei o mesmo padrão nos 4 blocos | **Sim** |
| **Scrollbar comendo o texto** | `overflow-y-auto` sem respiro nenhum | `pr-2` nos 4 containers com scroll | **Sim** |
| **Lint bloqueou o reset de estado no `useEffect`** | `react-hooks/set-state-in-effect` — `setState` síncrono dentro de efeito | Troquei pro padrão que o React recomenda pra "resetar estado quando uma prop muda": comparar o id anterior durante o render, não em efeito | **Sim** |
| **"+dente não funciona" — mas era a ordem, não o popover** | `registrar()` descartava o procedimento escolhido se "onde" ainda não tinha sido marcado, só com um aviso depois | Testei o popover ao vivo (abre nos 2 temas) antes de aceitar a queixa como bug de popover; achei a causa real e resolvi com estado pendente + `handleOndeChange` completando em qualquer ordem | **Sim** |
| **Assumi que o artefato mostrava o estado "painel do dente aberto"** | Cliquei num dente no artefato servido e vi um grid `.splitdente` de 1057px+290px aparecer — achei que era o estado colapsado do cockpit | Medi a posição real: `top: 4451px`, uma seção estática desconectada, 4000+ px abaixo, sem handler ligado ao clique do cockpit. Não inventei a "faixa fina" sem medida — escondi a direita inteira e avisei ele | **Sim** (decisão registrada, não é bug — é ausência de design pra esse estado) |
| **Browser pane não compositava a sessão inteira** (screenshot falhava, `getBoundingClientRect` voltava zero, DOM duplicado aparecia e sumia entre chamadas) | Ambiente do sandbox, não o código | Rodei verificação por `outerHTML` (string match de classes/textos) e por `getComputedStyle` (que funcionou mesmo com rect zerado), cruzando com o log real do dev server (`preview_logs`) como fonte de verdade | **Contornado, não resolvido** — ainda não sei a causa raiz |

## O que eu estava cogitando

- **"Mais detalhado" no histórico** — perguntei a ele o que faltava aparecer e não veio
  resposta direta antes de "executa o bloco fechado". Fui pela leitura mínima seguinda:
  só tirar o corte de 160 chars da nota, sem inventar campo novo. Se ele queria mais
  (procedimentos sem cortar em 2, por exemplo), não perguntei de novo — fica pra próxima.
- **Se a trava 1 do §5.6 precisava de uma flag "acabei de salvar" separada** — cheguei a
  desenhar isso, mas percebi que `semRascunho` (`eventosDraft` vazio) já cobre o caso sozinho,
  porque `handleSalvo` limpa o rascunho de forma síncrona, sem esperar o `router.refresh()`.
  Mais simples, sem estado novo.
- **Se valia a pena reverter o Fila do ROADMAP pra 21** sem também atualizar o "Novos de
  03/08" da linha de topo — fiz os dois juntos pra não deixar o cabeçalho mentindo sobre o
  que mudou na sessão.

## Arquivos alterados

**Novos, meus, não commitados:**
| Arquivo | O quê |
|---|---|
| `src/app/dashboard/meu-dia/_components/cockpit-grid.tsx` | Grid de 3 colunas + colapso da direita (C3) |
| `src/app/dashboard/meu-dia/_components/bloco-moldavel.tsx` | Acordeão genérico (§5.1) |
| `src/app/dashboard/meu-dia/_components/historico-bloco.tsx` | Última visita, detalhada |
| `src/app/dashboard/meu-dia/_components/a-fazer-bloco.tsx` | 5 pendências mais antigas + "ver todas" |
| `src/app/dashboard/meu-dia/_components/ja-feito-bloco.tsx` | Acumulado clínico |
| `src/app/dashboard/meu-dia/_components/nesta-sessao-bloco.tsx` | Genérico — instanciado 2x (Concluídos hoje / Novos procedimentos) |
| `src/app/dashboard/meu-dia/_components/meu-dia-format.ts` | `fmtData`/`ondeLabel` compartilhados |

**Modificados, meus:**
`src/server/dashboard/get-meu-dia.ts` (C0 + fix `ficha_id`) · `meu-dia-client.tsx` (orquestra
tudo, `handleSalvo`, `colapsarDireita`) · `rail.tsx` (arrasto) · `registrar-painel.tsx`
(ordem livre, número no typeahead, CTA, split odontograma/painel) · `page.tsx`/`loading.tsx`/
`error.tsx` (`PageContainer`) · `ROADMAP.md` (R-51 a R-54, contadores).

**Apagado, meu:** `contexto-coluna.tsx`.

**Herdado de sessões anteriores, ainda não commitado (não toquei):** `plans/specs/R-46-meu-dia.md` ·
`plans/specs/R-46c-colar-do-word.md` · `src/hooks/useAudioRecorder.ts` ·
`src/hooks/useCapturaLivre.ts` · `src/lib/audio-mime.ts` (R-48) · specs/artefato do R-46
cockpit (sessão de 02/08).

**Zero commit nesta sessão** — são 9 sessões de working tree acumulado agora.
