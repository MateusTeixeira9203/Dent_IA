# Handoff — 2026-08-01 19:53

> **HANDOFF** · **R-46** (b, b2, g) + **R-48** (voz) · sessão #11, seguindo do handoff de
> 01/08 00:07 · modo **execução → planejamento**, alternando várias vezes na sessão
> Sessão longa e cheia de virada: o Meu dia salva de verdade e foi provado no banco; a
> "confusão" que ele relatou no início ("nada funciona") era quase toda ilusão de
> ferramenta, não bug do app; 3 decisões velhas foram fechadas; 5 commits organizados e
> parados a pedido dele; o destino do Meu dia virou **cockpit em tela cheia**; e a fatia que
> desbloqueia a voz do cockpit (R-48) foi planejada, implementada por workflow, pegou um
> **bug real que um teste ao vivo tinha dado como resolvido sem estar**, corrigido e
> confirmado no iPhone dele. Nada empurrado (push).

## O que trabalhamos

1. **Verificação ao vivo do R-46b/R-46b2** — ele relatou "nada funciona" (chips, +dente,
   salvar). Investiguei fundo com clique real (não `.click()`) e achei que **quase tudo já
   funcionava** — o "bug" era o pane do Claude Browser reportando `innerText` vazio numa aba
   sem foco, mesmo com conteúdo real renderizado. Achei e corrigi 2 bugs reais no caminho
   (rótulo cru do typeahead, "quadrante undefined" em boca-toda) e construí o R-46b2 do zero
   (salvar e chamar próximo), provado ponta a ponta contra o banco de produção.
2. **3 decisões que travavam o R-46** — R-46b A3 (fonte do typeahead), A4 ("fazer hoje"
   direto), R-46g A1 (gate de assinatura). As 3 fechadas nesta sessão.
3. **5 commits organizados e revisados** — dividiu o que tinha virado 30 arquivos soltos.
   Push explicitamente adiado a pedido dele.
4. **Direção nova: o Meu dia vira cockpit em tela cheia**, o que o modo consulta prometia.
   Layout de 3 zonas + upload de Word + voz em destaque. Corrigi a spec do R-46c no processo
   (ele queria **subir arquivo**, não colar texto — achei que a extração já existe e está
   instalada, zero construção nova nisso).
5. **R-48 (voz confiável)** — spec, workflow (implementar + 5 verificadores adversariais em
   paralelo), achado de um bug real que um teste ao vivo tinha dado como resolvido, fix,
   reconfirmação ao vivo, e confirmação final dele **no iPhone real**: "funcionou 100%".

## O que concluímos

**Pronto e verificado de verdade (ao vivo, ou no banco, ou no aparelho):**
- **R-46b2** — provado no banco: 1 clique gravou ficha `concluida`, o evento do
  odontograma, fechou o agendamento e criou a notificação (as 4 confirmadas por SQL, não
  pela tela), usando o slot de teste "Teste R-31a (apagar)" que ele mesmo tinha marcado
  como descartável.
- **R-46b** — G1 (typeahead filtra os 16 tipos + catálogo comercial), G4 ("fazer hoje"),
  G5 (lista reflete o rascunho em tempo real) confirmados clicando de verdade (eventos de
  ponteiro reais, não `.click()` — floating-ui não reage a `.click()` sintético).
- **R-48** — **7 de 7 gates**, incluindo G1 no iPhone dele. Ver §"Erros" abaixo — o caso
  mais importante da sessão pra entender COMO isso foi verificado.

**🟡 codado, não verificado ao vivo ainda:**
- R-46b — G2 (lote multi-dente), G3 (ToothDetailPanel preserva os outros dentes ao editar
  um — é o achado mais perigoso da spec original, nunca clicado), G6 (dark/light).
- R-46b2 — G5 (duplo clique = 1 ficha só), G6 (retry da RPC de eventos), G2 (ficha idêntica
  no perfil do paciente).
- R-46g — segue como estava, G1/G3/G5/G9-G13 sem clique.

**Pela metade:**
- **R-46c** — spec escrita, corrigida no meio (upload em vez de colar), ainda não voltou
  pra aprovação final — a §10 (onde a UI mora) depende do artefato do cockpit, que ainda
  não existe.
- **Inventário do Meu dia / artefato do cockpit** — não começado. Última coisa combinada
  antes de "vamos encerrar".

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| R-46b A3: busca única (16 tipos + catálogo), item do catálogo pede o tipo à mão | Auto-mapear categoria→tipo | Dado real checado: categoria "Geral" mistura tudo, "Prevenção" tem 2 tipos na mesma categoria — auto-mapear seria adivinhar em cima de prontuário |
| R-46b A4: "fazer hoje" insere direto, aceito como implementado | Voltar a abrir o painel antes de marcar feito | "Gostei bastante", dele — sem ressalva |
| R-46g A1: gate de assinatura **ignorado** — implementei e revertei no mesmo dia | Manter o gate implementado | "Ainda não temos nem um sistema de pagamento pra fazer esse controle" — `status_assinatura` não é hoje cobrado/atualizado por nada real |
| R-46c granularidade: 1 upload = 1 ficha com o histórico inteiro | 1 ficha por visita (fiel, mas N uploads) · fatiar por regex de data (erra formato) | Ele decidiu direto — prioriza menos gesto |
| R-46c D14 (secretária cola): adiada, vira item próprio | Construir junto | Achado na investigação: bloqueado em 2 camadas (`salvarFicha` barra role, rota redireciona) — não é ajuste de permissão, é superfície nova |
| R-46c mora no Meu dia **e** no perfil | Só um dos dois | Perfil resolve "trazer histórico"; Meu dia resolve "estou atendendo agora e preciso do contexto" |
| Cockpit: voz conserta **primeiro**, cockpit depois | Cockpit sem voz, voz entra depois · tudo num item só | Voz em destaque no topo do cockpit, quebrada, seria a peça mais visível em cima do pedaço comprovadamente quebrado |
| Cockpit: desktop primeiro em artefato, ele decide o tablet depois de abrir no iPad | Escolher arquitetura (fixa vs. colapsável) sem ver a tela | Ele mesmo adiou — "os dois, decido depois de ver" |
| R-48 `onError` dispara só na falha NO MEIO da gravação, nunca na largada (`getUserMedia`) | Chamar em ambos os casos | A falha na largada já é reportada completa pelo retorno síncrono de `startRecording`; chamar os dois geraria toast duplicado, e o callback não tem como saber qual mensagem usar sem um 3º parâmetro |

## Erros e como pensei em resolver

| Erro | Causa (ou hipótese) | Como resolvi | Resolvido? |
|---|---|---|---|
| Registrar-painel "não funciona" (relatado por ele) — combobox parecia não abrir, item escolhido não virava draft | `document.body.innerText` retorna string curta/vazia quando a aba não está em foco no pane do Claude Browser, MESMO com conteúdo real renderizado (confirmei: `textContent` no mesmo momento retornava o conteúdo completo) | Troquei toda verificação pra `textContent` + eventos de ponteiro reais (pointerdown/mousedown/pointerup/mouseup/click) em vez de `.click()` bruto (que não aciona `useClick` do floating-ui) | **Sim** — nenhum bug real ficou, só 2 pequenos (rótulo do typeahead, "quadrante undefined") |
| Dev server: `TypeError: assinaturaBloqueada is not a function` depois de eu remover a função do arquivo | HMR do webpack não invalidou o módulo compilado do `page.tsx` mesmo com o arquivo já correto no disco (confirmado por grep) — mesma classe de bug documentada no handoff anterior | Reiniciei o dev server do zero em vez de caçar bug em código que já estava certo | **Sim** |
| Primeiro commit nasceu com um "@" solto no assunto | Sintaxe de here-string do PowerShell (`@'...'@`) usada por engano numa ferramenta Bash | `git commit --amend -F -` com heredoc bash de verdade | **Sim** |
| **G6 do R-48 "passou" no teste ao vivo original, mas 2 revisores adversariais independentes discordaram** — leitura de código mostrou que `onstop` (não `onerror`) dispara quando a track do mic termina sozinha (por spec do MediaRecorder), e `onstop` não tinha o fallback que `onerror` tem pra entregar o áudio parcial. O teste ao vivo original tinha assumido, sem confirmar, que foi `onerror` que rodou | O agente que testou ao vivo não tinha como saber QUAL handler disparou — só inferiu pela existência de uma 2ª requisição de rede, num ambiente com MediaStream sintético e pane instável | Li o código eu mesmo, confirmei o diagnóstico dos revisores, apliquei o fix (consolidei `onstop`/`onerror` num `finalizar()` só, travado por `sessaoFinalizadaRef` pra contar só o primeiro dos dois que disparar — resolve também a corrida que o 2º revisor apontou: navegador pode disparar os dois eventos pra uma falha só), e reconfirmei ao vivo com **log temporário** (mesma técnica que já tinha resolvido a dúvida do combobox mais cedo): parei a track de verdade, vi `onstop` disparar (não `onerror`), `finalizar` recebeu 77905 bytes reais, toast certo apareceu, upload real foi feito, `200 OK`. Removi os logs depois | **Sim** — e esse é o achado mais importante da sessão: **um teste ao vivo que "passa" pode estar testando a coisa errada** quando não confirma qual caminho de código realmente executou |

## O que eu estava cogitando

- **Não reverter o gate do R-46g A1, só desligar por flag** — cogitei deixar o código
  presente mas condicional, pra não perder o trabalho se ele mudasse de ideia rápido.
  Descartei: ele foi direto ("pode ignorar"), e código morto atrás de flag é exatamente o
  tipo de coisa que confunde uma sessão futura sobre "isso está ativo ou não?". Revertei
  limpo e registrei a decisão na spec — reimplementar é rápido se precisar.
- **Extrair `podeAtender` pra um lugar mais compartilhado** — é a 6ª cópia do mesmo
  condicional literal no projeto (`month-view.tsx`, `agendamentos-client.tsx`, `day-view.tsx`,
  `paciente-detail-client.tsx`, e agora `rail.tsx`/`meu-dia-client.tsx`). Descartei fazer a
  extração ampla — fora do escopo do que foi pedido; só evitei criar uma 7ª cópia DENTRO da
  mesma feature (exportei de `rail.tsx`, importei em `meu-dia-client.tsx`). Se ele quiser
  consolidar as 6, é um item próprio, pequeno e sem risco.
- **Pedir pro workflow do R-48 usar Playwright em vez do Claude Browser**, dado o histórico
  de instabilidade do pane documentado em vários handoffs. Não fiz — deixei os agentes de
  teste ao vivo escolherem a ferramenta; funcionou (com contorno via MediaStream sintético),
  mas g6 mostra que confiar demais no resultado de UM teste ao vivo, mesmo com ferramenta
  melhor, ainda pode enganar se ele não confirma qual caminho de código rodou.
- **R-46c A2 (limite de 5000 chars pra upload grande)** — virou aberta explícita na
  correção da spec, não resolvi sozinho. Fica esperando o artefato do cockpit definir onde
  o aviso de "arquivo grande demais" aparece na tela.

## Arquivos alterados

**Commitados (5 commits, main local, não empurrado):**
`chore(ui)` popover/combobox + `Odontograma.tsx` (numeração decídua exportada) ·
`fix(fichas)` `tooth-group-list.tsx` (boca toda) · `refactor(fichas)` `derivar-campos-legado.ts`
extraído de `FichasTab.tsx` · `feat(dashboard)` 16 arquivos do Meu dia (g+b+b2 — `page.tsx`,
`rail.tsx`, `meu-dia-client.tsx`, `contexto-coluna.tsx`, `registrar-painel.tsx`,
`onde-seletor.tsx`, `fdi-popover.tsx`, `actions.ts`, `error.tsx`, `loading.tsx`,
`abrir-meu-dia-button.tsx`, `get-meu-dia.ts` etc.) · `docs(plans)` specs R-46g/b/b2/c, R-28
Parte 3, discussão de atrito, auditoria.

**Não commitado (working tree agora):**
| Arquivo | O quê |
|---|---|
| `src/lib/audio-mime.ts` | Novo — `extensaoDoMime()`, puro |
| `src/hooks/useAudioRecorder.ts` | Reescrito — mimeType negociado, `startRecording` tipado, `resetError`, fix do I3 (`finalizar()` consolidado) |
| `src/hooks/useCapturaLivre.ts` | Nome do upload pelo mimeType real, retry sem perder texto, mensagens por motivo |
| `plans/specs/R-46c-colar-do-word.md` | Correção: upload de arquivo, não colar texto |
| `plans/ESTADO.md`, `plans/ROADMAP.md` | Reescritos nesta sessão (mais uma vez, no fechamento) |

**Arquivado nesta sessão:** `plans/_arquivo/specs/R-48-voz-confiavel.md` (nunca foi
commitado no lugar original — só existiu em `plans/specs/`, movido direto).
