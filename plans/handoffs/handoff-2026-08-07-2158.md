# Handoff — 2026-08-07 21:58

> **HANDOFF** · R-73 · R-74 · R-72 · R-75 · sessão #27 · modo **planejamento → execução**
> (começou revisando 2 documentos, virou debug ao vivo quando ele reportou bugs reais, terminou
> matando `/consulta` de vez) · **encerrada por ele** ("antes de finalizarmos, o que está
> faltando" → resumo → "pode rodar o handoff")

## O que trabalhamos

1. **Planejamento inicial**: li o handoff de 02:30 e a auditoria pré-produção de 07/08, achei 2
   discrepâncias antes de propor qualquer coisa — a migration 072 (backfill do R-64) **não
   estava faltando**, o arquivo sempre existiu e já estava aplicada em produção (conferido por
   SQL direto); e o achado #2 da auditoria (janela de hora fixa 7h-20h) só cobria metade do bug
   real (também afeta depois das 20h, e `week-view.tsx` além de `day-view.tsx`). Também
   reescopei "matar `/consulta`" — não é exclusão direta, o próprio Meu dia importava 2
   arquivos de dentro da pasta da rota.
2. **Bugs reais reportados ao vivo por ele** (interrompeu o planejamento): "+Encaixe" não
   funcionava (nem em prod nem local), Dex marcava documento importado inteiro como "realizado"
   sem confirmação, e ele pediu um jeito de trocar o status do atendimento manualmente.
3. **R-75 — Dex marcando "realizado" em documento importado.** Achei a causa lendo o prompt de
   `/api/dex/formatar-evolucao`: a regra "verbo no passado = feito" não distinguia dictado ao
   vivo de documento histórico colado. Fix: liga `modo:'exame_inicial'` (existia morto no
   código, nunca usado) só quando o texto vem de upload de **documento** (nunca áudio — audio
   continua "consulta"), e o prompt ganha regra condicional nesse modo. Eval rodado antes
   (14/16 atual, caso novo FALHOU como esperado) e depois (15/16 atual, 0 inventados, os 2
   casos novos passaram) — regra não-negociável do projeto pra mudar prompt de extração
   clínica.
4. **R-73 — Rail do Meu dia engolindo clique.** Saga de 3 hipóteses até achar a causa real —
   ver tabela de erros abaixo, é o item com mais raciocínio da sessão.
5. **R-74 — trocar status manualmente.** Seletor no card selecionado do Rail, reusa
   `atualizarStatusAgendamento` que a Agenda já usa.
6. **R-72 — matar `/consulta` de vez.** Fase 1 (mover `actions.ts`→
   `server/patients/registro-actions.ts`, `tooth-group-list.tsx`→
   `components/odontograma/`) + Fase 2 (apagar a rota inteira, redirecionar 6 botões
   "Iniciar consulta" pro Meu dia, tirar o passo "demo" do onboarding).

## O que concluímos

- **R-73 e R-74 — fechados, testados ao vivo por ele, commitados e no ar** (`6edc60f`).
- **R-72 — fechado, testado ao vivo por ele, commitado e no ar** (`9e13626`).
- **R-75 — codado e verificado por eval, mas NÃO testado no upload real da UI e NÃO
  commitado.** Ele adiou o teste de propósito ("o upload de documento eu testo dps") — fica
  🟡, não ✅, até ele confirmar.
- **R-46h e R-71 não foram tocados** — ficaram pra trás quando o foco mudou pro debug ao vivo.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| R-75: `modo:'exame_inicial'` liga só quando o texto vem de **upload de documento**, nunca de áudio | Aplicar a regra "só marca realizado com confirmação explícita" pro relato inteiro, incluindo dictado ao vivo (pedido literal dele) | Quebraria o caminho principal do produto — dentista fala "fiz uma resina" sem nunca dizer a palavra "concluído". Expliquei o risco, ele aceitou o escopo mais estreito |
| R-73: alvo do clique resolvido no `pointerdown`, ação disparada no `pointerup` por esse alvo guardado — não pelo `click` nativo | Só ajustar o limiar de distância/tempo do arraste (2 primeiras tentativas) | O `click` nativo mira em quem estiver na coordenada de tela no momento do `pointerup`, não em quem estava lá no `pointerdown` — se o rail reflui no meio do gesto, erra o alvo sem erro nenhum. Só descobri isso instrumentando os eventos brutos depois que os 2 primeiros fixes não resolveram |
| Rail do Meu dia perde o próprio link "Iniciar consulta"/"Continuar atendimento" | Redirecionar esse link pro Meu dia também (ficaria circular — já está lá) | Selecionar o slot já é a ação inteira agora; ele mesmo comentou durante a sessão que "iniciar consulta nem precisa" |
| Onboarding: passo "demo" **removido** (opção 1 de 3 que propus) | Reconstruir a demonstração em cima do Meu dia (opção 2) ou manter `/consulta/demo` viva só pra isso (opção 3) | Reconstruir é feature nova (Meu dia não tem modo demo/fake data), fora do escopo de "aposentar `/consulta`"; ele confirmou a opção mais simples |
| Onboarding: etapa `'aha'` do wizard removida por inteiro, não só o botão da demo | Deixar a etapa existindo só com "pular por agora" | Confirmei no código que `onSubmitIdentidade` **nunca** chama `setStep('aha')` — a etapa já estava inalcançável desde uma Fase 1 anterior (achado, não decisão minha) |
| Commit do R-72 (e antes, do R-73/R-74) separado do R-75, mesmo com 3 arquivos misturando os dois | Commitar tudo junto já que estava tudo no mesmo lote de trabalho | R-75 não foi testado por ele ainda — CLAUDE.md proíbe subir código não verificado. Usei revert-temporário→commit→restore pra separar sem `git add -p` (Bash não suporta modo interativo) |

## Erros e como pensei em resolver

| Erro | Causa/hipótese | Como resolvi (ou ia resolver) | Resolvido? |
|---|---|---|---|
| "+Encaixe" e trocar de paciente não respondiam no Rail | **3 hipóteses em sequência**: (1) dev server com bundle dessincronizado (Fast Refresh) — descartada, ele confirmou que falhava em prod E local; (2) ref de arrasto nunca zerava depois de clique normal, ficava presa em `moveu:true` e suprimia todo clique seguinte — **real, corrigida**; (3) limiar de 5px sozinho classificava tremor de clique como arrasto — **real, corrigida** (exige também tempo mínimo). Nenhuma das duas resolveu meu teste automatizado | Instrumentei os eventos brutos (`pointerdown`/`pointerup`/`click`) e achei a causa real: o alvo mudava de elemento entre pressionar e soltar (layout do rail se reorganizando por baixo do cursor). Redesenhei pra resolver o alvo no `pointerdown` e disparar no `pointerup` por esse alvo guardado | Sim — confirmado por ele com a mão, várias vezes, inclusive depois de arrastar o rail de propósito |
| Meu fix não aparecia pra ele mesmo depois de eu editar o arquivo | Descobri que `dentia.app.br` **não é servido pelo processo que eu controlo** — parei meu próprio dev server via `preview_stop` e o site continuou no ar normal. É um processo separado (dele, em outro terminal) | Pedi pra ele reiniciar o dev server manualmente (Ctrl+C + `npm run dev`) — funcionou | Sim, depois do restart dele |
| Depois de apagar `/consulta`, `tsc --noEmit` mostrava erros em arquivos que eu nunca toquei | `.next/types/` (cache de rotas do Next, auto-gerado) ainda referenciava as páginas apagadas — `tsc` sozinho não regenera isso, só o processo do Next rodando de novo | Apaguei `.next` inteiro (gitignored, build cache) e reiniciei meu servidor controlado pra regenerar limpo | Sim |
| `podeAtender()` em `rail.tsx` tinha comentário dizendo que `meu-dia-client.tsx` a usava | Comentário desatualizado — grep no projeto inteiro achou zero uso fora da própria definição, mesmo antes de eu remover o link que a chamava | Removi a função junto com o link que a usava (ficou órfã pelo meu próprio edit) — confirmado por grep, não só por typecheck (export não-usado não dá erro de compilador) | Sim |

## O que eu estava cogitando

- **`DexGuide`/`dex-mascot.tsx`** (mascote que aponta "clique no botão que está brilhando" —
  a demo que acabei de tirar) — descobri que já estava **desativado desde uma Fase 1 anterior**
  (comentado em `dashboard-shell.tsx`, nunca monta). Cogitei limpar junto já que ficou duplamente
  morto, mas decidi deixar como débito pré-existente — não referenciava `/consulta` diretamente,
  fora do escopo de hoje. Fica candidato a um item pequeno futuro.
- **As etapas `'plano'`/`'procedimentos'`/`'sucesso'` do onboarding** também ficaram
  praticamente inalcançáveis depois que tirei a demo (a única entrada em `'plano'` era via
  `?step=plano`, que só a demo linkava de volta) — mas `PLANOS_CONFIG` (preços SOLO/CLINICA)
  parecia conteúdo genuinamente separado (seleção de plano), não só resíduo da demo. Não toquei
  por incerteza — vale ele confirmar se essas etapas ainda servem pra alguma coisa antes de
  alguém mexer.
- **"Salvar e marcar como concluído"** — ele comentou durante o debug que talvez nem precise do
  seletor de status separado, um botão "Salvar e concluir" direto no Registrar seria mais
  simples. Não construí — o R-74 (seletor manual) já resolve o pedido literal, e não quis
  empilhar um 2º mecanismo sem confirmação. Fica anotado pra ele decidir se quer especificar.
- **ROADMAP.md em 216 linhas** (teto ~200) — já estava estourado antes desta sessão (212 linhas
  no início), meus 4 itens novos (R-72 a R-75) empurraram mais um pouco. Não fiz trim agora —
  reorganizar direito significa mandar detalhe pra spec, trabalho de conteúdo que não cabe
  apressado no fechamento. Fica sinalizado, não é a primeira vez que aparece.

## Arquivos alterados

**Commitados e no ar** (`6edc60f`, `9e13626`):
`src/app/dashboard/meu-dia/_components/rail.tsx` (reescrito — clique + status manual) ·
`src/app/dashboard/meu-dia/_components/meu-dia-client.tsx` (parcial — R-72/R-74) ·
`src/server/patients/registro-actions.ts` (novo, movido de `app/consulta/`) ·
`src/components/odontograma/tooth-group-list.tsx` (novo, movido) ·
9 arquivos apagados em `app/consulta/` (rota inteira) ·
`src/components/consulta/modo-consulta-loader.tsx` (apagado, órfão) ·
`src/app/dashboard/agendamentos/_components/agendamentos-client.tsx`,
`atender-agora-modal.tsx`, `assinatura-actions.ts` ·
`src/app/dashboard/pacientes/[id]/_components/paciente-detail-client.tsx` ·
`src/app/onboarding/_components/onboarding-client.tsx` ·
`src/components/dashboard/abrir-meu-dia-button.tsx`, `primeiros-passos-card.tsx` ·
`src/components/layout/dex-widget.tsx` ·
`src/components/pacientes/FichasTab.tsx` ·
`src/lib/onboarding-progress.ts` ·
`src/server/patients/salvar-ficha.ts` (comentário) ·
`src/app/dashboard/meu-dia/_components/registrar-painel.tsx` (parcial — R-72) ·
`src/app/dashboard/meu-dia/_components/nesta-sessao-bloco.tsx`

**Não commitados** (R-75, aguardando teste dele):
`src/app/api/dex/formatar-evolucao/route.ts` · `evals/extracao-clinica/golden.json` ·
`src/lib/dex/extrair-texto-arquivo.ts` · `src/components/fichas/captura-livre-card.tsx` ·
`src/app/dashboard/meu-dia/_components/anexar-documentos-bloco.tsx`,
`campo-magico-meu-dia.tsx` (só o tipo) · trechos de `meu-dia-client.tsx` e
`registrar-painel.tsx` (o resto do que essas 2 partiram entre R-72/R-75)

**Planejamento** (não commitado): `plans/ESTADO.md`, `plans/ROADMAP.md`,
`plans/specs/R-46h-orcamento-no-meu-dia.md` (de sessão anterior).
