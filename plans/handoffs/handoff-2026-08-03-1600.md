# Handoff — 2026-08-03 16:00

> **HANDOFF** · **R-46** (fatia cockpit) · sessão #14, seguindo do handoff de 03/08 01:54
> · modo **execução** o dia inteiro, com 2 pausas curtas de planejamento no meio (R-55, R-46c)
> Sessão mais longa da semana: testei C2/C3 ao vivo (pendência da sessão anterior), achei e
> corrigi 2 bugs reais no Registrar, investiguei e consertei um bug de perda de dado real em
> produção (R-55), fechei C5, e implementei o R-46c inteiro (colar histórico do Word) —
> migration incluída. **9 commits, nada empurrado.** working tree limpo no fim.

## O que trabalhamos

1. **C2/C3 testados ao vivo** — a pendência que abriu a sessão. C2 (rail + CTA + travas)
   provado com escrita real no banco (ficha criada 1×, CTA trava em "já registrado hoje").
   C3 (colapso do painel do dente) confirmado por medição: dente mede 31,45px com colapso,
   contra os 22,8px que reprovariam WCAG 2.2 — a justificativa original do contrato não bateu
   com a build atual, mas o comportamento em si funciona.
2. **`/pontual` do Registrar** — dois bugs achados testando ao vivo: Combobox não selecionava
   com Enter (faltava `autoHighlight`, comportamento default do base-ui) · profilaxia/
   clareamento/flúor/exame periodontal exigiam o chip "onde" que não existe pra esses 4 tipos
   (`odontograma.ts` já documentava isso em comentário — só não virava código). Os outros 13
   tipos continuam exigindo o chip — resolvem no máximo o NÍVEL da âncora, nunca o valor.
3. **R-55 — histórico e "Já feito" perdiam procedimento repetido.** Achado numa investigação
   provocada pela dele: `chaveAncora`/`vencedorPorAncora` em `get-meu-dia.ts` serve bem a
   pergunta "esta âncora está pendente?" e mal a "o que já foi feito?" — aplicada ao histórico,
   colapsava ocorrência repetida (medido no banco: 4 colisões reais, uma com datas diferentes
   — endodontia do mesmo dente em 21/07 e 23/07 virava 1 evento só). Corrigido separando as
   duas leituras: a chave/reduce ficam intocados servindo só à pendência; histórico e "Já
   feito" passam a ler todo evento realizado, sem dedup, agrupando só como apresentação.
   Emendei a trava do R-46-cockpit.md que dizia "o algoritmo não muda" — agora diz "não muda
   pro lado da pendência".
4. **C5 — múltipla seleção no odontograma.** O defeito real: `computeToothState` sempre
   devolvia `'default'` quando o dente tinha resumo clínico, então dente com evento NUNCA
   mostrava seleção — havia até um ternário defensivo em `FichasTab.tsx` escondendo isso em
   vez de resolver. Corrigido aditivamente: `ToothSVG` ganhou um anel independente de
   `state`/`clinico`, `registrar-painel.tsx` passou a ler/escrever o mesmo `onde` que os
   chips já usam (zero estado novo).
5. **R-46c — colar histórico do Word.** Achei que a spec já existente (`R-46c-colar-do-word.md`)
   estava desatualizada (referenciava `contexto-coluna.tsx`, apagado no cockpit) E que o
   `ESTADO.md` tinha etiquetado errado o escopo — ele descrevia "upload + organizar com IA"
   como pendente de aprovação, mas a spec real é explicitamente "nível 1, sem IA" (D7); o
   "organizar com IA" é um item **diferente**, nunca especificado, que o roadmap já reservava
   como R-46d. Emendei a spec pros arquivos reais, apliquei a migration (`fichas_origem_check`
   ganha `'importado'`), implementei o wrapper + dialog + os 3 pontos de exibição honesta
   (timeline do paciente, PDF, Meu dia — nenhum pode se apresentar como atendimento real).

## O que concluímos

**Codado, testado ao vivo com prova real (banco/PDF/tela), commitado:**
- C2/C3 do cockpit — testados, não modificados (já estavam corretos).
- `/pontual` Registrar (Enter + âncora boca) — testado ao vivo, funciona.
- R-55 (dedup) — testado ao vivo: registrei uma 2ª profilaxia de verdade, banco confirma 2
  linhas, tela mostra "2×" em vez de sumir uma. Histórico expandido mostra as 2 visitas certas.
- C5 (seleção múltipla) — testado ao vivo: dente com evento mostra anel, 2º toque com 1 aceso
  abre painel, com 2+ só remove do lote, região limpa a seleção de dente.
- R-46c (colar do Word) — testado ao vivo: importei histórico com data retroativa (2023),
  banco confirma origem/status/data/texto exatos, nenhum agendamento tocado, nenhuma
  notificação criada, PDF mostra o badge certo, timeline rotula certo.
- R-48 (voz confiável) — **não fiz eu, e não retestei.** Achei ele pronto e sem commit numa
  sessão anterior (01/08), commitei separado. Fica 🟡 até alguém testar no aparelho de novo.

**🟡 Nada disso subiu — está tudo em localhost, working tree limpo mas sem push.**

**Achado, registrado, não resolvido:**
- **Dois surfaces a mais** também mostram resumo de ficha sem checar `origem` —
  `fichasRecentes` (widget no resumo do paciente, `paciente-detail-client.tsx`) e a própria
  lista de fichas do `FichasTab.tsx`. A spec do R-46c só mapeou timeline+PDF+Meu dia; não
  expandi sozinho.
- **C4 (cockpit) virou dialog modal, não "tela cheia"** — o contrato do cockpit definia C4
  como container em tela cheia dentro da coluna central; o que constrói (seguindo a spec do
  R-46c, que já desenhava assim desde 01/08) é um modal por cima da tela. Só percebi a
  divergência quando ele perguntou sobre o campo mágico completo — deveria ter checado C4
  contra R-46c antes de codar, não depois.
- **Campo mágico é 1 componente único**, não "R-46c faz upload, R-46d organiza em cima" — ele
  foi explícito nisso no fim da sessão. `CapturaLivreCard` (perfil) já faz arquivo+voz+
  organizar; o `ColarDoWordDialog` que construí hoje é mais estreito e pode ficar redundante
  quando o R-46d for especificado. Registrado em memory (`project_campo_magico_unico.md`).

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Enter no Registrar via `autoHighlight` | Reescrever a lógica de seleção do Combobox | Já é o comportamento nativo do base-ui pra esse caso — 1 prop resolve |
| Boca/flúor/clareamento/exame perio ganham âncora implícita, ignoram chip existente | Mapear os 17 tipos (achado no workflow: só 4 têm âncora 100% determinada, os outros resolvem só o nível) | Automação segura só onde a resposta é única; os outros continuam exigindo o chip |
| R-55: separar as 2 leituras (chave/reduce intocados pra pendência, 2ª passagem sem dedup pra histórico) | Enriquecer a chave com `id`/`ficha_id` | Workflow: isso quebraria a pendência (evento `realizado` deixaria de fechar o `indicado` que ele resolve) — já ocorreu antes, documentado no próprio arquivo |
| "Já feito" recebe só o corte mínimo (1 linha + `n×`) | Desenhar a versão completa (sub-lista de ocorrências) | Ele: bloco vai sair no C6, não vale desenhar o que vai morrer |
| C5: anel aditivo, independente de `state`/`clinico` | Mudar `computeToothState` pra considerar seleção também no caso clínico | Aditivo não toca `crownFill`/`crownStroke` — zero risco nos outros 2 consumidores do componente |
| R-46c: só nível 1 (sem IA), corrigindo a spec existente | Escrever R-46d (organizar com IA) junto | Ele escolheu explicitamente depois que mostrei a divergência ESTADO×spec×roadmap |
| Botão "Colar do Word" no Meu dia mora no estado vazio do Histórico (`visitas.length === 0`) | Recriar a condição antiga (`semNadaAinda`, misturada com pendência/orto) | O cockpit já separou os blocos — a condição limpa é um efeito colateral bom do redesign |
| Commit dividido em 5 blocos (cockpit / R-55 / pontual / R-48 / docs), reconstruindo estado "antes do fix" pra 4 arquivos | 1 commit grande com tudo junto | Regra do projeto: fix não mistura com feature. Tive o conteúdo exato de antes de cada edição (já tinha lido os arquivos inteiros), então reconstruir foi seguro |
| Migration commitada sozinha, antes do código que depende dela | Junto com o commit de feature | Regra do projeto: migration vai primeiro e sozinha |

## Erros e como pensei em resolver

| Erro | Causa | Como resolvi | Resolvido? |
|---|---|---|---|
| **Primeiro commit saiu com 18 arquivos em vez de 14** | 3 renomeações de handoff já estavam staged no index de uma sessão anterior; `git commit` pegou tudo que tinha no index, não só o que eu tinha adicionado com `git add` | `git reset --soft HEAD~1`, `git restore --staged plans/`, recommitei só os 14 | **Sim** |
| **`jaFeito` crashava com "reading 'split' on undefined"** logo depois de codar o R-55 | Achei que era o pane ocluso (bug já documentado). Testei a lógica isolada em Node contra dado real do banco — funcionava perfeita, inclusive o caso alvo (endodontia repetida agrupando certo). O crash persistiu mesmo depois de reload/touch nos arquivos | Perguntei se o dev server era dele numa outra sessão — era. Matei o processo (PID via `Get-NetTCPConnection`) e subi um novo sob esta sessão; o crash sumiu na hora | **Sim**, mas a causa raiz exata (por que o servidor antigo ficou preso) não foi diagnosticada — só contornada |
| **`ESTADO.md` etiquetou "R-46c" com escopo que pertence ao R-46d** | Alguém (sessão anterior) escreveu "campo mágico completo, upload + organizar" resumindo o R-46c, mas a spec real (D7) é explicitamente sem IA — o roadmap já separava os dois itens em linhas diferentes | Investiguei antes de codar (achei o mecanismo `CapturaLivreCard`/`formatar-evolucao` já existente), mostrei a divergência, ele escolheu o escopo certo | **Sim** |
| **C4 (contrato do cockpit) pedia "tela cheia", constrói dialog modal** | Segui a spec do R-46c (que já desenhava `ColarDoWordDialog` desde 01/08) sem checar contra a definição de C4 no contrato do cockpit — os 2 documentos descreviam formas diferentes pro mesmo botão e eu não cruzei os dois antes de codar | Não resolvido nesta sessão — só descoberto quando ele perguntou sobre o campo mágico completo, no fim | **Não** — fica registrado como pendência real, não só nota |
| **Achei 2 surfaces a mais** (`fichasRecentes`, lista do `FichasTab`) que também vazam "Evolução"/dentista sem checar `origem`, testando o G2 do R-46c | A spec original (01/08) só investigou 1 mecanismo (a timeline) — não foi um audit exaustivo de tudo que lista ficha | Registrei no commit e no handoff, não expandi sozinho | **Não resolvido, registrado** |

## O que eu estava cogitando

- **Se valia testar G4/G5/G6 do R-46c ao vivo** (ordenação, data futura, limite de caracteres)
  ou confiar em code review. Fui por code review — são mecanismos simples (input `max`, Zod
  `max(5000)`, comparação de string de data) e já tinha feito bastante teste ao vivo nos
  caminhos de maior risco (escrita no banco, efeitos colaterais, honestidade do prontuário).
- **Se deveria consertar `fichasRecentes`/`FichasTab` list agora que achei o problema.** Decidi
  que não — a spec não pedia, e expandir escopo sozinho no meio da execução é exatamente o
  que a regra 2 do projeto proíbe. Registrei em vez de agir.
- **Se o `ColarDoWordDialog` deveria virar container em tela cheia agora**, já que achei a
  divergência com C4. Não decidi sozinho — perguntei, ele disse que o campo mágico é maior
  que isso (arquivo+voz+organizar juntos), o que muda o problema: não é "dialog vira tela
  cheia", é "será que o `CapturaLivreCard` migra pro Meu dia inteiro". Isso é pergunta de
  arquitetura pro R-46d, não uma correção de forma no que já existe.

## Arquivos alterados

**9 commits, working tree limpo:**
```
69e1754 docs(plans): emenda a spec do R-46c pro cockpit real
4044041 feat(pacientes): colar histórico do Word — R-46c
f5914d7 feat(db): fichas aceita origem='importado' — migration 127
81f5e71 feat(odontograma): múltipla seleção de dente com anel de contorno — C5
2c76c9e docs(plans): specs do R-46 cockpit, R-49, R-55, roadmap e estado da sessão
282e32f fix(voz): mic iOS não grava, retry perdido, falha silenciosa no meio do ditado — R-48
ced82f2 fix(dashboard): Registrar não selecionava com Enter e exigia "onde" pra boca toda
886731c fix(dashboard): histórico e "Já feito" perdiam procedimento repetido
0669503 feat(dashboard): cockpit do Meu dia em tela cheia (C0-C3)
```

**Novos:** `src/server/patients/importar-historico.ts` · `src/components/pacientes/
colar-do-word-dialog.tsx` · `supabase/migrations/20260803190000_127_fichas_origem_importado.sql`
· `plans/specs/R-55-historico-sem-perda-de-dado.md`.

**Migration aplicada e conferida no schema real** (não só na tabela de controle):
`fichas_origem_check` aceita `'importado'`.

**Memory novo:** `project_campo_magico_unico.md` (visão do campo mágico único, pra não se
perder antes do R-46d) · addendum em `project_qa_playwright_harness.md` (dev server travado
de outra sessão — sintoma idêntico ao pane ocluso, causa diferente).
