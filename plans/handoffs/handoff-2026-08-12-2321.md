# Handoff — 2026-08-12 23:21

> **HANDOFF** · **R-103b + R-103c** · sessão #38 (continuação) · modo pontual → planejamento →
> execução → planejamento → execução

<!-- Este arquivo é imutável: é o log do raciocínio da sessão.
     A situação ATUAL (o que falta, o que trava) mora no plans/ESTADO.md, não aqui. -->

## O que trabalhamos

1. **Pontual: plural do matcher local do campo mágico** — achado da sessão anterior, corrigido
   agora. `casaLabel` em `casar-procedimento-local.ts` cobre os 2 padrões reais (-ão→-ões,
   -al→-ais). 28/28 no harness Node.
2. **R-106 aberto** — status realizado/indicado errado na extração por IA virou item formal no
   ROADMAP, sem investigação ainda (exige eval antes de mexer no prompt, regra do CLAUDE.md).
3. **R-103b planejado e codado** — as 6 abertas do §4 do master respondidas por ele; ao formalizar
   A1, ele corrigiu um furo real (ver Decisões). Rota `/api/dex/retencao`, `classificarRetencao`
   (função pura), fiação no hub. 12/12 fixtures, testado ao vivo.
4. **R-103c planejado e codado** — ele cortou a métrica de "recorrentes" no meio do planejamento,
   por decisão dele (sem definição confiável hoje). Rota `/api/dex/mes`, `calcularNumerosMes`,
   coluna "Hoje e a semana" → "O mês", limpeza de 4 queries órfãs em `context.ts`. 7/7 fixtures,
   testado ao vivo com dado real (Império).
5. **G3 do R-103b/c não fechou** — investiguei o banco (Supabase MCP, só leitura), descobri que
   não existe dado nem conta pra testar isolamento entre 2 dentistas hoje. Propus inserir dado
   sintético; ele recusou — prefere dado real com o tempo.

## O que concluímos

- ✅ **Plural do matcher local** — codado, verificado (28/28 harness Node, typecheck limpo).
  **Não commitado.**
- 📌 **R-106** — item aberto no ROADMAP, zero código escrito, zero investigação.
- 🟡 **R-103b — codado, não commitado.** Typecheck/lint/build limpos, 12/12 fixtures. Testado ao
  vivo em Teste01: hub abre sem erro, rota responde 200 — mas sem dado nos 3 buckets lá, então
  nenhum card real foi visto na tela. **G3/G4/G5/G6/G8 pendentes** — todos exigem login que só
  ele tem (2 dentistas, secretária, protético) ou ferramenta que não usei (design-review).
- 🟡 **R-103c — codado, não commitado.** Typecheck/lint/build limpos, 7/7 fixtures. Testado ao
  vivo em Império **com dado real**: 6 atendimentos, 1,5 visitas/paciente, +200% de crescimento —
  a prova mais forte desta sessão, porque não foi harness isolado, foi a rota de verdade batendo
  no banco de verdade através do client autenticado (RLS ligada). Mesmos gates pendentes do b.
- ❌ **G3 (2 contas) não fechou.** Nem com dado sintético — a tentativa de inserir foi bloqueada
  pelo classificador de permissão do próprio Claude Code, e a proposta de ele mesmo rodar o SQL
  foi recusada. Fica registrado como bloqueio real, não como "esqueci de fazer".

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| A1 do R-103b corrigida: janela de "cancelou e não remarcou" inclui consulta **futura** cancelada, sem teto | Medir os 30 dias só no passado (redação original da proposta) | **Ele pegou o furo**, não eu: "um paciente que ia vir amanhã, cancelou e não remarcou — ele entra nessa aba". `agendamentos` não tem coluna de "quando cancelou", só `data_hora`+`status` — a leitura literal deixava esse paciente sem card nenhum |
| "Agendamento futuro" (D6, R-103b) = futuro **e não-cancelado**, nos 3 tipos | Ler literal ("nenhum agendamento futuro") | Confirmado por ele: cancelamento futuro não é promessa de volta |
| R-103b: classificação em TypeScript puro, não RPC/SQL | View ou RPC `SECURITY DEFINER` | A6 fechou escopo "meu" — RLS de `agendamentos` já cobre, `fichas` só precisa de filtro explícito na query. Zero motivo pra RPC nova |
| R-103b/c: rota nova (`/api/dex/retencao`, `/api/dex/mes`) em vez de estender `context`/`alerts` | Enfiar mais queries nas rotas existentes | Natureza diferente — linhas cruas pra classificar/agregar, não contagem. `context` já tinha 13 queries paralelas |
| R-103c: **cortar** a métrica de "recorrentes" (D2) | Escolher 1 das 3 definições propostas pelo planner | **Decisão dele**, não minha: sem conceito de recorrência em nenhum lugar do código hoje, nenhuma das 3 leituras tinha um "óbvio certo". Efeito colateral bom: matou o 2º estágio de query que só existia por causa dela — spec ficou mais simples, não só menor |
| G3: não inserir dado sintético mesmo com script pronto | Rodar eu mesmo via Supabase MCP, ou insistir que ele rodasse | Classificador de permissão bloqueou a 1ª tentativa; ofereci o script pra ele rodar; ele recusou as duas formas — **prefere esperar dado real**. Respeitei sem insistir |

## Erros e como pensei em resolver

| Erro | Causa (ou hipótese) | Como pensei em resolver | Resolvido? |
|---|---|---|---|
| INSERT de dado de teste no Supabase bloqueado pelo classificador de auto-mode, mesmo com "pode inserir" dito no chat | Confirmação em chat e permissão de execução de tool são camadas diferentes — a 2ª é automática e não le ê o histórico da conversa | Não tentei contornar (instrução explícita de não burlar). Expliquei o bloqueio pro usuário e ofereci o script SQL pronto pra ele rodar manualmente | Sim, mas o resultado final é que G3 continua aberto — a "resolução" foi processual (dei o caminho), não técnica |
| `read_console_messages` mostrou erro de "hooks order" em `DexWidget` logo depois de editar `useDexHub.ts` (adicionar `useState` novo) | Fast Refresh do dev server compara a árvore de hooks entre a versão antiga e nova do componente em pleno HMR — artefato conhecido, não bug real (mesma classe do que a sessão anterior já tinha documentado com o servidor compartilhado) | Abri uma aba nova e limpa (sem histórico de HMR) e recarreguei — zero erro. Confirmei que era artefato, não regressão | Sim |
| Relatei "219 linhas" pro `ROADMAP.md` de cabeça, sem contar de verdade | Assumi um número aproximado em vez de rodar `wc`/`awk` | Rodei `awk 'END{print NR}'` e `wc -l` pra conferir — achei 254, corrigi no `ESTADO.md` antes de deixar o número errado se espalhar | Sim |

## O que eu estava cogitando

- **Se dava pra fechar G3 parcialmente sem 2ª conta** — cheguei a considerar usar a sessão já
  autenticada como "teste" e só inserir dado exclusivo de um 2º dentista no banco, checando que
  não vaza pro lado de "teste". Tecnicamente prova metade do invariante (A não vaza B), mas ainda
  dependia do INSERT que foi recusado — não cheguei a fazer nem essa versão parcial.
- **Ordem dos pacientes dentro de cada bucket de retenção** (`retencao.ts`) — a spec não
  especifica, então escolhi `diasAtras` decrescente (evento mais antigo primeiro), seguindo o
  mesmo padrão que `orcamentosAtrasados` já usa no `context.ts` (mais estagnado primeiro). É
  julgamento meu, não requisito — se ele achar estranho na hora de ver o card de verdade, é 1
  linha pra inverter.
- **Se valia testar G3 no Império em vez de criar 2º dentista no Teste01** — Império já tem 3
  dentistas reais (inclusive um chamado "teste ", diferente do "teste" de Teste01); Teste01 só
  tem 1. Optei por propor o seed no Império por já ter a infraestrutura de contas — descartado
  de qualquer forma pela recusa dele.

## Arquivos alterados

| Arquivo / grupo | Mudança |
|---|---|
| `src/lib/odontograma/casar-procedimento-local.ts` + `.test.ts` | Pontual — `casaLabel` (plural -ão→-ões, -al→-ais), 7 testes novos |
| `plans/specs/R-103b-pendencias-do-dex.md` | **Novo** — spec do R-103b, D9 acrescentada (correção do A1) |
| `src/lib/dex/tipos.ts` | + `DexRetencaoTipo`, `DexRetencaoPaciente`, `DexRetencaoData`, `DexMesData`; `DexPendencia.retencaoTipo` |
| `src/lib/dex/retencao.ts` + `.test.ts` | **Novo** — `classificarRetencao`, 12 testes |
| `src/app/api/dex/retencao/route.ts` | **Novo** — `GET /api/dex/retencao` |
| `src/lib/dex/pendencias.ts` | `derivarPendencias` ganha 3º parâmetro (retenção), 3 cards novos |
| `plans/specs/R-103c-o-mes-do-dex.md` | **Novo** — spec do R-103c; D2 (recorrentes) cortada no meio do processo |
| `src/lib/dex/numeros-mes.ts` + `.test.ts` | **Novo** — `calcularNumerosMes`, 7 testes |
| `src/app/api/dex/mes/route.ts` | **Novo** — `GET /api/dex/mes` |
| `src/app/api/dex/context/route.ts` | Remove `consultasSemana`/`orcamentosAprovadosSemana`/`orcamentosPendentes`/`aniversariantesHoje` e as queries que só as alimentavam |
| `src/hooks/useDexHub.ts` | `Promise.all` vira 4 fetches; `construirNumeros` → `construirNumerosMes` |
| `src/components/layout/dex-hub/coluna-numeros.tsx` | Título "Hoje e a semana" → "O mês" |
| `plans/specs/R-103-painel-do-dex.md` (master) | §4 marcado como histórico — as 6 abertas resolvidas, linka pros sub-itens |
| `plans/ROADMAP.md`, `plans/ESTADO.md` | Atualizados ao longo da sessão (fila, R-106, status de b/c) |
