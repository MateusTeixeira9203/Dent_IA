# Handoff — 2026-08-04 01:31

> **HANDOFF** · **R-46** (Meu dia) · sessão #16, seguindo do handoff de 03/08 16:00
> · modo **planejamento ↔ execução**, alternando várias vezes — não foi 1 bloco só de cada.
> Sessão mais densa da semana: testei C2/C3 e depois R-51/R-52 ao vivo (2 rodadas, achando de
> novo o contorno do pane ocluído), fechei as 4 contradições do MAPA-MEU-DIA.md dele, executei
> D1+R-46d D0+R-52, e reabri/resolvi 3 decisões de produto grandes no meio da execução (jaFeito
> sai de vez, campo mágico substitui a barra, IA pode preencher odontometria). **4 commits,
> nada empurrado.** Terminei com `git status` limpo, working tree só com o `ROADMAP.md` desta
> escrita.

## O que trabalhamos

1. **C2/C3 e depois R-51/R-52 testados ao vivo**, achando de novo (2×) o bug do pane ocluído
   (`document.hidden: true` mesmo com o painel visível pro usuário) — o contorno que já
   funcionava (`.click()` nativo via `javascript_tool`, não `computer{}`) se confirmou robusto
   nas duas rodadas, inclusive pra fluxo completo de escrita (Salvar real, Encaminhar real).
2. **Fechei a Fase 0 do `MAPA-MEU-DIA.md`** (documento que ele escreveu sozinho, entre
   sessões) — verifiquei as alegações contra o código/banco reais (achei que o doc tinha
   nascido velho: 3 defeitos que ele descrevia como abertos já estavam commitados de manhã),
   achei 2 furos que o próprio mapa não tinha (D5 sem lugar na ordem de execução; dois itens
   🔵 simultâneos), e levei as 4 contradições (X1/X6/X7/X8) pra ele decidir.
3. **Executei o bloco desbloqueado:** D1 (queixa vazia → null), R-46d D0 (dedup extraído pra
   lib, 8 testes), R-52 completo (modo seleção + `EncaminharBar` + sucesso parcial no
   `encaminharProcedimento`) — os 3 com typecheck/lint/build limpos, R-52 com prova real no
   banco.
4. **Duas decisões de produto grandes reabertas e fechadas no meio da execução**, ambas
   trazidas por ele: (a) o "já feito" sai de vez da coluna direita — o dado vai pro histórico,
   que passa a ser a única fonte; (b) o campo mágico **substitui** a barra de procedimento
   inteira, absorvendo o R-46b e derrubando o caminho de 3 gestos determinístico que o mapa
   dele chama de "o ativo do produto". Argumentei contra em 2 pontos (LLM no milímetro,
   pintura em tempo real) — ele me deu a ressalva certa no primeiro (o dentista sempre revisa;
   o baseline real é 66% de odontometria vazia, não "número certo digitado à mão") e eu
   conceded, registrando como concessão explícita, não meio-termo.
5. **Escrevi 2 specs novas e emendei 4** — `R-53` extraída da doc combinada (corrigindo uma
   contradição real: o X1 já resolvido dizia "evento encaminhado entra no orçamento", mas os
   gates/invariantes do R-53 antigo ainda mandavam excluir); `R-58` (histórico detalhado) nova,
   com a reconciliação "o evento tem 2 datas" (`ficha_id` = onde indicado, `realizado_em` =
   quando feito — aparece nas duas entradas). Emendas em `R-46-C6`, `R-46d`, `R-49`,
   `R-51-53`.

## O que concluímos

**Codado, verificado, commitado (4 commits, `git log` no fim):**
- **D1** — `queixa_principal: ''` → `null` em `salvar-ficha.ts` (2 ramos: create e update).
  Prova por SQL: 4 fichas já afetadas em produção antes do fix.
- **R-46d D0** — `chaveDedupEvento`/`dedupEventosDraft`/`mesclarEventosSemPerda` extraídos pra
  `src/lib/odontograma/dedup-eventos-draft.ts`. 8 testes (`node --test`), todos passando.
  `FichasTab.tsx` perde ~30 linhas duplicadas.
- **R-52** — 🟡 **testado ao vivo com prova no banco.** Criei uma pendência real (raspagem
  Q2), entrei no modo seleção, encaminhei pra outro dentista, e confirmei por SQL:
  `encaminhado_para` setado certo, `dentista_id` preservado (autoria não transfere), e o item
  sumiu da lista de quem encaminhou depois do refresh.
- **R-51** — 🟡 **só typecheck/lint/build + dado sintético.** Não exercitado em cenário
  multi-sessão real (precisaria de um paciente de teste com tratamento em grupo — só havia 1
  disponível hoje, e ele decidiu que a verificação já feita era suficiente).

**Specs escritas/emendadas, nenhuma codada ainda:**
- `R-46-C6` — `aprovada`. `jaFeito` sai de vez (não vira painel — decisão final, 2ª volta).
  Painel do dente = resumo inline + `Sheet`. Colunas redistribuídas (esquerda = o que já
  aconteceu, direita = o que está pendente). Responsivo entra, P8 morre.
- `R-46d` — D0 commitado. D1 ganhou escopo grande (D7-D12): substitui a barra, absorve R-46b,
  anexo vira caixa sob o Histórico, detecção acende o odontograma com **motion** (não cor).
- `R-49` — emenda: I2 ("zero LLM no número clínico") **revogada**. IA pode preencher
  odontometria; tabela do procedimento abre sozinha como guarda-corpo obrigatório.
- `R-53` — spec própria, `contrato`. Query fica ficha-cêntrica (não flat) pra reusar
  `filtro-responsavel.ts` sem adaptar. Medido no banco: 113 indicados abertos, 20 pacientes,
  até 6 fichas por paciente com indicado aberto.
- `R-58` — novo, `contrato`. Texto da visita em evidência; evento fechado depois aparece nas
  duas entradas (indicado e feito). Entra antes do R-53, habilita o C6.
- `R-51-53` (doc antigo) — trimado: R-53 saiu, R-51/R-52 marcados como feitos, contrato
  duplicado do R-53 removido (não só marcado — ver erro #2 abaixo).

**🟡 Nada disso subiu — 4 commits locais, working tree limpo, produção continua em 31/07.**

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| `jaFeito` sai de vez — dado vira só `visitas[].eventos` | "Vira painel sob demanda mantendo `jaFeito`" (1ª leitura, revogada) | Palavra dele: *"o já feito será tudo registrado no histórico"*. Manter os 2 seria manter a redundância, escondida atrás de um clique |
| Painel do dente = resumo inline (leitura) + `Sheet` completo (edição) | Painel único ocupando o slot da coluna direita | Resolve os dois problemas de uma vez: WCAG (odontograma nunca muda de tamanho) e o atrito descrito (card não some debaixo do dedo) |
| X1 — orçamento usa `filtro-responsavel.ts` (mesma lib da ficha), config diferente do "A fazer" | Reimplementar regra própria pro orçamento | Palavra dele: *"a mesma lógica que usamos na ficha e o mesmo modelo"*. 1 lib, 2 configs, impede divergência |
| R-53: query ficha-cêntrica (embed), não flat | `buscarIndicadosAbertos` flat (spec original) | `filtro-responsavel.ts` é ficha-cêntrico por assinatura (autor vem da ficha) — flat quebraria o reuso que o X1 pede |
| R-53: evento encaminhado **entra** no orçamento | Excluir (`.is('encaminhado_para', null)`, spec original) | Excluir = procedimento nunca vira dinheiro, clínica perde receita em silêncio |
| Campo mágico **substitui** a barra de procedimento (D7) | Os 2 convivem (minha recomendação) | Decisão dele — entrada única, sem escolher ferramenta antes de começar |
| Fallback sem IA obrigatório: painel do dente continua registrando sem chamada de rede | Nenhum caminho sem IA | Trava minha, aceita por ele — sem isso existe estado em que registrar é impossível |
| Extração de **valor** fica fora desta rodada, entra depois do R-53 | Extrair valor junto (com trava de confirmação) | Decisão dele — R-53 ainda vai mudar de onde vem o item do orçamento; empilhar risco em cima de risco, em dinheiro |
| I2 do R-49 revogada — IA pode preencher número clínico, com tabela abrindo sozinha | Manter I2 (zero LLM no caminho do número) | **Ele me deu a ressalva certa**: dentista sempre revisa; baseline real é 66% vazio, não "número certo à mão". Concessão registrada, não meio-termo |
| Detecção no odontograma acende com **motion** (pulso/contorno), pinta só ao confirmar | Pintar direto durante a digitação | Preserva a invariante que ele mesmo protegeu ao cortar a Q5 (odontograma = exatamente o que o Salvar grava); frase incompleta não pode virar registro clínico |
| Colunas redistribuídas: esquerda = o que aconteceu (Histórico+anexo+Concluídos hoje), direita = o que está pendente (A fazer+Novos procedimentos+painel do dente) | Manter os 4 blocos amontoados na direita | Princípio dele — "Concluídos hoje" é registro (mesma natureza do Histórico), "Novos procedimentos" é pendência (mesma natureza de "A fazer") |
| Commit em 4 blocos (fix / refactor / feature / docs) | 1 commit grande | R-51+R-52 ficaram juntos apesar de serem itens diferentes — estão fisicamente entrelaçados em `get-meu-dia.ts` (mesma interface, mesma função `pushPendencia`), separar exigiria cirurgia além do que `git add -p` resolve |

## Erros e como pensei em resolver

| Erro | Causa | Como resolvi | Resolvido? |
|---|---|---|---|
| **Argumentei contra IA no milímetro** (R-49) achando que "número certo digitado" era o baseline | Comparei contra o caso ideal, não o real — não tinha o número dos 66% vazio na cabeça no momento do argumento | Ele trouxe a ressalva (dentista sempre verifica, tabela abre sozinha como trava) — reconheci que a comparação certa era contra o vazio, não contra "certo à mão", e revoguei a I2 | **Sim** |
| **Movendo R-53 pra spec própria, envolvi o contrato antigo em `<details>` em vez de remover** | Achei que preservar "histórico" dentro do doc combinado era mais seguro que apagar | O doc foi de 338 pra **443 linhas**, com o conteúdo duplicado nos 2 arquivos — exatamente o "nunca repita entre documentos" que o `CLAUDE.md` proíbe. Percebi sozinho conferindo `wc -l` depois de editar (checagem de rotina, não ele apontando) | **Sim** — removido de vez, ficou 377 (ainda acima do teto de 300, mas por conter R-51/R-52 feitos = virou histórico, não instrução ativa) |
| **A investigação dos 4 procedimentos-em-1-histórico** (proposta dele sobre múltiplas extrações) achei inicialmente que precisaria reabrir o R-54 (cortado ontem) | Não tinha medido — só quando rodei a query de verdade vi que o agrupamento por sessão **já funciona** (fichas com até 24 eventos numa entrada só); o problema só existe se salvar NO MEIO do atendimento | Medi antes de propor solução, mostrei o número (15 fichas com ≥4 eventos, 6 pares em <15min sem evento nenhum) e registrei como pergunta reaberta no R-58 §8, não como bug confirmado | **Não é bug — registrado corretamente como não-confirmado** |
| **R-51 não foi verificado ao vivo em cenário real** — só dado sintético + typecheck/lint/build | Só havia 1 paciente de teste hoje, sem tratamento multi-sessão fácil de montar sem gastar mais tempo de sessão | Perguntei se valia montar o cenário; ele disse que já era suficiente | **Aceito como está, não resolvido** — fica pro próximo teste ao vivo real |

## O que eu estava cogitando

- **Se "tempo real" da detecção (D9) deveria ser streaming de verdade ou debounce.** Não
  decidi — registrei os 3 níveis possíveis na spec do R-46d (§2.2) com debounce como
  recomendação, mas deixei explicitamente como decisão de implementação, não de spec.
- **Se a tabela-abre-sozinha (emenda do R-49) deveria valer pra todo procedimento ou só pros
  que têm campo numérico.** A spec restringe a "procedimento com campo numérico" — não é geral
  pra todos os 17 tipos. Não foi perguntado explicitamente, inferi da frase dele ("o único que
  vai ficar aberto é esse, porque ele é necessário").
- **Se valia tentar montar um cenário multi-sessão sintético agora pra fechar o teste do R-51.**
  Ofereci, ele preferiu seguir pro R-53 — não insisti.

## Arquivos alterados

**4 commits, working tree limpo (só o `ROADMAP.md` desta própria escrita de fechamento):**
```
f942295 docs(plans): mapa do Meu dia e specs da sessão — C6, R-46d, R-51-52, R-53, R-57, R-58
91e78c7 feat(dashboard): multi-sessão e encaminhar em lote no Meu dia — R-51/R-52
47e3402 refactor(odontograma): extrai dedup/merge do FichasTab pra lib compartilhada
faad7a4 fix(patients): queixa vazia grava null, não string vazia
```

**Novos:** `src/lib/odontograma/dedup-eventos-draft.ts` + `.test.ts` ·
`plans/MAPA-MEU-DIA.md` (dele, não meu) · `plans/specs/R-46-C6-layout-cockpit.md` ·
`plans/specs/R-46d-campo-magico.md` · `plans/specs/R-51-53-modelo-multissessao.md` ·
`plans/specs/R-53-orcamento-indicados-abertos.md` · `plans/specs/R-57-atrito-faixa-rapida.md`
(não trabalhada nesta sessão, só existia no working tree) · `plans/specs/R-58-historico-detalhado.md`.

**Nenhuma migration.** Nenhuma mudança de RLS.
