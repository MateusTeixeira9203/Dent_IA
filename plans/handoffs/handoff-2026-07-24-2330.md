# Handoff — 24/07 (tarde/noite): R-04 quebrado em 2, execução R-16+R-04-Fase3+R-02, auditoria ao vivo achou 2 bugs, corrigidos

> **HANDOFF** · **R-16 / R-04 / R-02** (todos 🟡) + **R-17 / R-18** (bugs de auditoria, corrigidos 🟡) · sessão longa
> **Modos:** planejamento (quebra + aprovação R-04/R-16) → execução (R-16, R-04 Fase 3, R-02 Fases 1 e 3) → auditoria completa (fatia Ficha, ao vivo) → execução (fix R-17/R-18) → discussão (modelos)

## O que trabalhamos

- **Quebra da spec R-04.** A spec passava de 400 linhas (teto ~300). Separei em dois itens: **R-04**
  (só encaminhar) e **R-16** (filtro por responsável) — o filtro não escreve nada, não depende do
  modo seleção e entrega valor sozinho. O Mateus aprovou os dois. Rodei uma **workflow adversarial**
  (3 lentes + refutação, 22 agentes) pra confirmar que a quebra não perdeu contrato: nada foi
  perdido pela quebra, mas 5 problemas pré-existentes reais apareceram e consertei (ref de arquivo
  quebrada `agendamentos/page.tsx` → `dashboard/agendamentos/`, FK do join que estava marcada
  "confirmar antes de codar" mas já estava no código, `✅ aprovada` no cabeçalho contra o vocabulário
  do CLAUDE.md, contradição interna sobre o que foi verificado com 2 contas, rótulo Opção A/B do #2
  que a migration 109 referencia).
- **Execução em sequência (R-16 → R-04 Fase 3 → R-02).** O Mateus pediu essa ordem. Codei tudo, com
  a disciplina de extrair a lógica pura pra `lib/` e testar com `node --test` (Node v24 roda TS
  nativo, zero dependência nova) — a única verificação possível sem login. **54 testes**, tsc + eslint
  + `next build` limpos.
- **Verificação independente das claims.** Como o Mateus perguntou "foi tudo isso implementado
  mesmo?", disparei uma **workflow de auditoria** (15 agentes, cada um verificando UMA claim minha
  lendo o código de verdade, sem confiar no meu resumo): **15/15 confirmadas**, 0 refutadas.
- **Auditoria completa AO VIVO na fatia Ficha.** O Mateus autorizou usar a sessão de browser já
  aberta (conta dele, paciente Marcos, "total liberdade"). Criei 3 registros (Canal/endo, Implante,
  Coroa), salvei, encaminhei, filtrei, desfiz — tudo com verificação assertiva (reload real,
  `getComputedStyle`, contagem de `<article>`). **Achei 2 bugs** (R-17, R-18).
- **Corrigi R-17 e R-18.** E o Mateus corrigiu meu diagnóstico do R-17 ao vivo: **não era z-index,
  era posição** — a barra e o dock de navegação (`floating-dock`, centro-inferior) miram o mesmo
  lugar; z-index só decide quem cobre quem. E é **sistêmico** (a `voice-ux` de gravação também
  colide). Abri o **R-19** pra a convenção de raiz.
- **Discussão de modelos** pras 3 sessões que o Mateus quer rodar (design / código / stress).

## O que concluímos

- **R-16 (filtro por responsável) — 🟡.** `lib/fichas/filtro-responsavel.ts` + chips na `FichasTab`.
  Os 3 chips (Todos/Meus/[Dra. Teste R04]) **testados ao vivo** (getComputedStyle + contagem):
  Meus esconde o encaminhado, [Dr X] mostra só o dele. 18 testes unitários. **Não é ✅:** achei o
  R-18 no meio, e o fix do R-18 não foi re-verificado ao vivo (pane travou).
- **R-04 Fase 3 (encaminhar em lote) — 🟡.** `registro-card.tsx` trocou `<select>` por checkbox + ×;
  `encaminhar-bar.tsx` novo. O encaminhamento **grava e persiste de verdade** (visto ao vivo, badge
  sobreviveu a reload). **Bug R-17** (barra atrás do dock em desktop) achado e corrigido, mas o fix
  **não foi visto ao vivo**.
- **R-02 Fase 1 (card único, I1/I2) — 🟡.** `agrupar-registros.ts` unifica a duplicação; rascunho e
  ficha salva usam o mesmo `RegistroCard`. **Visto ao vivo** no rascunho do Marcos (obs editável não
  colapsa o card — o fix de teclado segura; toggle local; EndoForm expansível; símbolos batem).
  **Não é ✅:** falta light mode e mobile completo no gate visual.
- **R-02 Fase 3 (alicerce) — 🟡.** `get-grupos-abertos.ts` + `grupos-abertos.ts` (8 testes). **Só a
  leitura**, sem chamador no app — decisão deliberada (ver Decisões). Sem UI pra ver.
- **R-17 e R-18 (bugs da auditoria) — corrigidos, 🟡.** R-17: `bottom-0 md:bottom-28` (sobe acima do
  dock em desktop). R-18: `filtroAindaValido()` reseta o filtro quando o responsável some. tsc +
  eslint + build + testes limpos; **não re-verificados ao vivo** — o pane travou renderizando a
  ficha (reproduzi até em página que não toquei; é o pane, não o código).
- **Nada commitado.** Combinado: commit no fim.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| **Quebrar R-04 em R-04 (encaminhar) + R-16 (filtro)** | Manter tudo numa spec de 400+ linhas | Teto de ~300 estourado = recorte errado; o filtro é independente, não escreve, entrega valor sozinho |
| **R-02 Fase 3: só a leitura (`buscarGruposAbertos`), sem ligar o auto-reaproveitamento de `grupo_id`** | Wire completo (abrir dente com grupo aberto reusa o grupo_id) | A spec resolveu "sem UI de confirmação AGORA", não "liga sem perguntar". Amarrar errado silenciosamente cria "trabalho fantasma" (risco que a própria spec registrou). A tela que torna seguro é do R-15 (cockpit). **Pendente de confirmação do Mateus** |
| **R-17: fix por POSIÇÃO (`md:bottom-28`), não z-index** | Só subir o z-index pra z-[60] (meu 1º diagnóstico) | O Mateus viu ao vivo: a barra e o dock ocupam o mesmo lugar físico; z-index não resolve sobreposição, só ordem |
| **R-19 (colisão sistêmica com o dock) = item próprio, não remendo agora** | Consertar a voice-ux junto com o mesmo fix | A voice-ux aparece na ficha (com dock) E na consulta (SEM dock — a rota `/consulta` não usa dashboard-shell), então o fix dela é contexto-dependente, não igual ao R-17. Merece convenção, não remendo |
| **Modelos: Fable 5 só pra design; Opus 4.8 pra código; Sonnet 5/Opus 4.8 pra stress** | Fable 5 pros três (intuição do Mateus) | Fable brilha em design (fonte: skill claude-api) mas é o mais caro (US$10/50 vs US$5/25 do Opus) e roda turnos de minutos. Em código/stress o ganho vem do multi-agente (ultracode), não do modelo mais caro |
| **Testes com `node --test` + lógica extraída pra `lib/`** | Instalar vitest/jest | Node v24 roda TS nativo (zero dependência nova); e o CLAUDE.md já manda a lógica morar em `lib/`, não no componente. `*.test.ts` excluído do tsconfig (import `.ts` que o tsc não aceita) |

## Erros e como pensei em resolver

| Erro | Causa / hipótese | Como pensei em resolver | Resolvido? |
|---|---|---|---|
| Diagnóstico errado do R-17 (tratei como z-index) | Vi "barra atrás do dock" e assumi z-order; não percebi que os dois ocupam o mesmo espaço físico | O Mateus corrigiu ao vivo ("não acontece só aí, é sistêmico"); reenquadrei pra posição (`md:bottom-28`) e mapeei todas as colisões centro-inferior (só encaminhar-bar e voice-ux) | Sim (o fix; ver ao vivo 🟡) |
| Não consigo re-verificar os fixes R-17/R-18 na tela | Browser pane trava renderizando a ficha — só o shell do layout aparece, o conteúdo client-side não compõe. Reproduzi em servidor reiniciado, aba nova, e página que NÃO toquei (lista de pacientes) | Isolei que é o pane, não o código (servidor responde 200 com render real ~2s). Marquei 🟡, servidor deixado no ar pro Mateus conferir | Não (constraint de ambiente; fixes são CSS/função pura, risco baixo) |
| `spawn_task`/observação de voz na ficha rápida | O Mateus disse que não queria que a ficha rápida exigisse gravação de voz | Registrei como ponto de PRODUTO separado (não é o bug de layout do R-19); vira conversa quando ele quiser | Registrado (não é bug) |
| Remoção do import morto `X` no FichasTab falhou 2x | O arquivo tem CR solto no meio da linha (`X,\r  Trash2`) — o Edit não casava | Removi via node com match de bytes exato incluindo o `\r` | Sim |
| Coordenadas de clique no viewport mobile não pegavam | Mismatch de escala do tooling no viewport estreito (o clique nem chegava no botão — 0 eventos capturados) | Voltei pro viewport desktop (onde cliques funcionaram) e usei `getBoundingClientRect`/refs; confirmei que era o tooling, não o app | Contornado |

## O que eu estava cogitando

- **R-16 executar junto com a Fase 3 do R-04** (mesmo arquivo `FichasTab.tsx`, contexto quente) — o
  Mateus acabou pedindo a sequência R-16 → R-04 → R-02, então rodou junto de fato.
- **Solução de raiz do R-19: esconder o dock quando há barra contextual** (padrão de "action bar"
  tipo Gmail) vs. a barra ficar acima do dock. Deixei como decisão de design pro Mateus — a de raiz
  (contexto global de bottom-bar) é mais robusta mas mais invasiva; o fix pontual do R-17 desbloqueia
  o encaminhar já.
- **Recontar o header da ficha sob filtro** (R-16): decidi recontar contador/pills/odontograma com
  `eventosFiltrados` (senão o header mente numa ficha mista, que é o caso central do R-04), mas
  deixei a **assinatura** com o estado real — filtrá-la seria bug clínico.

## Arquivos alterados

**Novos (sem commit):**
- `src/lib/fichas/filtro-responsavel.ts` + `.test.ts` (R-16, 18 testes)
- `src/lib/odontograma/agrupar-registros.ts` + `.test.ts` (R-02 Fase 1, 9 testes)
- `src/lib/odontograma/grupos-abertos.ts` + `.test.ts` (R-02 Fase 3, 8 testes)
- `src/server/patients/get-grupos-abertos.ts` (R-02 Fase 3, wrapper server-side)
- `src/components/fichas/encaminhar-bar.tsx` (R-04 Fase 3, barra fixa)
- `plans/specs/R-04-encaminhar-procedimento.md`, `R-16-filtro-responsavel-ficha.md`, `R-02-...md`
- `plans/artefatos/R-04-encaminhar-lote.html`, `R-02-simbolos-odontograma.html`
- `plans/auditorias/2026-07-24-ficha-odontograma.md`
- `supabase/migrations/20260723000002_109_encaminhamento_procedimento.sql`

**Modificados (sem commit):**
- `src/components/pacientes/FichasTab.tsx` (grosso: R-16 + R-04 Fase 3 + R-02 Fase 1 + fix R-18)
- `src/components/fichas/registro-card.tsx` (R-04 Fase 3: checkbox/×; R-02 Fase 1: editavel; fix teclado)
- `src/components/fichas/encaminhar-bar.tsx` (fix R-17: posição)
- backend R-04 já no working tree da sessão anterior (`actions.ts`, `attention-panel.tsx`,
  `dentista-dashboard.tsx`, `notificacoes.ts`, `route.ts`, `notification-bell.tsx`,
  `types/odontograma.ts`, `Odontograma.tsx`)
- `tsconfig.json` (exclui `**/*.test.ts` do typecheck)
- `plans/ESTADO.md`, `plans/ROADMAP.md`
