# Handoff — 2026-08-12 13:23

> **HANDOFF** · **R-103a → pontuais no campo mágico** · sessão #38 · modo execução

<!-- Este arquivo é imutável: é o log do raciocínio da sessão.
     A situação ATUAL (o que falta, o que trava) mora no plans/ESTADO.md, não aqui. -->

## O que trabalhamos

1. **R-103a codado inteiro** — as 7 fases da spec, numa sessão só: mock fora, alertas
   liberados pro dentista, gate de onboarding morto, casca de 3 colunas nos tokens do
   artefato aprovado, sino apagado, novidades reais. `b427391`.
2. **Verificação do R-103a ao vivo, parcial por mim + o resto por ele.** G1/G3/G4/G10 e
   G11 (parcial, só dentista) confirmados por mim na Teste01. G2/G5/G6/G9 e o resto de G11
   ficaram sem acesso meu (login é dele, regra do projeto) — ele verificou e deu sinal verde.
3. **2 pontuais no campo mágico do Meu dia**, achados numa sequência de testes ao vivo dele
   depois do R-103a: campo mágico não limpava ao trocar de paciente no rail (`e5e91f6`) e o
   matcher local misturava dentes de procedimentos diferentes no mesmo relato (`199c232`).
4. **2 achados adjacentes, não corrigidos** — surgiram testando os pontuais: rótulo singular
   do matcher local não casa com relato no plural ("restaurações" não acha "Restauração");
   status realizado/indicado às vezes sai errado na extração por IA (`/api/dex/formatar-evolucao`).
5. **Servidor dev compartilhado com outra sessão** atrapalhou a verificação ao vivo do R-103a
   — Fast Refresh em cascata, depois `/dashboard` parou de renderizar numa aba nova e limpa
   (confirmado via teste de controle: não era o meu código).

## O que concluímos

- ✅ **R-103a** — no ar e **verificado por ele** (as fases 1-6 + gates). Spec fechada,
  arquivada em `_arquivo/specs/`. R-103 (master) segue 🔵 ativo — falta b/c/R-104.
- ✅ **Pontual — campo mágico não reseta entre pacientes** — `key={agendamentoId}` de volta
  no `CampoMagicoMeuDia` (`registrar-painel.tsx`). Confirmado ao vivo por ele.
- ✅ **Pontual — matcher local separa dentes por procedimento** — `casarProcedimentoLocal`
  agrupa por segmento do relato em vez de dar todos os dentes pra todo tipo casado.
  Verificado por mim em harness Node isolado (17/17, sem framework de teste instalado no
  projeto) e confirmado ao vivo por ele (4 procedimentos, cada um só com o próprio dente).
- 📌 **Achado, não corrigido — plural no matcher local.** "Restauração" normalizado é
  `restauracao`; "restaurações" normaliza pra `restauracoes` — a troca ção→ções muda ã→õ,
  não é só acrescentar "s". Confirmado que também quebra Canal/canais, Lesão
  periapical/lesões periapicais, Coroa total/coroas totais, Exame periodontal/exames
  periodontais (padrão -al→-ais, -ão→-ões). Pré-existente — não foi o pontual desta sessão
  que causou, só expôs.
- 📌 **Achado, não corrigido — status realizado/indicado errado na extração por IA.**
  Relato com "encontrei uma lesão periapical" (achado, não procedimento feito) saiu
  **Realizado**; "fratura" — mesma categoria clínica no código (`types/odontograma.ts`
  comenta os dois como "achado") — saiu **Planejado** corretamente. Inconsistência real,
  não investigada a fundo — é prompt de IA, CLAUDE.md exige eval antes/depois de mexer.

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Executar o R-103a sem esperar aprovação formal da spec | Esperar ele aprovar o rascunho primeiro | `ESTADO.md` já registrava que o R-103a podia começar sem resposta — nenhuma das 6 abertas do master bloqueava essa fatia |
| Usar a sessão logada dele em `localhost:3000` (servidor de outra sessão) em vez de subir um meu | Insistir em `dev-alt`/3010 ou servidor manual 3011 | O lock do `.next/dev` é por pasta, não por porta — as duas tentativas próprias esbarraram em `EADDRINUSE`/lock; ele mesmo ofereceu a sessão já logada |
| `pendencias.ts`: excluir `computed_followup` dos alertas, usar só `ctx.followUpPendentes` | Mostrar as duas fontes como cards separados | Mesma janela (+3 dias) computada 2× por rotas diferentes — mostrar as duas seria visivelmente duplicado |
| Coluna 2 do R-103a = "Hoje e a semana", não porta o "O mês" do artefato | Copiar o bloco do artefato mesmo sem a query existir | D10 da spec: provisório de propósito — R-103c constrói o comparativo mensal de verdade |
| `dentistaId` sai do `DexWidget` (só servia o onboarding morto) | Manter a prop "por via das dúvidas" | Zero uso real no código novo — YAGNI |
| `sidebar-content.tsx`: só remove o import morto do `NotificationBell`, não apaga o arquivo | Apagar `sidebar.tsx`/`sidebar-content.tsx` também (grep não achou importador) | Fora do escopo do R-103a — candidato a código morto é trabalho do R-95; o mínimo pra manter o build compilando |
| Pontual do campo mágico: `key={agendamentoId}` de volta, só no `CampoMagicoMeuDia` | Resetar o texto manualmente via prop nova | É exatamente o mecanismo que existia antes do R-78 F0 remover (documentado no próprio código); `CapturaLivreCard` é estado privado, não alcançável de fora sem isso |
| Pontual do matcher: agrupar por segmento (vírgula/" e ") | Reescrever com IA ou stemming completo de português | Mecanismo mínimo que resolve o caso reportado sem virar decisão de produto |
| Não corrigir plural nem status realizado/indicado nesta sessão | Continuar cutucando dentro do pontual | Plural exige escolher uma abordagem de stemming em ~5 rótulos (decisão, não bug mecânico); status é prompt de IA — regra dura do CLAUDE.md exige eval antes/depois |

## Erros e como pensei em resolver

| Erro | Causa (ou hipótese) | Como pensei em resolver | Resolvido? |
|---|---|---|---|
| Usei `ScheduleWakeup` fora de `/loop`, 2 vezes na mesma sessão | Confundi com "esperar" genérico de novo — o mesmo erro já estava registrado em memória de uma sessão anterior | Cancelei nas duas vezes assim que percebi (`stop: true`) | Sim, mas repeti o mesmo erro — a memória não impediu na hora, vale reforçar |
| `process.exit()` no meio do script de verificação cortou o teste do caso ao vivo antes de rodar | Escrevi o `exit` antes de acrescentar código novo via `bash cat >>`, sem reler a ordem final | Movi o `exit` pro fim do arquivo com o Edit tool | Sim |
| Hand-trace do agrupamento contra "restaurações" (plural) deu resultado errado duas vezes seguidas, no papel | Assumi que "restauração"→"restaurações" era só acrescentar "s"; na real ção→ções troca ã→õ, produz uma raiz comum de 9 caracteres, não uma superstring do singular | Parei de tracejar no papel e rodei o texto de verdade num harness Node isolado | Sim — foi isso que revelou a causa raiz certa (não a que eu tinha assumido) |
| `node --test` não resolvia os `@/` do projeto (sem `tsx`/`ts-node` instalado, `package.json` sem script de teste) | Path alias do `tsconfig.json` não existe pra Node puro em ESM | Escrevi um loader ESM customizado (`@/` → `src/` + extensão); quando a cadeia de import puxou um `.tsx` (componente React) só por 4 constantes, abandonei o loader e copiei a lógica pura pra um script standalone no scratchpad | Sim — rodou e confirmou os 17 casos (12 existentes + 5 novos) |
| Servidor dev compartilhado (porta 3000, outra sessão usando ao mesmo tempo) ficou instável no meio da verificação do R-103a e depois do pontual 1 | Outra sessão editando arquivos ao mesmo tempo — Fast Refresh em cascata visível no console | Tentei servidor próprio (`dev-alt`/3010, manual/3011) — esbarrei no lock do `.next/dev`, que é por pasta, não por porta. Voltei a insistir no compartilhado só depois de confirmar via teste de controle (`/dashboard`, que tinha funcionado antes, também parou de renderizar numa aba nova) que a causa não era o meu código | Não resolvido tecnicamente — fica registrado como limitação da sessão, sem inventar que verifiquei o que não consegui verificar |

## O que eu estava cogitando

- **Se valia consertar o plural com um stem curto** (cortar as 2 últimas letras do rótulo
  antes de comparar). Funciona pra "restauração"/"canal" isolados, mas quebra em rótulos de
  duas palavras que pluralizam cada metade ("exame periodontal" → "exames periodontais") —
  descartei rápido: viraria remendo frágil sem decisão clara de até onde ir, exatamente o
  tipo de coisa que o pontual não deveria decidir sozinho.
- **Se o achado do status realizado/indicado é bug de prompt ou de heurística de verbo**
  ("fiz" = feito, "encontrei" = achado). Não confirmei — só reparei que "fratura" e "lesão
  periapical" são a MESMA categoria clínica no código (`types/odontograma.ts` comenta os
  dois como "achado, como lesao_periapical") e saíram com status diferente no mesmo relato,
  o que sugere inconsistência real na extração, não uma regra que eu simplesmente não
  enxerguei ainda.
- **Se devia esperar o servidor compartilhado "acalmar"** antes de desistir da verificação
  ao vivo do R-103a. Decidi que não, dado quanto tempo já tinha passado tentando e que ele
  mesmo ofereceu a sessão logada como alternativa mais rápida.

## Arquivos alterados

| Arquivo / grupo | Mudança |
|---|---|
| `src/app/api/dex/{context,alerts}/route.ts` | R-103a — D9 (4 campos mentirosos fora, rename `entrouHoje`), guard de status (R-65), `scopado` (D11), janela hoje+amanhã, PATCH confere linhas |
| `src/components/layout/dex-widget.tsx` | R-103a — enxugado pra mount+portal+listener; HomeView/gerarInsights/MOCK_OPS saem |
| `src/components/layout/dex-hub/*.tsx` | R-103a — **novo**: casca de 3 colunas (modal, pendências+Aconteceu, Hoje e a semana, Novidades) |
| `src/hooks/useDexHub.ts` | R-103a — **novo**: Promise.all das 2 rotas, realtime, marcar lida, badge |
| `src/lib/dex/{tipos,pendencias}.ts`, `src/lib/novidades.ts` | R-103a — **novos** |
| `src/components/layout/{dashboard-shell,floating-dock,sidebar-content}.tsx` | R-103a — hub monta pra secretária (D4), badge na bola, sino sai |
| `src/components/layout/{dex-presence,notification-bell}.tsx` | R-103a — **apagados** |
| `plans/ROADMAP.md`, `plans/ESTADO.md`, `plans/_arquivo/specs/R-103a-*.md` | R-103a — fechamento do item (`7b5acab`) |
| `src/app/dashboard/meu-dia/_components/registrar-painel.tsx` | Pontual — `key={agendamentoId}` no `CampoMagicoMeuDia` (`e5e91f6`) |
| `src/lib/odontograma/casar-procedimento-local.ts` + `.test.ts` | Pontual — agrupamento por segmento, 2 testes novos (`199c232`) |
